# CLAUDE.md — Joinzer

> Read at session start. Reality file: describes what is actually built and how we work.
> For target architecture (aspirational), see docs/architecture-target.md.
> For security rules, see @docs/security.md.

---

## 1. What We're Building

**Joinzer** is a mobile-first pickleball platform with **four product surfaces** sharing one app, one auth, one database:

1. **Coordination** — players create, discover, and join local play sessions. Original MVP.
2. **Leagues** — recurring competitive play with persistent rosters, weekly sessions, season standings.
3. **Tournaments** — discrete events with divisions and registrations.
4. **Players** — searchable directory with profiles, ratings, history, connections.

**Pilot market:** Las Vegas metro (Henderson, Summerlin, Green Valley, North Las Vegas).

All four surfaces share users, profiles, locations. Bottom nav: Home / Play / Leagues / Tournaments / Players / Profile.

---

## 2. Product North Star

- **Coordination:** Player-first. Speed of "find a game and show up" is the metric.
- **Leagues:** Organizer-and-captain-first. Season reliability and roster fairness.
- **Tournaments:** Organizer-first. Tournament-day reliability and the player↔organizer loop.
- **Players:** Discovery-first. Finding the right partner, opponent, or community.
- Setup surfaces are desktop-first. Day-of and player-facing surfaces are mobile-first.
  See `/docs/phases/two-form-factor.md`.

---

## 3. Build Philosophy

- Small, focused changes. Preserve working code.
- Read existing code before changing it.
- One concern per slice. Data model changes and layout changes do not share a PR.
- If a doc claim contradicts the codebase, **the codebase wins.** Flag it immediately and stop.

---

## 4. Stack (Actual)

| Layer | Choice |
|---|---|
| Frontend | Next.js 16 (App Router), React 19, TypeScript strict |
| Styling | Tailwind CSS only (no shadcn/ui, no Radix) |
| Icons | lucide-react |
| Backend | Supabase (Postgres + Auth + RLS) |
| Auth | Production: email/password + Google OAuth |
| Hosting | Vercel (frontend) + Supabase (backend) |
| Transactional email | Resend (registration / payment / refund / partner-match / sub-request / session-reminder / organizer announce) |
| In-app notifications | Implemented — `notifications` table, bell + panel in app header, deep links, 12+ triggers wired. Browser push is **live** — VAPID keys set in Vercel + a runtime key route (`/api/push/vapid-public-key`); the Enable toggle subscribes. |
| Payments | Stripe Checkout for tournaments / leagues / events; Stripe Connect Express for organizer payouts; refunds with reverse-transfer; tournament discount codes |
| Analytics | Google Analytics 4 via `@next/third-parties/google`, mounted in `app/layout.tsx`, ID from `NEXT_PUBLIC_GA_MEASUREMENT_ID`. **No Google Tag Manager.** Vercel Web Analytics / Speed Insights are **not** wired into the app (no `@vercel/*` package installed). |

**Do not introduce:** shadcn/ui, Radix, Redux, tRPC, Prisma, custom ORMs, Docker, CI pipelines beyond Vercel default.

---

## 5. Current State — Verified July 16, 2026 *(court-directory geocode cache re-verified August 4, 2026; the Albany / Worcester / Bakersfield publish verified against production August 5, 2026; worktree bootstrap/teardown verified by a real bootstrap + teardown August 5, 2026; the wave-3 14-metro release verified against production August 6, 2026 — 1,202 published across 48 live metros, sitemap and metro-page counts checked directly; every published row's outbound URLs probed August 6, 2026 by the link-health sweep, read-only; the three dead-domain rows re-verified and **repointed rather than retired** August 6, 2026, checked against production — **1,201 published across 48 live metros** (the wave-3 figure of 1,202 less `big-house-pickleball-colorado-springs-co`, drafted by the Colorado Springs session), Fresno 17 / Augusta 15 / Portland 41 and the sitemap counted directly — those areas only; the three import-pipeline expression gaps closed August 6, 2026, with **both production writes applied and verified against production the same day** — AdventHealth repaired to `location_precision='high'` and rendering with no approximate label, `tampa-addendum-2` published, site-wide **published 1545 → 1546 / listings 2364 → 2365 / distinct_slugs equal to total**, counted in SQL; **the eight-metro v3 build wave verified against production August 6, 2026 — 1,546 published across 52 live metros, per-metro counts and the site totals read in SQL, superseding every "48 live metros" figure earlier in this clause**; **user-submitted-venue path verified against the live database August 6, 2026** — the `locations.metro_area` NOT NULL failure reproduced and its fix confirmed by a rollback-wrapped probe, and the full `facility_listings` column/CHECK set audited against production; the rest of this section still carries the July 16 date)*

For specific schema details, check Supabase Table Editor. For specific route details, check the codebase. This section captures **what's shipped vs. not** at the phase level — not column-level detail.

### Shipped

- **Coordination MVP** — working in production
- **Tournaments product** — divisions, registration (solo + team), partner invites, brackets, live scoring, check-in (incl. QR), match reschedule, waitlist with auto-promote
- **Leagues product** — registration, rosters, weekly sessions, attendance, substitute pool + sub-request flow, scoring, standings, group chat, **bulk player CSV import** (organizer/co-admin; PR #339 — `/leagues/[id]/import` + `lib/leagues/csvImport.ts`; single-roster, one player per row; existing accounts register, unknown emails → stub + magic-link invite; entry point on the Overview), **player score entry setting** (`leagues.allow_player_scores`, migration `20260709000006`; PRs #343–#345, Create/Edit toggle). When on, a registered **participant** may score their own match, saved directly (organizer can always edit, which works on every format now). Wired for **all** formats via a "Your matches" surface: box/ladder (shared `fixtures/[fixtureId]/score` route relaxed for participants), round-robin (`sessions/[sessionId]/player-score` → `league_matches`, sub-credit aware), team (`.../lines/[lineId]/player-score` rolls up via `rollUpMatchup`); flex was already player-driven. The tournament `allow_player_scores` toggle is now **wired too** (PR #401 — see "Player-run wave" below); it was previously a stored-only stub.
- **Payments** — Stripe Checkout end-to-end; Stripe Connect Express onboarding for organizers; destination charges route fees to the organizer's account with `on_behalf_of`; refunds reverse the transfer and refund the application fee; tournament-level discount codes
- **Tournament organizer tools** — co-organizer + volunteer roles via `tournament_staff`, CSV team import, organizer-driven match reschedule, organizer-driven withdrawals with waitlist promotion
- **Transactional email** — Resend integration covering registration confirmation, payment confirmation, refund notice, solo partner-match notification, sub-request flow, daily session reminders (cron), and organizer-to-bracket announce
- **Two-form-factor refactor Slices 0–6** — all desktop-canonical routes shipped: primitives, tournament create/manage/sub-routes, league create/manage/sub-routes (standings/roster/edit)
- **Audit log** — `audit_log` table + `lib/audit/log.ts` helper. Wired into: match score, match ready, match reschedule, registration cancel/withdraw/refund, league sub request claim/approve/cancel. Still unwired: tournament/league create, division edits.
- **Partner mode setting (Fixed vs Rotating)** — tournaments + leagues both expose an organizer toggle on doubles divisions/formats. Leagues: `leagues.partner_mode` enum; scheduler honors `partner_user_id` cross-links in fixed mode. Tournaments: support rotating for `round_robin` bracket type only (`tournament_divisions.partner_mode`; `tournament_matches.team_*_partner_registration_id` columns hold the 4-player layout).
- **Email log** — `email_log` table; all transactional emails logged via `lib/email/send.ts` (recipient, subject, Resend ID, status).
- **Fixed-partner assignment UI** — Roster page shows "Team X/Y" pairs for fixed-partner leagues; organizer assigns/changes via `/api/leagues/[id]/assign-partner`. Teams shown in attendance and match cards in Live Session Manager.
- **Tournament division defaults** — Create Tournament sets default format (round robin/single/double elim), points-to-win (11/15/21), win-by (1/2), and primary venue. Divisions inherit all and can override. Columns: `tournaments.default_win_by`, `tournaments.default_games_to`, `tournaments.default_bracket_type`, `tournament_divisions.location_id`.
- **Gender validation** — All tournament registration paths enforce gender for mens/womens divisions (RPC + route + auto-pairing). League roster add-player dropdown filters by gender for gender-specific formats.

- **In-app notification center** — `notifications` table + RLS + indexes. Bell + panel in app header (all breakpoints). Deep links per notification. 12+ triggers wired across tournaments, leagues, Stripe webhook, cron.
- **Browser push — LIVE (fixed July 14, 2026)** — a runtime route `GET /api/push/vapid-public-key` serves the (public, safe) key from the runtime env + a legible guard, replacing the fragile build-time `NEXT_PUBLIC_` inlining that broke it; valid VAPID keys are set in Vercel (the route returns a valid 65-byte key; the Enable toggle subscribes). Full implementation: `profiles.push_subscription` column (migration `20260601000001`), service worker (`public/sw.js`), subscribe/unsubscribe API (`app/api/push/subscribe`), VAPID sender with expired-subscription cleanup (`lib/push/send.ts`), `PushSubscribeButton` mounted in Profile + NotificationPanel, and `sendPush`/`sendPushBatch` wired into `lib/notifications/create.ts` so every in-app notification also fires a push. **A VAPID keypair was generated and all three env vars (`NEXT_PUBLIC_VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT`) are set in Vercel + local (`.env.local.example` has the names, PR #257).** The bug that was blocking it (now fixed): clicking "Enable push notifications" on live `/profile` doesn't subscribe — `PushSubscribeButton.enable()`'s `pushManager.subscribe()` throws (3 unread `[push] subscribe failed:` console errors). **Prime suspect:** `NEXT_PUBLIC_VAPID_PUBLIC_KEY` is inlined at **build** time, so if it wasn't set on the **Production** env before the last build ran, `process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY` is `undefined` in the client bundle → `urlBase64ToUint8Array(undefined)` throws a `TypeError`. To resume: read the exact console error, confirm the var is on Production, redeploy with a fresh build, hard-reload (SW cache `jz-shell-v3`). Note: `PushSubscribeButton` does **not** gate its visibility on the key — it renders on any active SW and only reads the key inside `enable()`.
- **Players directory** — `/players` with search, skill/gender filters, availability. `/players/[id]` individual profile page. Cards link to profiles; invite-tap still works when player is available.
- **Public browse pages** — `/browse/leagues` and `/browse/tournaments` accessible without auth. LandingNav/Footer shell. Middleware allowlisted. CTAs on landing page link directly to browse.
- **Upcoming / Past toggle on the three list pages (July 15 2026)** — Play, Leagues, and Tournaments each show an "Upcoming | Past" segmented toggle (shared server component `components/features/UpcomingPastToggle.tsx`, `?when=past` URL param, preserves other filters). Past reuses the same cards + existing search/filters, newest-first. Per-surface "past": tournaments `start_date < today`; play `starts_at < now` incl. `completed`, excl. `cancelled`; leagues `completed` OR active-with-`end_date`-passed (excl. `cancelled`). Upcoming leagues tightened to active AND (`end_date` null or ≥ today) so an ended season moves to Past. The tournaments list also now filters to **published + public** (drafts / cancelled / private hidden), except a logged-in organizer still sees their **own** (any status/visibility) so an in-progress draft isn't lost from the list.
- **Prizes & Awards — advertised (July 15 2026)** — organizers list prizes (cash / trophy / medal / merch / other, each a place + description) on **tournaments, leagues, and play sessions** via a shared `PrizesEditor` in all 6 create/edit forms (stored on a `prizes` jsonb column — migration `20260715000003`); players see a `PrizesDisplay` "🏆 Prizes & Awards" panel near the register CTA on all 3 detail pages. **Display-only — Joinzer moves no prize money** (organizers hand them out; no escrow/payout). Shared type + icon map + `normalizePrizes` in `lib/prizes.ts`. Mirrors the `refund_policy` wiring exactly.
- **Marketing site** — updated hero ("Play. Compete. Find your game."), trust strip, CompeteSection CTAs, login tagline. OrganizersSection added (PR #39).
- **Advanced Schedule Builder** — merged to main and live on Vercel (PR #58). Organizer surface at `/tournaments/[id]/schedule/builder`: define date/time/court "blocks", drag divisions into them, see capacity + player-conflict warnings, then generate a draft match schedule (greedy per-block scheduler), preview by time/court/division, and publish. Draft matches carry `tournament_matches.is_draft=true` and are filtered out of all participant/organizer reads until published. Schema migration `20260615000001_advanced_schedule_builder` applied to prod (`tournament_schedule_blocks`, `tournament_division_blocks`, `tournaments.schedule_settings_json`, `tournament_matches.schedule_block_id`+`is_draft`). MVP limitation: elimination BYE auto-advance not applied to drafts. Builder division drag-and-drop uses `@dnd-kit` (pointer + touch + keyboard sensors; dropdown fallback retained) — the old native HTML5 DnD didn't work on touch and was unreliable cross-browser. The legacy one-click per-division "Generate Matches" flow is unchanged and coexists. Schedule generation now lives in two places: the Schedule Builder and the per-division "Generate Matches" flow (division Manage). The tournament-level one-click **"Generate Full Tournament"** card (`ScheduleGenerator`) was **removed June 17, 2026** to consolidate scheduling into those two surfaces — the card was unmounted from the organizer overview and the component file deleted. The non-functional "Reschedule" quick-action chip (placeholder "coming soon" toast) was removed from the organizer Live view at the same time — per-match reschedule still lives on the Schedule tab.
- **Bracket correctness (June 17, 2026)** — single elimination now distributes byes across the bracket (no phantom "BYE vs BYE"; non-power-of-2 fields get proper player-vs-BYE auto-advances), and the bulk `generate-all` path respects locked seeds + dedupes via the shared `buildMatches`. **Double elimination** got a full **losers-bracket rewrite** plus the if-necessary **bracket reset** — the winners-bracket-final loser now drops into the LB final, and the LB champion must beat the undefeated WB champion twice. Verified end-to-end by a full-bracket simulation for 4/8/16-team fields (`lib/tournament/__tests__/{doubleElimSimulation,bracketReset,bracketByes}.test.ts`). **Non-power-of-2 double elim (byes) is now verified end-to-end too** — `doubleElimNonPow2.test.ts` plays out every field size 4–32 (single champion, everyone else eliminated at exactly two losses, reset included), and `playoffPlaceholders.test.ts` does the same for the up-front placeholder double-elim bracket (pool playoffs) at sizes 5–12 incl. the if-necessary reset.
- **Print-ready bracket export (June 17, 2026)** — the organizer **Export** quick action (Live view) opens `/tournaments/[id]/print`, a print-optimized page that renders every active division's bracket (reuses `BracketView`, read-only) for Save-as-PDF / printing — landscape, white background, one division per page, auto-opens the print dialog.
- **Schedule Builder — draft scheduler hardening + inline controls (June 18, 2026)** — the greedy per-block draft scheduler (`lib/tournament/scheduleGenerator.ts`) now: holds later-round matches until their feeder rounds finish (per-division dependency floor, so no "TBD vs TBD" at the same slot as round 1), keeps a division clustered **on its own courts** for later rounds, prevents court double-booking across overlapping blocks, and no longer crashes on a midnight time overflow. Pinned by `lib/tournament/__tests__/scheduleGenerator.test.ts`. The Builder **preview** gained **inline draft controls**: per-division **Regenerate** (rebuilds just that division by re-packing its whole block) and inline **edit** of a draft match's court/time (✎) in all three views; the generate/regenerate POST now returns a **lean summary** and the builder **refetches drafts via GET**. Preview UX: date column in By Court/By Division, By Time sorted by court within each slot, and a warning when two blocks share courts at overlapping times.
- **Offline tournament run mode — code-complete, pending real-device QA (July 1, 2026)** — a single organizer on one device can run an **entire** tournament with **no connectivity**. Surface: `/tournaments/[id]/run` (`<RunMode>`), reached via **"Run offline"** in the organizer manage nav. Open it once online → the whole tournament (divisions, matches, registrations) is hydrated into **IndexedDB** (`lib/offline/tournamentDB.ts`, whole-tournament store); go fully offline → the `/run` page **cold-loads** from the service-worker cache and reads IndexedDB. Offline you can **score** (reuses the Phase-1 `BracketView` local-advance engine), **check players in**, **seed playoffs from standings**, and **reschedule** court/time — each op applies to the store immediately and queues in a unified **outbox** (`lib/offline/outbox.ts`, IndexedDB FIFO, dedupe-by-key). On reconnect, `lib/offline/reconcile.ts` drains scores→outbox FIFO and, **only when fully drained**, bulk-refetches and replaces the store (so it never clobbers un-synced writes; server-assigned ids like the double-elim reset row become authoritative). New organizer check-in route `PATCH /api/tournaments/[id]/registrations/[regId]/checkin` (`canOperate`-gated, idempotent). Generation stays an **online setup** action — offline run mode only scores/seeds/checks-in/reschedules an already-generated tournament (up-front placeholder brackets mean the whole bracket already exists). **Offline is lead-organizer only (Phase 3, Option 1):** the "Run offline" nav entry shows for the tournament owner only, and `<RunMode>` is **read-only for co-organizers/volunteers** (`offline-bundle` returns `is_lead_organizer`; a banner points non-leads to the live view) — so there's exactly **one offline writer per tournament** and the single-writer reconcile stays sound. Migration `20260626000003_add_playoff_slot_sources` (`tournament_matches.team_1_source`/`team_2_source` jsonb) is applied to prod. Full design + build order in `docs/phases/offline-run-mode-phase-2.md` (Phase 1: `offline-scoring-phase-1.md`; multi-writer design: `offline-multi-device-phase-3.md`). **Not yet enabled beyond code:** needs a manual airplane-mode pass on a real phone against the Phase-2 acceptance criteria. **Full multi-device / co-organizer offline (concurrent writers + conflict resolution) is Phase 3, Option 2 — designed, deferred** until a multi-court no-signal event needs it.

- **Rolling Schedule mode (July 2, 2026, PR #177)** — a second scheduling method alongside the original **Timed** (`tournaments.scheduling_method` enum, `'timed'` default | `'rolling'`; toggle on Create/Edit). **Rolling** has no clock times after the first round: matches carry a tournament-wide **Match #** (`tournament_matches.sequence_number`, nullable, shown in **both** modes) and are called by number as courts free up. Generation lives in `lib/tournament/schedule/`: `buildRollingSchedule` reuses the extracted, behavior-preserving `orderByDependency` (from `scheduleGenerator`), distributes matches round-robin across a block's courts **capped at peak concurrency** (a 6-player RR uses 3 courts even if the block offers more), stamps the block start time on the **first match of each court** (rest untimed), and `assignSequenceTimed`/`assignSequenceInOrder` assign the stable 1..N Match # **once at generation** (never on reschedule). `generate-schedule` branches on `scheduling_method`. Rolling **reuses the Advanced Schedule Builder** (time-interval settings + block End hidden; block Start = "first matches start at"). Display: `BracketView` shows a "Match N" chip and renders a match time only when present (via IsRolling threading, now presence-based); organizer Schedule tab gains a "By Match #" view; LiveScoreboard shows "Court n · Match k"; SchedulePreview shows a "By Match #" ordered list. **Timed is unchanged** — every gate defaults to timed, `sequence_number` is additive/nullable (existing tournaments untouched until regenerated), and the `orderByDependency` extraction is pure ordering (existing scheduler tests unchanged). Migration `20260702000001_rolling_schedule` applied to prod. Architecture is extensible to a future **Hybrid** (add to the `SchedulingMethod` type + the one generation branch). Match Number is now first-class for **both** methods (TDs reference "Match 17"). Scheduling method is also a **per-division override** (PR #182): nullable `tournament_divisions.scheduling_method` (null → inherits the tournament) with a "Scheduling" selector on the division add/edit forms, so a rolling tournament can host a timed division and vice versa; `generate-schedule` splits each block by the division's *effective* method (timed rows → `scheduleBlockMatches`, rolling rows → `buildRollingSchedule`) into one dense tournament-wide Match #. Live **court board** on the public scoreboard for rolling tournaments (per court: "In Progress: Match N / Next: Match M", auto-advances via realtime). "Run offline" is signposted (nav "Run offline (no wifi)" + in-page explainer; lead-organizer only).

- **Show seed numbers setting (July 2, 2026, PR #184 + #185)** — organizer toggle to display or hide seed labels (#1, #2…) next to teams/players. Tournament-level default set on Create/Edit (`tournaments.show_seeds` boolean, default `false`) that becomes the default for every division, with a nullable per-division override (`tournament_divisions.show_seeds`; null → inherits the tournament). Effective value everywhere = `division.show_seeds ?? tournament.show_seeds`, applied consistently across **all** views — brackets, Schedule, Scores, Export/print, and offline Run mode — via `BracketView`'s `showSeeds` prop (previously the export hardcoded seeds on). The division-page SeedingPanel checkbox now persists the per-division override server-side (replaced the old per-browser localStorage toggle). Migration `20260702000003_show_seeds_setting` applied to prod. **Auto-seed (PR #185):** because "Show seed numbers" is only a *display* toggle and seeds were previously written only by the manual "Save Seeds" flow, turning it on but never seeding left every `seed` null → nothing rendered. Now, when a division's effective `show_seeds` is on and no seeds were set manually, all three generation paths (per-division `generate-matches`, Schedule Builder `generate-schedule`, bulk `generate-all`) persist seeds 1..N in **bracket-build order** via the shared `lib/tournament/autoSeed.ts` `persistAutoSeeds` (`buildDivisionMatchRows` now also returns the team build-order + whether explicit seeds existed). Manually-seeded fields are untouched; rotating-doubles is skipped. The Schedule Builder draft preview also prefixes `#N` on team labels when the division shows seeds. Existing tournaments generated before this won't retro-fill until re-generated. Nullable division column → existing tournaments unchanged (seeds hidden by default).

- **Box leagues + unified attendance (July 6, 2026, PRs #225–#236)** — **Box leagues** (`leagues.format_kind='box'`) run tiered boxes per **cycle** (`league_periods` / `league_boxes` / `league_box_members`), round-robin `league_fixtures`, promotion/relegation, and per-cycle standings (results grouped by round). The organizer runs a box league entirely from the **Run Session** surface (`/leagues/[id]/attendance`, always shown for box admins via `lib/leagues/runSession.ts` `getRunSessionAction`), which is the box hub in order: **(1) seed** — pick the **number of boxes** on game day (there is *no* create-time box size); players auto-fill evenly via `lib/leagues/boxAssignment.ts` `distributeIntoBoxes` (sizes differ by ≤1, remainder to the top boxes — 13→5/4/4; stored as `format_settings.num_boxes`), shown only until matches are scored; **(2) attendance + subs**; **(3) generate + score matches**; **(4) advance** the cycle (promotion/relegation carries the box count forward). The **Roster** page is registration-only. The round-robin **attendance grid + substitute flow** (Here / Coming / Late / Can't Come / Sub / Not Here) is **shared across formats** via `components/features/leagues/AttendanceGrid.tsx` (normalized `AttendeeRow[]` + the pure `buildAttendeeRows` sub-overlay resolver in `lib/leagues/attendance.ts`), backed by the format-agnostic **`league_attendance`** table (occasion = session XOR cycle `period_id`; attendee = registration / user / guest; RLS deny-all + service role). Box members are grouped by box; a sub's fixture results credit the covered **registration** so standings/promotion stay correct with no scoring change. Round-robin still stores attendance on `league_session_players` (intentionally **not** migrated onto `league_attendance` — rationale in `docs/phases/unified-attendance.md`). v1 attendance/subs are **entrant-level** (a team in doubles, a player in singles). Design: `docs/phases/unified-attendance.md`, `docs/phases/league-formats.md`.

- **Ladder League (July 7, 2026)** — a third league format (`leagues.format_kind='ladder'`) alongside Round Robin and Box: a season-long **continuous ranking** (`ladder_positions`, `1..N`) updated after **king-of-the-court** nights, with rank **trend** (`ladder_position_history`). Distinct from Box on purpose (Box = tiered round-robin groups; Ladder = adjacent, movement-based rank). Each session is a `league_periods` row (`period_kind='ladder_session'`); per-court games reuse `league_fixtures` (`round_number` + `court_number`, `match_stage` `ladder_round`/`ladder_bye`); attendance/subs reuse `league_attendance`. **Play:** entrants (singles or fixed-partner doubles teams) seed onto **courts of 2** by rank (round 1), then each round the **winner moves up a court / loser down** (`rounds_per_session`, default 6). **Movement:** after the night, bounded — a player moves **≤ `max_move`** spots (default 3) toward the night's win-% order (`computeFixtureStandings`), via odd-even transposition; **absent entrants hold rank**, a **sub plays for the covered entrant's spot**. The organizer confirms via a **preview → Finalize** step (no auto-mutation). Pure engine in `lib/leagues/ladder.ts` (initial ranking, `seedKotcRound`/`nextKotcRound`, `boundedMovement`, `reintegrateRanking`; 16 tests); server reads/finalize in `lib/leagues/ladderServer.ts`. Surfaces: Create/Edit format option + settings; Roster hosts the drag-to-order editor (`POST /ladder/rank`); Run hub `/leagues/[id]/ladder` (attendance via the shared `BoxAttendanceManager`, KOTC rounds via `LadderRounds`, routes `/ladder/{start-session,round,finalize}`); Standings shows the ranking + `▲/▼` + trend `Sparkline`; overview shows the viewer's rank + tonight's court. Migration `20260707000001_ladder_league` applied to prod. **v1 limits:** no re-finalize after a session closes (correct scores first, else manual reorder on Roster); absent = hold rank only. Not reused: box `applyPromotionRelegation` (leapfrogs winners to the tier top — wrong for a continuous ladder). Design supersedes the *challenge*-ladder sketch in `docs/phases/league-formats.md §10`.

- **Rating system — Phase 2 shipped (calculated engine live, July 8, 2026, PRs #272–276)** — the **Joinzer Score is now calculated** and **player-visible when earned**. Engine in `lib/rating/`: `glicko2.ts` (Glicko-2, validated vs Glickman's worked example), `engine.ts` (`computeRatings` — weekly rating periods; doubles = play vs the opposing team average; tracks games + distinct events; `confidenceState` = the locked gate: RD<110 AND ≥15 games AND ≥3 events), `normalize.ts` (rating⇄0–100 Score; rating 1500→Score 50), `extract.ts` (RR `league_matches` + box/ladder `league_fixtures` + `tournament_matches` incl. rotating doubles → `GameRecord[]`; **league+tournament only**, excludes byes/drafts/placeholders; RR subs = physical player, box/ladder sub-overlay deferred), `recompute.ts` (idempotent full recompute → `player_ratings` + `profiles` cache). Migrations `20260708000001` (`player_ratings` table, RLS deny-all; `profiles.primary_*` cache) + `20260708000002` (`primary_confidence`/`primary_games`), applied to prod. Backfill run: 422 games → 164 ratings / 122 players; **16 (8 Established + 8 Rusty) show a Score, rest keep self-reported Level.** Display via `lib/rating/display.ts` `ratingDisplay()` — earned Score (Established/Rusty) vs Phase-1 self-reported fallback — on own profile, public profile, directory. **Primary format = the Established format (doubles wins ties), else most-played.** All rating logic is unit-tested (42 tests). **Recompute trigger:** nightly Vercel cron `/api/cron/recompute-ratings` (`0 9 * * *`, `vercel.json`, CRON_SECRET-guarded, maxDuration 60) keeps Scores fresh; ad-hoc via `npx tsx scripts/recompute-ratings.mjs`; an on-finalize hook (same job, sooner) is still optional. **Trend sparkline shipped** (PR #280): engine emits per-period `history` snapshots; recompute caches the primary track's Score-over-time as `profiles.primary_score_history` (jsonb, migration `20260708000003`); own + public profile show a `Sparkline` when earned + ≥2 points. **Per-format trendlines (PR #331):** the profile Rating card was rebuilt as a "Joinzer Score" card — big primary Score + a Format/Games/Score/Trend table where **each format** gets its own trendline; recompute now also persists each track's Score history to `player_ratings.score_history` (jsonb, migration `20260709000004`, backfilled), exposed via `ResumeFormatRating`; `Sparkline` gained optional `width`/`height` (default 72×24). **Deferred (need external input, not just build time):** DUPR **API sync** (Phase 3 — needs DUPR partner API credentials/contract; manual DUPR + `dupr_verified` already ship); multi-sport tables (Phase 4 — code is already activity-aware, but the full DB/label expansion is premature until a real 2nd sport exists). Design + locked decisions in `docs/phases/rating-engine-phase2.md`.

- **Rating system — Phase 0 + 1 (July 7, 2026, PR #267)** — finalized strategy in `docs/phases/rating-system.md` (source of truth): **Joinzer Score** = universal public 0–100, **Joinzer Level** = activity-specific label, **DUPR** optional/secondary and never treated as verified, architecture **activity-aware** (pickleball first). Phase 0 (trust): migration `20260707000002` added `profiles.dupr_verified` (default false), `dupr_last_synced_at`, `self_reported_rating`, `self_reported_scale` (`dupr|self|other`), backfilled from legacy fields (legacy `dupr_rating`/`estimated_rating`/`rating_source` **retained**). `RatingBadge` now truthful — green ✓ only when `dupr_verified=true`; self-entered DUPR "· self-entered", estimates "· self-reported" (no longer keys verification off `rating_source='dupr_known'`; backward-compatible). Phase 1 (identity): shared `lib/rating/levels.ts` `scoreToLevel(activity, score)` (pickleball: New Player/Beginner/Intermediate/Advanced/Elite over 0-20/21-40/41-60/61-80/81-100) + provisional `selfReportedLevel()`; own profile, public profile, players directory lead with **Joinzer Level** + honest self-reported status (no Score shown until calculated). Old 1000-scale `joinzerRatingLabel` removed; `profiles.joinzer_rating` still exists but superseded (retire later).

- **Player profile résumé — Phase 1 shipped (July 9, 2026, PRs #313–#318)** — the public `/players/[id]` page is now a **player résumé**, not a rating card. **Not flag-gated (live).** Sections: hero (photo, name, Joinzer Level, earned Score/self-reported status, member-since, home court), rating summary (Score/Level/confidence/games + `Sparkline` + per-format Score chips + a DUPR line *only* when a real declared DUPR exists — verified or self-entered-on-DUPR-scale, not a stale `dupr_rating`), **career snapshot** (matches/W/L/win%/streak/events — *competitive only*), **recent form** (last-10 W/L strip), **upcoming** competitive events, and **about/preferred-play**. Career/form/events are **computed on read** by a new pure `lib/rating/stats.ts` `computePlayerStats(records, userId)` (11 tests) that consumes the **existing** `extract.ts` `GameRecord[]` — verified live: a player's `stats.matches` = the sum of the rating engine's per-format `games_counted`. Assembled by `lib/profile/resume.ts` `loadPlayerResume` (service-role; PII-safe — never selects email/phone). Migration `20260709000001` added four nullable `profiles` personality columns (`bio`, `dominant_hand`, `preferred_side`, `preferred_formats text[]`), editable via `ProfileEditForm`; own `/profile` gained a "View public profile" link. **Phase 2 shipped (July 9, 2026, PRs #320–#322):** career stats now cached in a `player_stats` jsonb table (migration `20260709000002`, RLS deny-all) populated by the **nightly recompute cron** (same single extract that builds `player_ratings`; `recomputePlayerStats`), with the loader reading the cache and falling back to compute-on-read only on a miss (new/zero-match players); **count badges** (compute-on-read, no table — Established/match-milestone/win-streak/tournament·league-player/Early-Member via `lib/profile/badges.ts`, 7 tests); and an **event-history list** (completed leagues/tournaments from the player's registrations, partitioned from upcoming). **Phase 3 — placements/titles shipped (July 9, 2026, PRs #324–#326):** a pure `lib/profile/placements.ts` `computePlacements(bracketType, matches, regs)` derives champion/finalist/podium per **completed tournament division** — single-elim (final + semifinal losers), double-elim (reset-aware; LB-final loser = 3rd), round-robin (standings top 3), pool→playoffs (playoff bracket) — 10 tests, returns `[]` unless finished. `lib/profile/recomputeAchievements.ts` persists them per player (both doubles partners) into `player_achievements` (migration `20260709000003`, RLS deny-all, unique per (player, division)) via the nightly recompute (full replace; backfill = 56: 16 champions/16 finalists/24 podium, spot-checked vs the bracket final). Profile shows a **"Titles & podiums"** section (medals). **Tournaments only** — leagues have no season-end "champion" concept in the code (they run indefinitely; box has none), so league titles are deferred until a league-season/playoff feature exists. **Privacy opt-out shipped (July 9, 2026, PR #334):** `profiles.discoverable boolean default true` (migration `20260709000005`) + a "Directory visibility" toggle in `ProfileEditForm`; when off, the player is filtered from `/players` and their `/players/[id]` returns notFound to everyone but themselves. **Organizer-identity page shipped (July 9, 2026, PR #336):** `/organizers/[id]` — hero + stats strip (tournaments · leagues · distinct players served) + hosted tournament/league lists, via `lib/profile/organizer.ts` (PII-safe service-role loader; null → 404 for non-organizers; scoped to `tournaments.organizer_id` + `leagues.created_by`, casual events excluded); player profile links to it when the player hosts anything. **Phase 3 complete** except league titles (blocked on a league-season/champion concept). Plan: `docs/phases/player-profile-phase1.md`.

- **Team League format — Phase 1 shipped (July 8, 2026, PRs #285–#291)** — a fourth league format (`leagues.format_kind='team'`): roster-based teams play a weekly round-robin of **team matchups**, each made of individual **lines** (e.g. Line 1 singles, Line 2 doubles). **Behind a feature flag** (`NEXT_PUBLIC_ENABLE_TEAM_LEAGUES`, default **OFF** — the format button + branches only render when set). ~70% reuse of existing machinery. **Schema (Phase 0a, migration `20260708000004`):** `league_teams` (name, captain, seed, status) + `league_team_members` (role) — RLS deny-all, server via service role — plus additive nullable `league_fixtures` columns: `team_1_id`/`team_2_id`/`winner_team_id` (FK `league_teams`) and `team_1_partner_registration_id`/`team_2_partner_registration_id` (ad-hoc doubles per line, mirrors the tournament rotating-doubles columns). **Parent/child fixture model:** a matchup = parent `league_fixtures` (`match_stage='team_matchup'`, on a `league_periods('matchday')` row); each line = child fixture (`match_stage='team_line'`, `parent_fixture_id`). **Standings seam (Phase 0b):** extracted the entity-agnostic `rankEntities()` core in `lib/tournament/standings.ts` (`computeStandings` now delegates to it — behavior-identical, 388 tests); Team League ranks TEAM ids through the same core, no fork. **Phase 1 surfaces:** team CRUD + roster assignment (`/leagues/[id]/teams`, organizer-only), Create/Edit form + nav branching (line count/discipline/roster min-max in `format_settings_json.lines`), round-robin schedule generation (`buildTeamRoundRobin` reuses `circleMethodPairs`), **lineup entry** (`/leagues/[id]/teams/matchups/[matchupId]` → child line fixtures with per-line player slots), **line scoring + matchup roll-up** (`PATCH .../matchups/[matchupId]/score`: line wins per team → `winner_team_id`, `status='completed'` when all lines in), and **team standings** (matchup W–L → line-win differential, authed `TeamStandings` + public `/l/[id]` parity via `getTeamPublicStandings`). Child line fixtures feed **Joinzer player ratings** for free (parents skipped — null `team_*_registration_id`). Additive throughout — RR/Box/Ladder/tournament code and data untouched. Canonical plan in `docs/phases/team-league.md`. **Phase 1 v1 limits:** organizer-only (no captain self-service lineup/roster yet), no playoffs (bracket-engine reuse is a later phase), lineup locks once a matchup is scored (no clear-results flow), attendance/subs not yet wired into team lineups.

- **Flex League format — Phase 1 shipped (July 8, 2026, PRs #295–#300)** — a fifth league format (`leagues.format_kind='flex'`) and the **first player-driven** one: a **self-scheduled round-robin**. Each entrant gets an opponent list + a season deadline, arranges their own court/time, **reports** the score, and the opponent **confirms**; standings update as matches are confirmed. **Behind a feature flag** (`NEXT_PUBLIC_ENABLE_FLEX_LEAGUES`, default **OFF**). ~60% reuse and **no migration** — `format_kind='flex'` was already permitted and `league_fixtures` already carried the Flex columns (`window_start`/`window_end`, `reported_by`, `confirmed_by`). **Whole-season model:** one round-robin up front (no per-round windows in Phase 1); entrants are singles or fixed-doubles (folded via `dedupeRegistrationsToTeams`). **State machine (`lib/leagues/flexFixture.ts`, 22 tests):** `scheduled → in_progress (reported, awaiting confirm) → completed`; dispute → `disputed` → organizer `resolve`. `'in_progress'` is reused to mean "reported, awaiting confirm" so no status enum change. Actor rules: either entrant reports; the **opposing** entrant confirms/disputes (can't confirm your own report); organizer can do anything and is the only resolver. **Routes:** `POST flex/generate` (`roundRobinMatches` → `league_fixtures` `match_stage='round_robin'`, regen-guarded), `PATCH flex/fixtures/[id]/{report,confirm,dispute,resolve}` (thin over the pure fns; `flexServer.ts` loads fixture + per-side user sets + is-organizer). **UI:** organizer hub `/leagues/[id]/flex` (`FlexManager` — generate, progress readout, dispute queue with inline resolve, round-by-round grid); player "Your matches" on the league overview (`FlexPlayerMatches` — report/confirm/dispute from the player's perspective). **Standings:** whole-league via `computeFixtureStandings` (registration entity, no box scope) — authed + public `/l/[id]` parity (`getFlexPublicStandings`). **Notifications:** report→opponent, confirm→reporter, dispute→organizer. Rating feed is free (flex fixtures are `league_fixtures`). Verified: 22 unit tests + a live seed→standings round-trip (confirmed unconfirmed matches are excluded) + a Playwright E2E through real routes (create → generate → report → cross-user confirm). Additive — RR/Box/Ladder/Team/tournament untouched. Canonical plan in `docs/phases/flex-league.md`. **Phase 1 v1 limits:** whole-season only (per-round windows deferred), dispute resolution is a simple organizer score override. (Deadline/forfeit cron + self-scheduling shipped in Phase 2 — see below.)

- **Security hardening pass (July 10, 2026, PRs #353–#357, migrations `20260710000001`–`...07`)** — a focused RLS/authorization sweep. Fixed: anon-readable analytics/PDT views (grants revoked), world-open invitation tables (RLS scoped), anon-callable registration RPCs (EXECUTE locked to the right roles), exposed `stripe_payment_intent_id` on registration tables (hidden from public via column GRANTs), and self-escalation via overly-permissive registration write policies (scoped). Reaffirms the model: **RLS deny-all + service-role-in-routes — the API route is the auth boundary.** Storage + function `search_path` also hardened. Remaining LOW-severity items (dashboard-only): leaked-password protection toggle, avatars bucket listing. Details in `docs/security.md` + the security-hardening memory.

- **Location / venue system (July 10–11, 2026, PRs #364–#374, migrations `20260710000008`–`...10`)** — organizers can add venues **not** already in the directory, from **every** create *and* edit form (League / Tournament / Play). `locations` gained `address`/`city`/`state`/`zip_code`/`country` + `created_by` + `status` (`approved`|`pending`) + an editable `short_code`. `POST /api/locations` (service-role, name-dedup, inserts `pending`) backs an "add a new location" toggle; the address area auto-fills. Dropdowns are gated to **approved-OR-mine**. Admin **pending-venues approval page** (`/admin/...`) to approve/reject. Map: a "View on map" picker on create forms geocodes new venues, and each venue pin shows an always-visible `short_code` label (override-able). Shared `LocationAddress` / `NewLocationFields` primitives.

- **Player-run wave (July 13, 2026, PRs #375–#403, migrations `20260713000001`–`...04`)** — a large push toward the north star: **players and captains run things, not just organizers.** Shipped together:
  - **Player self-sub nominations** — a player picks their **own** substitute (dropdown), applied **immediately with no organizer approval** (reversed the earlier approval model), with **undo**. Unified `sub_nominations` table (deny-all) + `POST /api/sub-nominations` + shared `AddSubForMe`. Live across **Play** (#389–#391), **round-robin** leagues (#392, real per-session sub keeping credit), **tournaments** (#393, pre-bracket spot transfer via `user_id` swap), and **box + ladder** (#394/#396, incl. player self-**check-in** + doubles per-slot sub). Flex (already player-run) and Team (needs captain flow) intentionally excluded; doubles per-slot self-sub deferred for RR leagues (v1 = singles).
  - **Tournament player self-scoring wired** (#401) — the `allow_player_scores` toggle, previously a stored-only stub, now lets a match participant score their own match via a "My Matches" surface; advancement runs through the same engine, organizer can still edit.
  - **Box/ladder player self-scoring** on by default with an off-switch (#395/#399).
  - **Team League captain self-service** (#397/#398/#403) — see Team Phase 2 below.
  - **Chat standardization** (#388) — one prominent, expandable `ChatPanel` across Play / Leagues / Tournaments (all three message tables); `event_messages` added to realtime (Play chat was silently non-live before); leagues chat now inline. Known follow-up: message-INSERT RLS is client-side-gated only (no server-side registration check).
  - **Player-facing surfacing + freshness** — players got Roster + Schedule tabs (#378), grouped Current/Future sessions (#376), a prominent "Enter my score" CTA (#375); Play captains can invite/add players by email (#379/#380); manual **Refresh** CTAs + near-session auto-refresh + a live "who's coming" list on league surfaces (#381–#387).
- **Attendance sync is bidirectional + live (July 15 2026)** — an organizer changing a player's status in the RR live session manager (`PATCH /api/league-sessions/[sessionId]/players/[playerId]`) now mirrors into the player-facing self-report table (`league_session_attendance`, reverse-mapped present↔checked_in_present etc.) **and broadcasts to `attendanceTopic(sessionId)`**, so the player's "Who's coming" chip updates in real time (plus a check-in notification when marked present) — matching the player→organizer direction that already worked. Same change **closed a missing-authorization hole**: that route was authenticated-only (any signed-in user could change anyone's status) → now organizer/co-admin only.

- **Substitutions — Phase 1: unified league sub domain (July 16 2026, PR #433, migration `20260716000003`)** — foundation for the unified "request a sub / accept an open opportunity" system (full plan: `docs/phases/substitutions-implementation-plan.md`; audit + strategy: `docs/phases/substitutions-action-center.md`). **`league_sub_requests` is now the single league substitute-request model.** Extended in place: `league_period_id` (box/ladder) alongside `league_session_id` (RR) with a **session-XOR-period** CHECK; **status collapsed to `open|filled|cancelled|expired`** (dead `claimed`/`approved`/`fulfilled` retired — they never actually placed a sub); `fulfillment_mode` (`open_pool|self_assigned|organizer_assigned`), `filled_by/filled_at/expires_at/cancelled_at/cancelled_by/placed_with_override/format/gender_required`; **partial-unique indexes = one active open request per covered player per occasion**; dropped the unused client-insert RLS (writes are service-role; `lsr_read USING(true)` kept — league/Home pool reads use the user client). **Schema + data only — no acceptance/placement yet (Phase 2 = an atomic `accept_sub_request` RPC doing claim + placement in one transaction via `assignRrSub`/`assignAttendanceSub`, preserving `sub_credit_cap`).** The old claim/organizer-approve are **paused (clean 409)** until Phase 2; create + cancel work; `SubRequestsSection` is read-only. `sub_nominations` (Play + tournament) **untouched** (its misleading "pending until approved" comment corrected — it actually applies immediately). Legacy `claimed`/`approved` refs in `LiveSessionManager`/live-page are dead-but-harmless (deferred to a Phase-6 cleanup). Verified: constraint/dedupe SQL assertions (`supabase/tests/sub_requests_phase1_verification.sql`) + 522 unit tests + tsc/build/eslint.
- **Substitutions — Phase 2: atomic acceptance + placement (July 16 2026, PR #434, migration `20260716000004`)** — the **correctness core**. An eligible player accepts an open `league_sub_requests` row and the **claim (`open`→`filled`) + the actual substitute placement happen in ONE Postgres transaction** (`accept_sub_request(request_id, accepter_id)` — SECURITY DEFINER, `EXECUTE = service_role` only; the accept route is the trust boundary, passing the `getUser()` id). Status and participation can never diverge: any failure rolls back both. **Scope = round-robin (`league_session_id`) + box/ladder (`league_period_id`) only**; team/flex/tournament/play raise `unsupported_format`. **Shared placement primitives** `place_league_sub_rr` / `place_league_sub_attendance` are the single source of truth for sub linkage — `assignRrSub`/`assignAttendanceSub` now **delegate** to them, so organizer manual-assign, player self-sub, and open-pool accept can't drift (`sub_credit_cap` untouched; credit stays on the covered participant). The one exception: the **box/ladder organizer `attendance/assign-sub` route keeps its own placement** (it also does guest subs, existing-attendance-row subs, and doubles-pair clear-and-replace — folding those in is the Phase-3 "organizer assignment through the unified model" work; a code comment keeps the linkage columns in lockstep). **Hard gates re-derived under the row lock:** open+not-expired, not-own-request, account eligible (`is_stub`), supported format, required division gender (from `leagues.format`), occasion not started, generation guard (no rounds/fixtures), duplicate participation, **schedule conflict** (day-granular Pacific — another RR session the accepter signalled attendance at, or a joined Play event, same date; periods carry no clock so period-scoped accepts skip this), covered-not-already-subbed, scope match. **Concurrency**: `SELECT … FOR UPDATE` + conditional `UPDATE … WHERE status='open'` + a per-(occasion, accepter) advisory lock → exactly one success, one deterministic `already_filled`, no double placement. **Idempotent** same-user retry (no re-place/re-audit). Audit written **inside** the txn. New `POST /api/league-sub-requests/[id]/accept` (RPC + post-commit notifications + realtime attendance nudge); `SubRequestsSection` regained a minimal accessible **"I can sub"** action (no organizer-approval implication). Verified: **18/18** SQL harness checks (`supabase/tests/sub_requests_phase2_verification.sql` — RR/box/ladder happy, all rejection guards, idempotency, atomic rollback via trigger-injected placement failure) + **real 2-connection concurrency** against prod (one won, one `already_filled`, cleaned up) + tsc + eslint + 522 unit tests + next build. **Next = Phase 3** (the "I can't make it" request-flow + open-pool creation + "I already have someone" + organizer assignment through the unified model).
- **Substitutions — Phase 3: player request flows + unified organizer assignment (July 16 2026, PR #435, migration `20260716000005`)** — primarily product/UX. **"I can't make it" now opens a mobile decision sheet** (`SubDecisionSheet`) with three choices: **Just mark me absent** (existing attendance write, no request) / **Find me a substitute** (`open_pool`) / **I already have a substitute** (pick a Joinzer player → `self_assigned`). Everything is derived server-side, so the common path is **2 taps**. Two new SECURITY-DEFINER wrapper RPCs (`EXECUTE = service_role` only): **`create_player_sub_request`** — `open_pool` inserts an `open` request; `self_assigned` inserts **and** immediately fills it via the Phase-2 `accept_sub_request` in ONE txn (chosen sub passes the **same hard gates** as an open-pool accept; the requester **cannot override**). **`assign_organizer_sub_request`** — organizer assignment through the unified model: creates (or **reuses the covered player's open request**, no orphan) an `organizer_assigned` **filled** record **and** places via the shared Phase-2 primitive in one txn; enforces the non-overridable integrity gates (account eligibility, required gender, duplicate placement, scope) and records `placed_with_override` for a permitted **soft** (rating/logistical) override — organizers keep their existing discretion (schedule-conflict + post-generation guard are **not** applied on the organizer path, matching prior behavior). The player create route (`POST /api/league-sub-requests`, rewritten) **forces `requesting_player_id` = `getUser().id`** and can never set `organizer_assigned`/`override`. **Requester status line** (`RequesterSubStatus`): "🔎 Looking for a substitute…" (+ Cancel while open) / "✓ Substitute confirmed: X" / "✓ Assigned by organizer: X" — no raw status labels; wired on the league page (box/ladder) + Schedule tab (RR), optimistic in-session elsewhere. **RR organizer assign-sub + box/ladder single-Joinzer-user assign** now route through the unified model (produce a `league_sub_requests` record via the shared primitive). **Box/ladder guest + doubles-pair** assigns keep their existing preserved placement; their unified record is deferred (needs a guest-identity column + multi-record modeling). League `AddSubForMe`/`UndoSubButton` **removed** from `PlayerCheckIn`/`BoxLadderCheckIn`; **Play + tournament `sub_nominations` untouched** (the `sub_nominations` league branches are now dead-but-harmless — Phase 6). Verified: **18/18** Phase 3 SQL harness (`supabase/tests/sub_requests_phase3_verification.sql` — open_pool + self_assigned across RR/box/ladder, dedupe, guards, self_assigned atomic rollback, organizer assign + reuse-open-request + override + gender hard-gate + already_covered) + Phase 2 regression + tsc + eslint + 522 unit tests + next build. **Next = Phase 4** (Home Action Center "Needs Your Attention" + `/subs` browse + `open_to_subbing` surfacing + matched-candidate ranking/warnings).
- **Substitutions — Phase 4: discovery — matched opportunities + Home Action Center + `/subs` (July 16 2026, PR #436, migration `20260716000006`)** — open opportunities are now discoverable. **One authoritative matching core** (`lib/subs/matching.ts`, pure + 27 unit tests) separates **hard eligibility** (mirrors the accept RPC's gates), **transparent additive ranking** (urgency 0–100 dominant · prior league +30 · prior sub +22 · same organizer +12 · rating closeness 0–20 · home-court +15 · recency 0–10; ties break on `createdAt` asc — deterministic; sparse data ⇒ visible + ranked lower, never hidden), and **rating-mismatch warnings** (display only, >0.5 outside `leagues.skill_min/max`, never a gate). `lib/subs/loadOpportunities.ts` batches context (~10 queries regardless of pool size) and is reused by Home + `/subs`; `lib/subs/candidates.ts` inverts it for a **bounded, opted-in, deduped** proactive-notification fan-out (`sub_request_notifications` dedupe table). **Home "Needs your attention"** = server-derived typed `ActionItem[]` (`lib/home/actionItems.ts`, NO table), placed **directly under the greeting, above the amber profile nudges** (moved there July 17 2026, PR #439 — urgent-first), renders nothing when empty, realtime-refreshed; replaced the old bottom `SubRequestsSection` on Home. Item types + priority (lower first, cap 4, matched ≤2): **run_session_today** (0 — organizer: a today RR session in a league you own, still scheduled → live manager; PR #444) · **incomplete_payment** (0.5 — unpaid paid-Play-event participation or a `pending` reserve-then-pay `tournament_order`; PR #440) · **own_open_sub_request** (1, +Cancel) · **attendance_needed** (2 — an RR session today/tomorrow the registered player hasn't responded to; PR #440) · **score_confirmation** (2.7 — a flex-league result awaiting the player's confirm; PR #441) · **substitute_found** (3, ages out) · **draft_event** (3.5 — organizer: a draft tournament to finish/publish; tournaments-only since leagues have no draft state; PR #444) · **matched_sub_opportunity** (5, **only when `open_to_subbing`**). Dual-audience (player + organizer). The union is extensible (waitlist, registration_deadline, schedule_change, etc.). **Home layout** (`app/(app)/home/page.tsx`): greeting → Needs-your-attention → amber nudges → sub-opportunities link → **My Schedule** (top **3**, "See all" for the rest; PR #441) → context CTA → **Upcoming Events** (`UpcomingEventsSection`). **Home polish (July 17 2026, PR #446):** the greeting subline reflects imminence ("You're hosting today"/"You're playing today"/"…something tomorrow"/generic, derived from the full footprint); a session already surfaced in Needs-your-attention (run-today / attendance-needed) is **deduped out of My Schedule**; My Schedule cards carry **Today/Tomorrow** pills; a **brand-new player** (no footprint, non-declared-organizer) gets `UpcomingEventsSection` **pulled up** ("Games near you", top 4 via new `title`/`limit` props) above the onboarding card; the amber nudge zone was re-ordered urgent-first (transient waiting-on-partner states above one **consolidated, softened** profile-setup card — PR #445); and the page's data fetching was restructured into 3 dependency waves (user-id-only queries — profile/joined/tournament-regs/Action-Center — no longer wait on the league-id lookup). **Live Action Center (PR #447):** home now mounts a second `RealtimeRefresh` on the user's private `notificationsTopic(userId)` channel (in addition to `sub-requests:changed`) — since nearly every Action Center change also creates a notification, that one per-user stream keeps the screen honest in real time without wiring per-entity topics; `RealtimeRefresh` gained a `private` prop for the RLS-gated `realtime.messages` channel. **Upcoming Events discovery (PR #442):** hard filters = joinable + not-already-in + **gender eligibility** (gender-specific `mens_`/`womens_` **leagues** shown only to matching-gender players; mixed/open/coed + tournaments [multi-division] + Play [open] not filtered; unknown gender ⇒ shown); soft ranking = **proximity to home court (0–40, dominant) > skill fit (0–30) > urgency-to-decide (0–12, capped, wide window so far-out tournaments aren't penalized)** + scarcity bump for nearly-full sessions. Leagues' skill is a **soft** signal now (was a hard filter). Note: waitlist auto-promote is automatic (no confirm step) → it's a notification, not an Action Center item. **`/subs`** (Open / My substitutions / My requests) with an accept-confirmation sheet calling the Phase-2 atomic route, Load more, realtime pool refresh; open to **every eligible player** regardless of preference. **`open_to_subbing`** (default false; profile edit form) gates Home surfacing + proactive notifs **only** — `/subs` is always open. **RLS tightened:** `league_sub_requests` SELECT is now **own+filled only** (was `USING(true)`); the pool + organizer views are served by **service-role loaders** (the one client-side pool read — the league page — moved to service role). Coarse `sub-requests:changed` broadcast on create/accept/cancel/organizer-assign refreshes Home + `/subs` (deny-all-safe). **v1 limits:** `player_availability` signal deferred (column doesn't exist); geographic boost is home-court-id match only (no distances/geolocation); no escalation scheduler (one notification wave on creation); box/ladder period timing granularity (no clock → no period schedule-conflict). Verified: 27 matching unit tests + RLS scoping check + Phase 3 regression + tsc + eslint + 549 unit tests + next build. **Next = Phase 5** (filled-sub withdrawal + atomic reverse/reopen + original-player reclaim + expiration cron + shared opportunity links + notification polish).
- **Substitutions — Phase 5: lifecycle — withdrawal / reclaim / organizer correction / expiration / shared links (July 16 2026, PR #437, migration `20260716000007`)** — completes the operational lifecycle. **Reversal is the inverse of the Phase-2 placement primitives, and every transition (reverse + status change + audit) is ONE Postgres transaction** — status and participation can never diverge. **No new statuses** (withdraw/reopen/reclaim/replace/expire are transitions + audit events over open|filled|cancelled|expired). New SECURITY-DEFINER RPCs (documented as `service_role`-only; **the grants did not actually match that claim until `20260806000003` — see the RPC-lockdown entry below**): `reverse_league_sub_rr`/`reverse_league_sub_attendance` (inverse primitives); **`withdraw_sub_request`** (the filled sub withdraws before start → `filled→open`, reopens to `open_pool`, `+1 notification_generation`, covered restored to `cannot_attend`); **`reclaim_sub_request`** (requester "I can attend after all" before start → `filled→cancelled`, attendance restored to `coming`/`planning_to_attend`); **`organizer_correct_sub_request`** (reopen | cancel | replace, before start); **`expire_sub_requests`** (bounded, idempotent, **race-safe** conditional `status=open→expired` via `FOR UPDATE SKIP LOCKED` — a just-filled request is never overwritten). Cutoff = occasion start (no buffer); after start → organizer-only (post-start correction currently returns a clear error — deferred). **Notification generations**: `league_sub_requests.notification_generation` + `sub_request_notifications.generation` (dedupe key `request+user+generation`) so a **reopen re-notifies** without deleting history; the **withdrawing sub is excluded** from the reopen wave. Routes: `POST …/withdraw`, `…/reclaim`, `…/organizer-correct` (authorized via `canOperateSession`/`authorizeOrganizer`) + a **daily** `CRON_SECRET`-guarded `/api/cron/expire-sub-requests` (the loaders + accept RPC still reject stale requests between runs, so this is canonical cleanup, not correctness). **Corrected Aug 6 2026:** this originally read "Hobby blocks sub-daily" — true when written, but the account moved to **Vercel Pro on 2026-07-30** (see the crawl-budget incident entry below), so the daily cadence is a *choice*, not a platform limit. It stays daily deliberately: the pre-session warning added Aug 6 2026 rides this same run rather than adding a cron. **Shared-link `/subs/[requestId]`** — PII-safe detail, eligibility **re-derived server-side** (the link is never proof of eligibility); logged-out → middleware `/login?next=/subs/[id]` (open-redirect-safe, login page gates `next` to internal paths) → returns + re-evaluates. **Web Share/copy-link** on cards + detail + requester status. UI: **Withdraw** (My substitutions, before-start, confirm), **"I can attend after all"** reclaim (`RequesterSubStatus`), **organizer reopen/cancel/replace** controls (`OrganizerSubControls` on the detail page). Verified: **16/16** lifecycle SQL harness (`supabase/tests/sub_requests_phase5_verification.sql`) + **real concurrency** (withdraw vs reclaim → one won, one `not_filled`, cleaned up) + atomic rollback + tsc + eslint + 549 unit tests + next build. **v1 limits:** post-start organizer correction returns a clear "ask organizer / unsafe" error rather than mutating generated state (**closed Aug 6 2026 — see the post-generation record-close entry below; mutating generated state is still unsupported, but the organizer can now correct the record**); organizer controls surface on the shared-link detail page (not yet on the live-session manager — LiveSessionManager's legacy claim/approve UI is Phase-6 cleanup); box/ladder guest+doubles-pair reversal untested (those assigns still lack a unified record from Phase 3). **Next = Phase 6** (retire dead `claimed`/`approved` refs in LiveSessionManager + live/page.tsx + the dead `sub_nominations` league branches + deprecated props/loaders; box/ladder guest/pair unified record; a persistent `/subs` nav entry; doc reconciliation) and, separately, official league announcements.
- **Substitutions — Phase 6: consolidation + cleanup (July 16 2026, PR #438, migration `20260716000008`)** — final phase; the league substitution system is now **complete + production-ready**. **Removed**: the dead league `sub_nominations` branches (`createLeagueNomination`/`createLeaguePeriodNomination` + dispatch — `sub_nominations` now serves **Play + tournaments only**); the legacy PATCH claim/approve flow + `sendClaimNotification`/`emailHtml` (**PATCH is cancel-only**; acceptance = the atomic accept route, assignment/correction = the unified organizer route/RPC); the deprecated `activeSelfSub` prop + its `sub_nominations` loader reads (PlayerCheckIn/BoxLadderCheckIn/league page/PlayerSchedule/schedule page); the LiveSessionManager `claimed`/`approved` type + approve handler + UI (the substitute panel now lists **open + filled** requests linking to the unified `/subs/[id]` controls). **Guest records**: `league_sub_requests.filled_by_guest_name` added; the box/ladder organizer route now creates an `organizer_assigned` **filled** record **per covered slot** for guests (`filled_by_user_id` NULL + `filled_by_guest_name`) + doubles-pair users, reusing the covered player's open/filled record (best-effort, post-placement — the placement itself still goes through the shared primitive `place_league_sub_attendance`; the record write is the one documented in-app direct writer). Reversal-by-slot already handles guest rows → **guest reclaim + organizer reopen/cancel work** (SQL-verified). `claimed_by_user_id`/`approved_by_user_id` **deprecated** (commented, retained for history, never written). **Organizer-control visibility** now uses `canOperateSession`/`authorizeOrganizer` (co-admins + self-run hosts, matching the server). **Persistent "Sub opportunities" link** on Home (independent of `open_to_subbing`). Plan doc reconciled to the shipped system. **Intentional survivors** (documented, not dead): `league_session_subs` (a separate live "raise-your-hand availability" toggle, distinct from the request system); `sub_nominations` Play/tournament code + historical `surface='league'` rows. **Remaining limitations:** post-start generated-state mutation unsupported (organizer gets a clear error — **and since Aug 6 2026 can close the record instead; see below**); box/ladder period time granularity; home-court-only geography; daily expiration cron (**was a Hobby-tier constraint; the account has been on Vercel Pro since 2026-07-30, so the cadence is now a choice**); team/flex + tournament/Play remain separate substitution systems; multi-slot doubles organizer assignment is **per-slot best-effort** (not both-or-neither atomic) via the box/ladder route; replace-with-guest not supported via the unified RPC (organizer re-assigns). Verified: tsc + eslint + 549 unit tests + next build + guest lifecycle SQL + full repo legacy sweep. **The substitution project is complete. Official league announcements remain a separate, not-yet-started effort.**
- **Substitutions — post-generation record close + pre-session unfilled warning (Aug 6 2026, migrations `20260806000001` / `20260806000002`)** — two gaps left open when the substitution project closed at Phase 6. **(1) The organizer was frozen out at *generation*, not at start.** `organizer_correct_sub_request` calls `_sub_occasion_open`, which raises `generation_started` the moment `league_rounds`/`league_fixtures` exist, so a substitute who accepted and then no-showed left the request reading "filled by X" permanently — and the error told the user to *"ask your organizer"*, the one person who also could not act. New SECURITY-DEFINER **`organizer_close_sub_request_record`** (`service_role`-only EXECUTE — the migration asserted that from day one, but it only became **true** with `20260806000003`; see the RPC-lockdown entry below) moves `filled → cancelled` with **`record_closed_reason`** (`no_show` | `other`) and **touches no placement, attendance, round or fixture row** — the lineup, the results and `sub_credit_cap` are untouched by construction (no standings path reads `league_sub_requests`). It is a **separate function**, not a mode on `organizer_correct_sub_request`, because every branch of that RPC calls `_reverse_sub_placement` unconditionally; it does share the existing route (`mode: 'close_record'`), because the auth block is the ADR-03 boundary and a second copy of it is the bigger risk. The guard is **inverted and reuses the one existing definition**: it calls `_sub_occasion_open` and refuses with `use_standard_correction` if that does *not* raise, accepting only `generation_started`/`occasion_started` and re-raising everything else — so `occasion_not_found` is never mistaken for a started occasion, and the record-only path is unusable while the ordinary reversing path still works. **`filled_by_user_id`/`filled_at` are deliberately preserved** (a no-show must stay attributable), so `cancelled ⇒ filled_by IS NULL` is **not** a safe assumption; the RPC returns `closed_sub` rather than `removed_sub` so the existing "the organizer removed you from this substitute spot" notification — false here — cannot fire. The closed-out substitute is **not notified** in v1 (deliberate: reputationally loaded, post-hoc, non-appealable). UI: a confirm-gated **"Close record"** control in `OrganizerSubControls`, reachable from the live-session substitute panel. **This amends ADR-06** — `filled ⟺ a placement row exists` is now conditional; the amendment, its four bounding constraints, and the cross-reference from ADR-06 itself are in `docs/decisions.md`. **(2) The "no substitute was found" notice fired after the game.** `expires_at` for a session-scoped request *is* the session start, so the daily cron notified at ~3am the next morning. `/api/cron/expire-sub-requests` now runs a **warning pass before its expiry sweep** — open session-scoped requests starting within 24h that nobody has taken notify the **requester and the league organizer** (never the same person twice) roughly 15 hours out, since that cron already runs 3am PDT on the morning of that evening's sessions. **No new cron.** Dedupe is `league_sub_requests.unfilled_warned_generation` compared `IS DISTINCT FROM notification_generation`, so a reopen re-arms the warning automatically with no edit to the withdraw/reopen RPCs — deliberately *not* a `sub_request_notifications` row, since that table means "candidates notified" and excludes the requester. The post-expiry notice is **kept but conditional**: it fires only for requests never warned — **period-scoped (box/ladder) requests, which have `expires_at = null` and no clock, get no pre-emptive warning at all** and keep it, as do same-day creations that missed the run. One notification per dead request, and where possible it is the early actionable one. Selection rules are pure + unit-tested (`lib/subs/unfilledWarning.ts`, 19 tests). Plan doc §2.6 + the state table + decisions #3/#4 + the acceptance checklist reconciled — §2.6 had **contradicted itself** ("act after the start-time cutoff" alongside "post-generation guard never overridable"); the implementation had correctly followed decision #4.
- **SECURITY — SECURITY DEFINER RPCs were executable by `anon`; 16 locked down (Aug 6 2026, migration `20260806000003`)** — **28 of 31 `SECURITY DEFINER` functions in `public` were EXECUTE-able by `anon` and `authenticated`.** Root cause: Supabase grants EXECUTE on new `public` functions **directly to the anon/authenticated roles**, not via the `PUBLIC` pseudo-role, so the `revoke all … from public` that every subs migration used revoked a grant that was never the access path. The repo had already hit this once — `20260710000004_lock_registration_rpc_execute_roles.sql` fixed three registration RPCs and documents the mechanism in its own header — but the substitution migrations were written six days later and used the insufficient pattern anyway. **Why it mattered:** these functions take the acting user as a *parameter* (`p_actor_id`/`p_user_id`/`p_accepter_id`) and do **no internal authorization** by design, because ADR-03 makes the route the boundary; that is sound only while the function is unreachable except through the route, and `pgrst.db_schemas` is unset (defaults to `public`), so all of them were callable over PostgREST with the publishable anon key, actor of the caller's choosing. **16 locked:** the 14 substitution RPCs (7 actor-parameter lifecycle functions, `expire_sub_requests`, and the 6 internal placement/reversal primitives), plus `accept_free_partner_invite` (the 2026-05-26 hardening gap in `docs/decisions.md`) and `increment_discount_uses` (no authorization at all — an anon caller could burn a tournament's discount allowance). **A second, mirror-image bug surfaced during verification:** those last two ALSO carried an explicit `PUBLIC` grant (`=X/postgres` in `proacl`), which the subs functions did not, so the role-only revoke left them fully callable — revoking from `PUBLIC` alone is insufficient because of the direct role grants, **and** revoking from the roles alone is insufficient because of the `PUBLIC` grant. Both are required. **12 deliberately left executable at the time, recorded in the migration so the omission can't read as an oversight — now 11, see the `is_captain_of` entry below:** the 7 RLS predicate helpers policies invoke as the *calling* role (**29 policies** across them — revoking would silently return empty reads app-wide; counted with **word-boundary** matching, because a substring count inflates `can_read_league` to 10 by swallowing the six `can_read_league_session` policies, when the true split is 4 + 6); `is_captain_of` (owner carve-out at the time, flagged as belonging in the revoke list on the evidence — **subsequently locked by `20260806000004`**); the three self-authenticating client-called RPCs `join_event`/`leave_event`/`assign_captain` (each derives the actor from `auth.uid()` and is invoked with the *user's* client, so revoking breaks live flows); and `sync_last_login`, a trigger-returning function PostgREST cannot expose as an RPC. **Safety evidence gathered before applying:** zero of the 16 are referenced by any RLS policy, view, matview, trigger, column default, generated column or CHECK constraint — the detector validated with a negative control (the same query finds 3–6 policy references each for the helpers); every app call site uses the service-role client; internal callers are other SECURITY DEFINER functions, which run as definer. **Verified after applying:** locked 16 → `anon`/`authenticated` both **0**, `service_role` **16**; helpers 8/8 and client-called 4/4 still executable; the three pre-locked registration RPCs still 0/0 as the control; plus an RLS smoke check reading `league_sessions` as `authenticated` through `can_read_league` (3 rows, helper resolves). Purely subtractive — no body, signature, owner or `service_role` grant changed.
- **SECURITY — `is_captain_of` locked; the anon-executable set is now 11 (Aug 6 2026, migration `20260806000004`)** — the one function `20260806000003` deliberately left open under an owner carve-out that grouped it with the RLS predicate helpers. **It is not one:** zero RLS policies reference it (word-boundary count against `pg_policy`; substring matching agrees at 0), and its only caller is `lib/profile/captain-check.ts:12` via the **service-role** client. **Why it was the sharp one:** `is_captain_of(viewer_id, target_id)` is `LANGUAGE sql STABLE`, so it **cannot** read `auth.uid()` and **cannot** raise — both identities arrive as parameters, so it cannot self-guard *by construction*. That makes it categorically different from `join_event`/`leave_event`/`assign_captain`, which each open with `auth.uid()` + a null check and are merely over-granted. Probed as `anon` it **executed and returned `false`** rather than erroring — a working oracle, not a latent one: given two user UUIDs, an unauthenticated caller learns whether one organizes a tournament or event the other is registered in. Participation data about real people, limited only by UUIDs not being guessable, which is obscurity rather than a control. **Rewriting it to derive `viewer_id` from `auth.uid()` was considered and rejected** — the sole caller is server-side with the service role and legitimately supplies both identities, so a signature change is more blast radius for no additional protection. **Both revoke paths were required:** its ACL carried an explicit `PUBLIC` grant (`=X/postgres`) alongside the direct role grants, so a role-only revoke would have left it callable — the same mirror-image defect `20260806000003` hit, and the reason `docs/security.md` now mandates both. **Verified end to end, not from the catalog alone:** `has_function_privilege` for `anon`/`authenticated` went true → **false** with `service_role` unchanged at true; the real `captain-check.ts` path was *exercised* over PostgREST and still returns `true` for a known captain/participant pair, while the same call as `anon` gets **`42501 permission denied for function is_captain_of`**. That exercise mattered because `captain-check.ts` drops `error` and returns `data === true`, so a broken grant would have degraded to a silent `false` rather than an error. The anon-executable `SECURITY DEFINER` set went **12 → 11**, and the eleven are exactly the intended ones (7 RLS helpers · `join_event` · `leave_event` · `assign_captain` · `sync_last_login`). Rollback captured live from prod *before* applying: `supabase/rollback/20260806000004_lock_is_captain_of_ROLLBACK.sql`.
- **Player-run leagues (July 15 2026, PRs #417/#418, migration `20260715000005`)** — a round-robin **session** league can now run **without a dedicated court monitor**: a designated player (or the first player to show up) runs the live session (attendance, generate each round one-at-a-time, subs, lock/complete, end-day) while players self-score. **Slice 1 (authz core):** `leagues.self_run` (default false), `leagues.season_host_user_id`, `league_sessions.host_user_id` (all additive/nullable), plus `lib/leagues/canOperateSession.ts` — `owner || co-admin || (self_run && effective host = session.host_user_id ?? league.season_host_user_id)`, swapped in for the old owner-only gate across **all 8** run-session routes (generate-next-round, session PATCH/end-day, attendance override, player-status PATCH, add sub/guest, assign-sub, round lock/complete, round-match edit). Two side-effects: **co-admins can now actually operate sessions** (the live page let them in but the actions 403'd before), and **two routes that had no authorization beyond signed-in** (add session player, edit round match) are now gated. Scoring is NOT gated here — self-run leagues distribute it via `allow_player_scores`. **Slice 2 (UI):** a **"Player-run league"** toggle on Create/Edit (RR-only; turning it on gently defaults player self-scoring on) + an optional **Season host** picker on Edit (fed by the roster); a host endpoint `POST /api/league-sessions/[sessionId]/host` (`{host_user_id}`: a roster player **claims** an empty seat for themselves — race-safe single-writer — while an operator **assigns/hands-off/releases**); and live-page host gating — the effective host reaches the run screen, a roster player with no host sees **Claim host**, others see "🎾 {name} is hosting," and operators get a **HostControls** strip (You're hosting + hand-off to a present player + Release). Covers all four modes: first-present-player claims, season-long host, host subs herself for a night (hand-off), fully self-running with nobody pre-assigned. **Single effective host per session** (matches the offline-run-mode single-writer decision). **v1 limits:** round-robin session leagues only (box/ladder/team/flex have their own run surfaces + player-run paths); claim requires **roster membership**, not "present" (avoids the attendance chicken-and-egg); if a season host is set but ghosts, only the owner (remotely) or that host can hand off — a random player can't claim over a set season host; **no real-device QA yet.**

- **Leagues — draft/visibility/organizer parity with tournaments (July 21 2026, migration `20260721000001`)** — leagues gained the tournament publishing model so create/edit is consistent across both. `leagues.status` now allows `'draft'` (check `draft/active/completed/cancelled`); added `visibility` (public/private, default public) + `contact_name`/`contact_email`. **New leagues default to Draft.** Create/Edit gained a "Visibility & Publishing" section (Status Draft/Published + Visibility Public/Private toggles + Organizer name/email, mirroring the tournament form); the old "Status" dropdown was relabeled **"Registration"** (it's `registration_status`). `'active'` stays the published/live value (no `active→published` rename). **Draft/private hiding mirrors the tournament read-filter** (`and(status.neq.draft,status.neq.cancelled,visibility.eq.public)` OR owner): the leagues list shows published+public **plus the owner's own** drafts/private; `/browse/leagues` + home Upcoming Events add `status=active`+`visibility=public`; public `/l/[id]` also gates `visibility=public`. A draft's direct link still resolves (unlisted + un-joinable) — identical to tournaments (no detail-page `notFound`). The league page shows the organizer contact (`contact_name || creator` + `mailto`). **All existing leagues (28) were bulk-set `visibility='private'`** on July 21 to clear the public surface of legacy/test leagues; new real leagues go public when the organizer publishes them.

- **Team League — Phase 2 (captain-run) shipped (July 13, 2026, PRs #397/#398/#403)** — team leagues are no longer organizer-only. A **team captain** can now run their own team's match-day: **per-side lineup** (each captain sets only their half; the matchup assembles from both sides), **matchup scoring**, **roster self-management** (add/remove their own team's players via `assertTeamRosterAccess`), and **captaincy transfer** to a teammate. Organizer's both-sides flows are unchanged. Still behind `NEXT_PUBLIC_ENABLE_TEAM_LEAGUES`. **Still not built:** playoffs (bracket-engine reuse over final standings) and attendance/sub integration for team lineups.

- **Flex League — Phase 2 shipped (July 13, 2026, PRs #400/#402)** — **deadline reminders + auto-forfeit cron** (`/api/cron/flex-deadlines`, `CRON_SECRET`-guarded, in `vercel.json`): 3 days before a flex league's `end_date` each still-unplayed match reminds its entrants; past the deadline any match still `scheduled` is **forfeited** (both sides + organizer notified; forfeits excluded from standings; reported-but-unconfirmed/disputed left for the organizer). Plus **player self-scheduling** — on "Your matches" an entrant sets the agreed **time + court** (stored/displayed in UTC as a proposed meeting time), visible to both sides. No migration (columns/status already existed). **Still deferred to a later phase:** per-round windows (`league_periods period_kind='window'`). **Reminder trigger widened July 31 2026 (PR #484)** — see the cron-reliability entry below.

- **Cron reliability pass (July 31, 2026, PR #484)** — cleanup after discovering that **`CRON_SECRET` was never set in Vercel, so every scheduled cron returned 401 from the day it shipped** (`session-reminders` since May 7, the other four since mid-July — roughly three months of total, silent failure; a clean JSON 401 is indistinguishable from a healthy no-op). The secret itself was set separately. This PR fixes what the outage exposed, no migrations: **(1)** `/api/cron/league-partner-timeout` is now **scheduled** (`0 12 * * *`) — the route existed but was never in `vercel.json`, so expired partner invitations never resolved at all and captains' Stripe holds hung indefinitely (`voidCaptainHold`'s only other caller is the decline route); `league_partner_invitations` was empty when scheduled, so the first run was a verified no-op. **(2)** `flex-deadlines`' 3-day reminder was a **single-point trigger** (`daysUntil === 3`) with no catch-up — now fires anywhere in a 0–3 day window, deduped **per user** against the `flex_deadline_approaching` notifications already written (pure logic in `lib/leagues/flexDeadlines.ts`); a player in several unplayed fixtures now gets one reminder per league rather than one per fixture. **(3)** `session-reminders` built its `events` window as a Pacific date string with **UTC boundaries**, offsetting the query 7–8h — it pulled in *today's* 5pm–midnight sessions and dropped *tomorrow's*, breaking the evening slot in both directions; now over-fetches and filters exactly via `lib/utils/pacific-day.ts` (the `league_sessions` branch used date equality and was never affected). **(4)** All six cron routes share `lib/cron/auth.ts` `assertCronSecret`, which logs a missing secret distinctly from a wrong token. **Cron health posture is ADR-15**: manual Vercel dashboard checks plus a standing invariant that any `CRON_SECRET` change is followed by verifying one run returns 200 — deliberately **no heartbeat table and no external watcher**, because no in-app code can make silence loud and both were premature at zero users. Not converted: `/api/revalidate-directory` (also `CRON_SECRET`-guarded, but called by local import scripts, not the scheduler).

- **Paid-event gate — "book a call" (July 15 2026)** — creating **free** events (play / leagues / tournaments) is open to everyone; **charging money is gated** behind organizer approval. `profiles.can_create_paid_events` (default false; migration `20260715000004`; grandfathered anyone already Stripe-connected + Marty). Enforced: the 3 create forms disable the fee field, force `cost_cents`/`price_cents` to 0, and show `EnablePaymentsCTA` (Calendly "book a call") when not approved; `POST /api/stripe/connect/onboard` 403s unless approved; the payouts page shows the CTA instead of the Connect button; `connect/status` returns `canCreatePaid`. Approval is **manual** — flip the flag after the call. **Follow-ups (not yet gated):** the edit forms, and a server-side backstop in the 5 paid-checkout routes (defense-in-depth vs a client bypass — but an unapproved organizer also can't reach Stripe Connect, so money can't reach their bank).
- **Payments & pricing — refunds + early-bird (July 14, 2026)** — three additive money features across tournaments/leagues/play. **(1) Refund policy + no-refund date** (migration `20260714000001`): organizers enter free-text `refund_policy` + a dedicated `no_refund_date` on all three surfaces (all 6 create/edit forms); shown to players near the register/join CTA (`RefundPolicyNote`); the tournament + league cancel routes use `no_refund_date` as the authoritative refund cutoff (falling back to `registration_closes_at`). **(2) Event refunds** (migration `20260714000002`): paid play sessions now refund on leave (gated by `no_refund_date` via shared `lib/payments/refundWindow.ts`) and refund all participants on organizer-cancel (ungated); added `event_participants.refunded_at` + `'refunded'` to its payment_status CHECK. **(3) Early-bird tiered pricing** (migration `20260714000003`, `price_tiers jsonb` on all three): a price ladder ("register by <date> at <price>") resolved at checkout by `lib/payments/priceTiers.ts` (tier sets base → code stacks; Pacific-date, register-by inclusive); shared `PriceTiersEditor` in all 6 forms; detail pages show the effective fee + an `EarlyBirdNote`. Wired into all 4 checkout/register routes.

- **Multi-division cart — Phase 5a + 5b + 5c shipped (July 14, 2026)** — bundle several tournament divisions in one payment with a multi-division discount. **5a (engine + runtime):** Schema (migration `20260714000004`): `tournament_orders` + `tournament_order_items` (deny-all RLS) + `tournaments.multi_division_discount jsonb`. Pure engine `lib/payments/multiDivisionDiscount.ts` (`computeBundle`: subtotal → bundle discount → stacked code → total + per-division `net_cents` allocation for receipts/accounting; **per-division cancels refund the *marginal* bundle value via `bundleCancelRefundCents`, NOT the static `net_cents`** — see the refund note below; 19 tests). Organizer config = a "% off each additional division" field on Create/Edit (`percent_additional`, min 2). Runtime: reserve-then-pay `POST /api/tournaments/[id]/orders` (validate 2+ open divisions → reserve N regs [`registered`/`unpaid`] + order → one Stripe Checkout, Connect destination charge), webhook `event_type='tournament_order'` fulfillment (idempotent; emails the payer a division-list receipt + ICS and each covered partner a "your partner paid your entry" note, once per order), `/api/cron/expire-tournament-orders` (daily, `vercel.json` — was `*/15` but Hobby blocks sub-daily crons, which silently froze all deploys; restore 15-min on Pro) to cancel abandoned reservations (plus, so a retry isn't blocked and stale holds don't falsely fill a division until that daily cron, the orders route reaps abandoned pending bundles before its capacity check via shared `lib/payments/reapAbandonedOrders.ts` — the payer's own any-age **and** anyone's >30 min stale — race-safe: a completed/paid Checkout session is left for the webhook, open ones are expired; the cron shares the same helper. July 15 2026, PRs #405/#406), and **marginal** per-division refund in the tournament cancel route (recomputes the bundle over the divisions that remain and refunds `bundleTotal(before) − bundleTotal(after)` — so cancelling a deeply-discounted add-on can't shrink what you paid for a division you keep, and dropping below `min_divisions` reprices what remains; closes a refund arbitrage, **July 15 2026, PR #404**. The bundle discount **and any applied code** are snapshotted on `tournament_orders.discount_config` / `code_config` (migrations `20260715000001` / `...02`) so refunds recompute against the terms actually purchased — a coded bundle refunds the marginal value **net of the code** (no over-refund); partner "pay-for-both" seats + standalone regs refund unchanged, proportional Connect reverse). **5b (player UI):** `BundleRegisterPanel` in the player `DivisionsSection` — when a tournament has a `multi_division_discount` and the logged-in player is eligible for 2+ open divisions (not registered/closed/full), they multi-select divisions, see the live subtotal/discount/total (`computeBundle`), get a schedule-conflict warning (same date + overlapping block times, from the surfaced division timing), and check out once via the orders route. Additive — the per-division Register button is unchanged; panel renders only for non-organizers. **5c (pay-for-both):** for a doubles division in the bundle the player can also cover a partner's entry in the same payment — the panel shows an "Also pay for my partner" toggle + email per selected doubles division; the orders route resolves each partner (must have a Joinzer account, not the payer, not already registered, gender-matched), reserves the pair cross-linked, and adds a **full-price** partner `tournament_order_item` (the bundle discount rewards entering more *divisions*, not a second seat). Each seat is its own order item with its own `net_cents`, so per-seat refunds stay exact and the webhook is unchanged. 5c also hardened the orders route with payer gender validation + team-slot-aware capacity (both were rough in 5a). **Discount codes on bundles (July 15 2026, PR #407):** the bundle checkout accepts a tournament discount code — the panel previews it via `POST /api/tournaments/[id]/discount-codes/validate` (display-only), and the orders route re-validates authoritatively and applies it **stacked after** the bundle discount on the player's own entries only (partner seats stay full price), storing `code_discount_cents` + `discount_code_id`. The Stripe webhook already increments code usage on paid orders; a free-after-code order increments in-route. **Pay-for-both now supports a partner WITHOUT a Joinzer account (July 15 2026, PR #409):** the orders route creates a stub via `createStub` (stamping the division's required gender for gender-specific divisions) instead of 404'ing, and the webhook emails the covered stub a magic-link "claim your spot" (via `generateLink`) after payment instead of the "view tournament" CTA. **Real-Stripe smoke test PASSED (July 15, 2026)** — test mode, non-Connect (plain platform charge), via `stripe listen` + local dev: reserve-then-pay (2 spots held, order `pending`, `net_cents` allocated) → webhook `tournament_order` fulfillment (order + both regs → `paid`, items → `registered`) → confirmation email sent → **per-seat refund** (cancelled one bundled division → refunded only its `$9` of `$18`, the sibling stayed `paid`) → refund email. Connect destination-charge branch (`reverse_transfer`) still unverified (needs a test Connect account). Design + phasing in `docs/phases/multi-division-cart.md`; runbook in `docs/phases/multi-division-stripe-smoke-test.md`. **Cart hardening COMPLETE (July 15 2026):** abandoned-reservation reap — own retry + anyone's >30 min stale, shared `lib/payments/reapAbandonedOrders.ts` (PRs #405/#406); discount codes on bundles + the code-aware marginal refund (#407/#408); partner-invite stub for pay-for-both (#409); and the partner-seat-on-cancel decision — a payer cancelling their **own** doubles entry leaves the **gifted partner seat intact** (not revoked/refunded; the partner or organizer cancels it separately — see the cancel route comment). **Only remaining:** the Stripe **Connect** destination-charge smoke test (`reverse_transfer` on the marginal refund), blocked on a test Connect account.

- **Standings → "Standings/Results" + period browsing (July 14, 2026)** — the "Standings" tab/headers are renamed **"Standings/Results"** across all league formats + the public `/l/[id]` + the tournament tab. A generalized `PeriodSelector` shows the current period's scores by default and lets players/spectators pick previous ones: round-robin → **Week**, ladder → **Session**, team → **Matchday** (box already had Cycles); flex lists **all** confirmed matches. Applied to the authed standings page AND the public page (extended `publicStandings.ts`). Plus a scannable **QR code** on the public-results share card (`api.qrserver.com`, public-UUID URL).

- **Session end-of-day prompts + score gate (July 14, 2026)** — box leagues show a persistent "cycle complete → advance" banner once every box match is scored (covers the player-self-scored / dismissed cases). Round-robin **live** sessions detect "every present player has faced everyone" (`lib/leagues/sessionCompletion.ts`) → a banner + a 3-choice modal (Generate another round / 🏁 End the day / Not yet). RR also now **requires the current round to be fully scored** (every non-bye match, not just one) before "Generate next round" — an in-progress locked round or a partially-scored round is blocked.

- **Tournament registration UX (July 14, 2026)** — **shareable per-division link** (`/tournaments/[id]?division=<id>` deep-link; a Share button per division card; landing highlights + scrolls to the division) and **division timing surfaced to players** — each division card shows its scheduled day/time from the existing schedule blocks (`tournament_division_blocks` → `tournament_schedule_blocks`), read via the service-role page loader. This is the concurrency read path the multi-division cross-sell (5b) will use.

- **Admin — duplicate venue detection (July 14, 2026)** — the pending-venues approval page (`/admin/locations`) flags likely-duplicate venues under each pending row (name similarity + geographic proximity + shared ZIP via `lib/locations/duplicates.ts`, 14 tests) so the admin can reject twins before approving.

- **Realtime architecture — Phase 1 shipped (July 14, 2026)** — a reusable event-driven realtime foundation replacing the old per-component `supabase.channel()` fragmentation, built on Supabase Realtime (no new deps, **no React Query**). `lib/realtime/`: a `RealtimeProvider` (mounted in `app/(app)/layout.tsx`) owning **one shared browser client + socket** (`lib/supabase/client.ts` is now a memoized singleton) and a **refcounted `ChannelManager`** (N subscribers to a topic share one channel); a typed topic/event registry (`topics.ts`); hooks `useRealtimeChannel` / `useRealtimeList` / `useAttendanceBroadcast` / `useConnectionStatus`; and `serverBroadcast.ts`. **Two delivery mechanisms under one hook API:** `postgres_changes` for client-readable tables (the 3 chat message tables) and **server Broadcast** for deny-all/sensitive tables (attendance) — the write route emits a minimal non-PII event so RLS stays deny-all (the load-bearing decision, since `postgres_changes` respects RLS and delivers nothing on deny-all tables). **Wired:** `ChatPanel` moved onto `useRealtimeList` (INSERT/UPDATE/DELETE patch the one row, optimistic sends dedupe by client-generated id, reconnect refetches the tail, scroll preserved with a "N new messages" pill); live attendance for round-robin (`WhoIsComing`, now broadcast not pg_changes) and box/ladder (`BoxAttendanceManager`, previously not live) via broadcast from the attendance write routes; subtle `ConnectionIndicator` (green dot / Reconnecting / Offline) in the header. **Phase 2 (July 14, 2026):** **toasts** (`components/ui/ToastProvider.tsx` — auto-dismiss, capped, de-duped; `WhoIsComing` toasts when *another* player changes status); **in-context chat unread badge** ("N new" on `ChatPanel` via localStorage last-read, cleared on engage — cross-app nav badges still deferred); and **live tournament scores** — migration `20260714000005` published `tournament_matches` (public-readable → `postgres_changes`, not broadcast) plus `league_matches` + `league_session_players`, which **also fixed two silently-dead subscriptions** (`LiveScoreboard` and `LiveSessionManager` subscribed to tables that were never in the `supabase_realtime` publication, so their "live" never fired); `LiveScoreboard` consolidated onto the shared infra. **Phase 3 (July 14, 2026) — live league fixtures/results:** `league_fixtures` is deny-all (box/ladder/flex/team), so each fixture score route broadcasts a coarse per-league "changed" signal (`broadcastLeagueFixtures`) and a reusable `RealtimeRefresh` client component — mounted once in a new `leagues/[id]/layout.tsx` — debounce-triggers `router.refresh()` so every league sub-page (standings/results, ladder, flex, box, team) re-derives from fresh server data (the third consumption mode alongside `useRealtimeList` and broadcast-patch). **Full coverage:** every league write route broadcasts — box/ladder score, all 5 flex routes, ladder round + finalize, team matchup + line scores, flex self-schedule, and all generation routes (box generate/save, cycle advance, flex/team schedule); **RR scores** too (player-score route broadcasts; RR organizer scoring writes `league_matches` client-side with no `league_id`, so a gated `POST /fixtures-changed` ping re-emits, called by `LockedRoundsScoring`/`MatchEntryForm`); and the **public `/l/[id]`** page is live via a self-contained `RealtimeProvider` + `RealtimeRefresh` (`PublicLeagueLive`). **Phase 3 (July 14, 2026):** **presence** (`usePresence` + `ViewerCount` "N watching" on the tournament live scoreboard + public `/l/[id]`); **cross-app chat unread badges** (`ChatUnreadProvider` in the app layout → dot on the Leagues/Tournaments nav; `GET /api/chat/unread-sources` + per-entity subscribe + localStorage last-read + `chat:read` event; per-entity because message-table SELECT RLS is `USING(true)`); and **live interactive bracket** (`DivisionManageView` merges other scorers' `tournament_matches` changes but skips matches with an unsynced local write, so offline/in-progress scores aren't clobbered). **Phase 4 (July 14, 2026):** chat **edit/delete own messages** (migration `20260714000007` author UPDATE/DELETE policies for league/tournament messages — event already had them — + ChatPanel inline UI; the realtime layer already patched UPDATE/DELETE); **live notification bell + toasts** (`createNotification`/`createNotifications` broadcast per-recipient → the bell badge + a 🔔 toast go instant, replacing the 60s poll, now a 120s fallback); **live tournament check-in** (organizer `PlayersTab` subscribes to `tournament_registrations` UPDATEs — QR self-check-ins + co-organizer toggles update the roster live). **Private-channel authorization done (July 14, 2026, migration `20260714000008`):** the per-user `notifications:<userId>` broadcast is now a **private** channel (only the recipient may receive it) — RLS on `realtime.messages` (`realtime.topic() = 'notifications:' || auth.uid()`) + `ChannelSpec.private` → `config.private`, `serverBroadcast({private:true})`, and `RealtimeProvider` sets `realtime.setAuth()`. Validated with two real signed-in users (own = received; other = CHANNEL_ERROR). Attendance + league-fixtures stay public by design (non-PII / anon spectators). (Bespoke-channel cleanup **done** — TournamentOrganizerView / GroupChat / LiveSessionManager / DivisionsSection all migrated onto `useRealtimeChannel`; the only direct `supabase.channel()` left is the ChannelManager + the documented `usePresence` exception.) **Chat RLS hardened (July 14, 2026, migration `20260714000006`):** message SELECT (all 3 tables) + league/tournament INSERT are now membership-scoped via SECURITY DEFINER helpers (`is_{league,tournament,event}_chat_member`, locked search_path); each ChatPanel render is member-gated to match (so non-members don't see the server-rendered initial batch either). Was `USING(true)` — any authed user could read/post any chat. Verified member reads / non-member blocked via set-role JWT-claims. **Transport validated (July 14, 2026):** both paths confirmed end-to-end headlessly — a server-Broadcast round-trip (HTTP 202 → subscribed client received it) and a postgres_changes delivery on the now-published `tournament_matches` (real change delivered to an anon subscriber). Gotcha: a *same-value* update fires no event; only real changes do. UI-level two-device checks (optimistic/scroll/toasts/mid-edit) remain — `docs/phases/realtime-smoke-test.md`. Full design + "how to add a realtime feature" in `docs/phases/realtime-architecture.md`.

- **Court directory — Reno–Sparks metro imported + published (July 28, 2026)** — the second metro after Phoenix. 36 merged research venues (union of two independent nine-stage runs) landed via `scripts/import-reno-merged.mjs` in three dry-runnable stages: 36 → `facility_candidates` (`batch='reno-merged-2026-07-28'`, the lifecycle/work queue), 36 → `facility_listings` as **draft**, then a gate pass publishing **29** and leaving **7** draft. Site-wide published listings 176 → **205**. **Publish gate (owner ruling, July 28): coordinate present + coordinate precision ≠ `low` + slug + `access_type` ≠ `unknown` + candidate `research_status='verified'`. `court_count` is deliberately NOT a gate condition** — **⚠ THE PRECISION CLAUSE OF THIS RULING WAS SUPERSEDED August 4, 2026 by ADR-16** (`docs/strategy/decision-log.md`): a `low`-precision coordinate now **publishes**, behind a user-visible approximate-location label. Coordinate-*present* is still required. **⚠ AND THE REMAINING CONDITIONS WERE SUPERSEDED August 5, 2026 by ADR-17** — `access_type` and candidate `research_status` are no longer gate conditions at all; the gate is now **name (present + not generic) + coordinate + city + slug**, defined once in `scripts/lib/publish-gate.mjs` and shared by both publish scripts. Only correctness verdicts (`duplicate`/`not_venue`/`not_pickleball`) plus an explicit `held` still block. **Nothing published by itself when that landed:** ADR-17 also split the *gate* (quality) from the *release fence* (`verified_by IS NOT NULL`), and the fence is what keeps 446 gate-passing drafts held until an owner runs `--stage=publish` for their metro. The July 28 wording is left intact deliberately — it is the record of what this import actually ran under — brief §5's gate is coords + slug + enrichment/approval, and a non-null count was an extra research-side condition, never Joinzer's. The 7 held back: 2 `probable` (no controlling-entity source), 1 `held` (venue closed to 2026-09-01), 2 with no storable coordinate, 2 with a `low`-precision coordinate. They keep their real `research_status` and carry **`verified_by = NULL`** so the reconcile gate can't grab them. **`source` is set explicitly to the batch tag `reno-merged-2026-07-28`** — `facility_listings.source` is `NOT NULL DEFAULT 'osm'` and omitting it would mislabel the batch as OSM-ingested, which would be half-true and hard to spot since 23 of the 36 genuinely carry OSM-derived coordinates; the dataset kind lives at `provenance.method='merged_research'`, and the batch tag is what makes the one-statement rollback (`update … set status='draft' where source=…`) work. **Per-field provenance:** `facility_candidates` has no jsonb and no venue-fact columns, so it cannot hold `court_count`/`indoor`/`fee_type`/`website`/`phone` or any per-field evidence — that all lives in `facility_listings.provenance` (per-field `source_url`/`source_tier`/`confidence`/notes, the coordinate ladder record, aliases, sub_area, next_review_at, conflicts, publish-gate verdict). Nothing from the artifact is dropped. **ADR-12:** every coordinate re-sourced off Google Places (OSM cluster centroid 11 / aggregator 11 / Nominatim 12 / null 2); `address_source='manual_research'` throughout; only `place_id` stored from Places. **ADR-14:** aggregator evidence sits at tier 4 in `provenance` only — the importer asserts no aggregator URL reaches `website`/`name_source_url` on a published row, and `lib/directory/loadFacilities.ts` documents `provenance`/`website`/`name_source_url` as render-restricted. `regency-at-caramella-ranch` publishes with **`court_count` nulled** — the venue is tier-1 legitimate but its count was aggregator-only and actively disputed (55places 7 vs OSM 3); evidence retained in `provenance.court_count_suppressed`. **ODbL:** 23 rows (19 published) carry OSM-derived coordinates; `OsmAttribution` renders on both `/courts` and `/courts/[slug]` — verified, don't remove it. Migration `20260728000001_formalize_facility_candidates_address_source` (additive, idempotent, no-op against prod) closes repo↔prod drift: `facility_candidates.address_source` existed in production via a migration that was applied but never committed. `scripts/publish-facilities.mjs` gained two reconcile guards (research-status join + coordinate precision — **not** court_count) so a future Reno enrichment run can't auto-publish the held rows; verified to un-publish 0 currently-published rows in either metro. Research artifacts live in the gitignored `reno-count/`. **Crawler reachability — FIXED July 28, 2026 (PR #469).** Previously `/sitemap.xml` and `/robots.txt` were 307-redirected to `/login` by `middleware.ts` (the allowlist covered `/courts` but not those two), so no court page was discoverable via sitemap — it affected all 205 published pages, not just Reno. Both are now allowlisted by **exact equality** (not `startsWith` — single fixed routes, no subtree exposed). The omission was incidental, not a deliberate gate: `app/sitemap.ts`, `app/robots.ts` and the `/courts` allowlist line all landed in one commit (`3d647b5`, PR #463) whose entire middleware diff was that single `/courts` line, and `202b8f6` ("Allow /manifest.webmanifest through auth middleware") was an earlier corrective for the same class of miss. The matcher's exclusion list only skips image extensions, so `.xml`/`.txt` genuinely reach the middleware body. **Crawl posture got tighter, not looser:** the 307 resolved to HTML carrying no valid directives, which Google treats as "no robots.txt" (crawl everything); serving the real file means `allow: /` + `disallow: /api/` is now actually honored.

- **Court directory — metro landing pages + facet filters (July 28, 2026)** — `/courts` was one flat list of all 205 published rows with Reno venues mixed into Phoenix ones. It is now a **metro hub**, with server-rendered landing pages at **`/courts/in/[metro]`** (`/courts/in/phoenix`, `/courts/in/reno-sparks`). **Route shape is `/courts/in/[metro]`, not `/courts/[metro]`, deliberately:** `facility_listings.slug` is `unique not null` with no reserved-word mechanism (`makeUniqueSlug` in the import scripts has no reserved list), so sharing the `/courts/*` namespace would let a future venue slugged `phoenix` be **silently shadowed** by the metro page, and reserving slugs per-metro would need a migration for every new metro. The `in` segment reserves exactly one implausible word and leaves `app/courts/[slug]/page.tsx` untouched. **Metro slugs are derived from the stored `metro_area` value, never hardcoded** — `loadPublishedMetros()` feeds the hub, the routes and the sitemap from one source, so publishing a new metro makes all three appear **with no deploy** (the 73 draft Las Vegas rows — 64 from `vegas-parity-2026-07` plus 9 imported 2026-07-31 as `vegas-merged-2026-07-31` — already carry `metro_area='Las Vegas'` and will surface on publish). **Facets (`lib/directory/facets.ts`, 42 unit tests): fee, access, setting, reservation policy, city — inclusive-only.** Every option is an affirmative equality test; there is no `!==`, no `== null`, no `OR IS NULL` in the file. **The trap is that `'unknown'` is a STORED value** meaning "researched but undetermined" (NULL means "not yet researched" — migration `20260724000002`), so `count(non-null)` reads as coverage when it isn't: reservation_policy is 92% non-null but only **61% usable** site-wide (62 Phoenix rows are `'unknown'`), fee_type 93% non-null but **85% usable**. Both absences are excluded from every bucket and never render as a chip; each facet shows an honest coverage line ("Confirmed for 114 of 176") when coverage is partial. `indoor` is the one place a `false` IS an affirmative fact (145 researched-outdoor rows), so Indoor/Outdoor are both honest and NULL is in neither. **Lighting (35% fill) and surface (7%) get no facet at all**; `court_count` (67%) is a **sort**, not a facet — rows without a confirmed count move to a labeled "Court count not confirmed" section rather than being hidden or implied to have zero. A facet with fewer than two non-zero options is dropped entirely, which is how a thin-coverage metro (Reno `fee_type` 48% vs Phoenix 100%) adapts with no per-metro code. **Faceted nav is a crawl trap**, so the clean metro URL is the only indexable one — any URL carrying a filter or sort param is `noindex, follow` and self-canonical, and filtered URLs are kept out of the sitemap. Mobile-first per ADR-09: native `<details>` panel collapsed at 375px, always open at md+, 44px tap targets, every control a plain `<Link>` so filtering works with JS off. QA'd at 375 and 1280. The directory also had **no internal link from anywhere** (not LandingNav, not LandingFooter, not `/browse`) — 205 pages reachable only by direct URL; a footer link plus a hub→metro→facility breadcrumb loop (with BreadcrumbList JSON-LD) fixes that. **No schema change** — every facet uses an existing populated column. **A facet with fewer than two non-zero options is dropped — EXCEPT when it holds a selection** (see the active-filter fix below); that exception was added July 29 and is load-bearing.

- **Court directory — Playwright suite committed + active-filter fix (July 29, 2026, PR #471)** — two things, one PR. **(1) The 55-spec court directory suite is now in git.** It was written alongside PR #470 but never committed, so a fresh clone had zero coverage for `/courts`, `/courts/in/[metro]` and `/courts/[slug]` — a suite that exists on one machine is not a gate. Committed as-is on its own commit, passing against its parent, so the baseline predates the fix. Now **57 specs** (`tests/e2e/courts-{crawler,directory,honesty}.spec.ts` + `helpers/directory.ts`), run at 375px and 1280px. **(2) An applied filter could become invisible AND individually unremovable.** `buildFacetViews` drops a facet once fewer than two options have a non-zero count, and both `ActiveFilters` and the panel were built from those views — so a filter narrow enough to collapse its own facet lost its chip *and* its whole control group. Verified on production: `/courts/in/phoenix?fee=free&city=anthem` returned 0 rows and rendered **one** chip for **two** applied filters, whose href was `?fee=free` — removing it left the invisible filter applied. Only "Clear all" recovered. Fix: new pure export **`activeFilterChips(selection, facets)`** — an applied filter is a fact about the URL, not about what the panel had room to render — plus a drop-rule exemption for any facet holding a selection (decisive at 375px, where the panel ships collapsed and the user may never have seen the filter applied). **Active-filter chips deliberately carry no count**: a chip claims only "this filter is applied", so it makes no numeric assertion and cannot make a false one; `"Free (0)"` was considered and rejected because when the result set is empty and each facet holds one value, *every* chip's count is necessarily zero, so it would disambiguate nothing. The regression spec was proven as a **negative control** — it fails on the parent commit at both viewports and passes on the fix. Unit tests 591 → 599. No schema change.

- **Court directory — canonical host fix ⚠️ OPEN, NOT DEPLOYED (PR #472)** — every page declares a canonical URL that **307-redirects**. `metadataBase` resolves from `getSiteUrl()`, whose default was the apex `https://joinzer.com`, while `app/sitemap.ts`, `app/robots.ts` and both directory JSON-LD breadcrumbs each carried their own hardcoded `https://www.joinzer.com`. The apex→www redirect is **Vercel domain config** (`next.config.mjs` has no `redirects()`), so www is canonical by platform decision and `metadataBase` was the lone dissenter — across all 205 facility pages, both metro pages, `/courts`, and `og:image` on every page in the app. `NEXT_PUBLIC_SITE_URL` is **unset in Vercel (Production and Preview) and in every `.env` file**, so the hardcoded default was the live value everywhere and the one-line default change is the production fix. PR #472 points all five consumers at `getSiteUrl()`. **It is deliberately held**: `getSiteUrl()` also builds Supabase magic-link `redirectTo` URLs, which Supabase honors only when they match the project's Redirect URLs allowlist — apex-only today. Merging before `https://www.joinzer.com/**` is allowlisted would silently drop the `next` param and dump partner invitees on `/home`. External config first, dependent code second — the same rule as migration-before-code. See `docs/partner-invite-flow.md`.

- **Court directory — crawl-budget incident fix (July 30, 2026)** — Vercel paused the project on 2026-07-29 (Hobby limits blown: 1.8M/1M Function Invocations, 12h6m/4h Fluid Active CPU, 15.44/10 GB Fast Origin Transfer, against **zero real users**). Root cause: `/courts/in/[metro]`'s 4 combinable facets + a per-metro city facet + a sort toggle form a faceted-navigation crawl trap, and `/courts` + `/courts/[slug]` were `force-dynamic` with no functional reason (neither reads `searchParams`). Three-part fix, no schema change: **(1)** `/courts` and `/courts/[slug]` converted to ISR (`revalidate = 21600`, i.e. 6h — matches "changes on the order of weeks, stale-by-an-hour is fine"); `/courts/[slug]` also gained `generateStaticParams()` (build-time pre-render of all published slugs, try/catch-guarded so a build-time DB hiccup degrades to on-demand rendering, not a failed build) — this is the biggest lever, 205 of ~208 sitemap URLs. **(2)** `lib/directory/loadFacilities.ts`'s six loaders wrapped in `unstable_cache` (`DIRECTORY_CACHE_SECONDS`, same 6h window) — cuts DB round-trips on the metro pages, which stay genuinely `force-dynamic` (reading `searchParams` opts a route into dynamic rendering regardless of any config export; PPR was ruled out as out-of-scope/experimental for an incident fix). **(3)** `app/robots.ts` gained a scoped `Disallow: /courts/in/*?*` — blocks crawling of any filtered/sorted permutation while leaving every canonical courts URL (`/courts`, `/courts/in/<metro>`, `/courts/[slug]`) fully crawlable; `noindex` alone doesn't stop a crawler from fetching the page to see the tag. `tests/e2e/courts-crawler.spec.ts`'s robots.txt assertion was rewritten (not just threaded) to check the real two-sided intent — no blanket `/courts*` disallow AND a scoped filtered-URL disallow present — since the prior literal "no Disallow starting with /courts" check predated this need. **Second-order regression found and fixed in the same slice:** converting `/courts` and `/courts/[slug]` to ISR made Next's `<Link>` eagerly full-prefetch them on scroll-into-view (dynamic routes with no `loading.tsx` get no such prefetch; static ones do) — on a metro page with up to 176 `FacilityRows` links plus 3 more links to `/courts` (breadcrumb, "all metros"/"all courts", `LandingFooter`), this became a background-request storm that blew past the e2e suite's `networkidle` waits (proven with a standalone repro script isolating pending requests, not guessed). Fixed with `prefetch={false}` on those specific links (`FacilityRows.tsx`, both courts page breadcrumbs/back-links, `LandingFooter`'s Court Directory link) — a defensible product choice independent of the test (the destination is already ISR-cached, so prefetch saves little latency at real request-volume cost), and it also cut the courts e2e suite's own runtime from 8+ minutes to 1.4 minutes. Gates: tsc 0, build 0 (route manifest: `/courts` and `/courts/[slug]` now `○`/`●` with `6h`/`1y` revalidate/expire columns, `/courts/in/[metro]` and `/sitemap.xml` unchanged `ƒ Dynamic`), vitest 58/58 · 604/604 (unchanged), courts Playwright suite 57/57 at both viewports. **Not fixed, flagged for a future slice:** `middleware.ts` runs a `supabase.auth.getUser()` round-trip on every request matching its matcher, including every public `/courts/*` request (allowlisted for the redirect decision, but the auth check still executes first) — a real secondary cost, untouched here since it's security-adjacent and outside this incident's scope fence. **Efficacy caveat:** Vercel Hobby's runtime-log retention is 1 hour, so the actual per-path traffic composition during the incident window could not be log-confirmed; the diagnosis rests on code-path analysis (facet combinatorics + sitemap size), not a verified breakdown. The owner separately upgraded to Vercel Pro (2026-07-30) and production is live again — this fix reduces cost/load regardless of plan tier.

- **Court directory — Little Rock MSA imported + published (July 30, 2026)** — the fifth metro. 19 venues (17 verified + 2 probable) across the Little Rock–North Little Rock–Conway AR MSA landed via `scripts/import-little-rock-merged.mjs` (`batch`/`source` = `little-rock-2026-07-30`): 19 → `facility_candidates`, **18 INSERT + 1 reconcile UPDATE** → `facility_listings` as draft, then a gate pass publishing **16** and leaving **3** draft. Site-wide published 236 → **252**, total listings 852 → **870 (+18, NOT +19 — the reconcile discriminator)**, candidates 550 → **569**; published metros 4 → **5**. **`metro_area` is `'Little Rock'`** — the anchor city, following Daytona (whose MSA is "Deltona–Daytona Beach–Ormond Beach"); the full MSA form would slug to `little-rock-north-little-rock-conway`, and a compound name doesn't earn its length when venues also sit in Bryant, Benton, Haskell, Cabot and Maumelle. **The reconcile:** Conway Community Center merged onto a dormant `source='osm'` listing (`osm_id='way/1498910770'`) — the independent Nominatim geocode resolved to *that exact OSM element*, 2 m away, and its slug was already `conway-community-center-conway-ar`, so an INSERT would also have collided on the unique slug. UPDATE keyed on `osm_id`, guarded `and status='draft'`, asserted to affect exactly 1 row, `osm_id` preserved, OSM original stashed in `provenance.osm_reconcile.osm_original`. Critically its `access_type` was `'unknown'`, which the publish gate blocks — the reconcile sets it to `'public'`, without which the row would have silently stayed draft. Unlike Daytona's "Pickleball Courts" the existing name was already real, so `isGenericName()` forced no rename. **The source workbook's coordinate data was corrupted and was NOT used as a source.** On the Venues tab, rows LR-008..LR-019 had every column from `phone` onward shifted one place right (the *latitude* column held a phone number, the *longitude* column held the latitude) and the longitude fell out of the row entirely, landing in the Review Decisions tab's `decision_notes` column. Every coordinate was instead independently geocoded via Nominatim/OSM, with the reconstructed workbook pair kept per row as `provenance.coordinate.workbook_crosscheck` — which proved the point: **on 9 of 19 rows the workbook disagreed by more than 1 km** (2.9 km Jess Odom, 2.4 km Bishop Park, 5.35 km Indianhead), and on Henson Park two independent OSM lookups agreed with each other to 85 m while both rejected the workbook by 1.3 km. Preflight *re-derives* each cross-check distance rather than trusting the stored number, so a bad artifact edit cannot launder a workbook coordinate back in as authoritative — it caught a 355-vs-336 m error on the first run. **3 held drafts:** `pickleball-kingdom-little-rock-ar` (`precision='low'` — the venue is absent from OSM, four query variants all returned HTTP 200 with an empty set, so only the Bass Pro Pkwy street band geocodes), plus `indianhead-park-sherwood-ar` and `tyndall-park-benton-ar`, which were **imported deliberately at `research_status='probable'`** so they sit in the work queue with full provenance while the gate blocks them — a departure from Daytona's outright exclusion, justified because those had identity risk whereas these two carry named government sources and were independently corroborated by geocoding. Promotion later is a one-field UPDATE plus a publish re-run. **Enum discipline:** the workbook was invalid in six columns again — `fee_type` (`paid`/`day_pass`/`annual_pass`), `reservation_policy` (`first_come`/`scheduled`/`open_play`/`reservation_available`), `court_configuration` (`shared-use`, hyphenated here where Greensboro used an underscore), `line_type` (`painted`), `net_setup` (`portable`), `surface` (`gym`, `cushioned`). **Unlike Greensboro, `surface` is mostly usable here** — 12 of 19 values (`asphalt`/`wood`/`hard`/`concrete`/`acrylic`) are live-vocab and written as-is; only `gym` and `cushioned` fall back to NULL. **No schema change, no migration.** `/courts/in/little-rock` live with no deploy, subject to the usual 6h ISR/`unstable_cache` lag. Research artifacts in the gitignored `little-rock-count/`. **Flagged for the next refresh, shipped as researched:** Silver Springs Country Club is `access_type='public'` + `fee_type='free'` (surprising for a country club, though its source explicitly states public access), and Sherman Park Community Center is `public`+`membership` while its sibling North Little Rock Community Center under the same operator is `membership`+`membership`.

- **Court directory — Greensboro–High Point MSA imported + published (July 30, 2026)** — the fourth metro after Phoenix, Reno–Sparks and Daytona Beach. 15 verified venues across Guilford, Randolph and Rockingham counties landed via `scripts/import-greensboro-merged.mjs` (`batch`/`source` = `greensboro-high-point-2026-07-30`): 15 → `facility_candidates` (`research_status='verified'`), 15 → `facility_listings` as **draft**, then a gate pass publishing **14** and leaving **1** draft. Site-wide published listings 222 → **236**, total listings 837 → **852 (+15, proving no duplicate)**, candidates 535 → **550**; published metros 3 → **4**. Structurally this is the **Reno shape, not the Daytona shape — pure greenfield, 15 INSERTs, zero reconciles.** Three dormant `source='osm'` rows do sit inside the envelope (Kaplan Center, Friendly Swim & Tennis, J. Spencer Love Tennis Center) but all are different venues; nearest approach from any imported row is 475 m. That is not assumed — **preflight recomputes the distance from all 15 against every live listing in the envelope on every run** (`RECONCILE_RADIUS_M = 200`) and aborts with a reconcile-vs-insert escalation rather than guessing. **`metro_area` is stored as `'Greensboro-High Point'` — ASCII hyphen, no state suffix** — because `metroLabel()` appends `state` itself, so the workbook's raw `'Greensboro–High Point, NC'` would have rendered "Greensboro–High Point, NC, NC" and slugged to `/courts/in/greensboro-high-point-nc`, unlike every other metro. **All 15 coordinates were geocoded at import via Nominatim/OSM** (the workbook had addresses but no lat/lng); no Google or Places call was made, so ADR-12 is not approached, and because every coordinate is OSM-derived **every row carries the ODbL marker** (Daytona set it on only the one reconciled row). Precision uses a **high/medium/low ladder** — an extension of Daytona's binary high/low; the gate tests `!= 'low'`, so `medium` (correct site, anchor is a neighbouring feature or a large polygon centroid — Lake Daniel's greenway centroid, Bur-Mil's clubhouse) publishes. **The 1 held draft is `hartley-drive-family-ymca-high-point-nc`:** the venue is absent from OSM entirely — eight query variants all returned HTTP 200 with an empty result set (verified directly, not inferred from rate-limiting) — so only the Hartley Drive street band geocodes, giving `precision='low'`. Same treatment as Daytona's bethune-beach row: import as draft, let the gate hold it, re-geocode to the exact 150 W Hartley Dr number to unblock. **8 `probable`/`held` workbook candidates were held out entirely** and carry no rows at all; several are sourced only to playtimescheduler.com, a tier-4 aggregator that ADR-14 bars from verifying a venue on its own. **Enum discipline — the workbook disagreed with the live schema in six columns, not the two the brief flagged:** `reservation_policy` was invalid in **all six** of its workbook forms (`first_come_first_served`, `..._except_programs`, `scheduled_drop_in`, `scheduled_open_play`, `required`, `reservable`), plus `fee_type` (`drop_in`, `membership_or_guest_pass`), `verification_status` (`reviewed`), `court_configuration` (`shared_use`), `line_type` (`painted`) and `net_setup` (`portable`). All were mapped in the input artifact and every one is asserted against the live vocabulary in preflight, which aborts rather than coerces. The "scheduled" qualifier is availability, not policy — it maps to `drop_in` with the schedule text left in `public_notes`. **`surface` is NULL on all 15:** the workbook's `gym floor` / `multi-sport` are not in the live CHECK vocabulary, `wood` would invent a material and `other` conveys nothing, so the raw string is preserved in `provenance.fields.surface` instead. This batch is also the **first to populate `court_configuration` / `line_type` / `net_setup`**, which Reno and Daytona left null. **No schema change, no migration** — the schema already existed; this is data. `/courts/in/greensboro-high-point` went live with no deploy, subject to the same 6h ISR/`unstable_cache` propagation lag. Research artifacts live in the gitignored `greensboro-count/`.

- **Court directory — Daytona/Volusia MSA imported + published (July 30, 2026)** — the third metro after Phoenix and Reno–Sparks. 18 verified venues from the Deltona–Daytona Beach–Ormond Beach FL MSA landed via `scripts/import-daytona-merged.mjs` (`batch`/`source` = `daytona-2026-07-30`), mirroring the Reno script's dry-runnable stages: 18 → `facility_candidates` (`research_status='verified'`), 17 → `facility_listings` as **draft**, then a gate pass publishing **17** and leaving **1** draft. Site-wide published listings 205 → **222**; published metros 2 → **3**. **The structural difference from Reno (pure greenfield, 36 INSERTs): this batch RECONCILES one venue onto a pre-existing row.** `ormond-nova` matched a dormant `source='osm'` listing (`osm_id='way/1367420477'`, 119 m away, named the generic "Pickleball Courts"), so it is an **UPDATE keyed on `osm_id` and guarded `and status='draft'`, not an INSERT** — the listings stage is 17 INSERT + 1 UPDATE. Inserting instead would have created a duplicate the dedupe never catches. The decisive proof it stayed an UPDATE is the **site-wide** count delta (820 → 837 = **+17**, not +18); the batch count is 18 either way, so the script's own verify stage cannot catch that class. The reconcile renames the row (mandatory — `publish-facilities.mjs`'s `isGenericName()` would draft "Pickleball Courts" back), re-tags `source` to the batch so the one-statement rollback covers it, preserves `osm_id`, and stashes the original OSM centroid + surface + name into `provenance.osm_reconcile.osm_original` with an ODbL marker. **The 1 held draft is `bethune-beach-park-new-smyrna-beach-fl`:** its coordinate is an approximate S Atlantic Ave band rather than the exact 6656 number, so it carries `provenance.coordinate.precision='low'` and the publish gate correctly blocks it. **A low-precision coordinate is a feature at import, not a blocker** — import as draft and let the gate hold it; never fake precision to clear the gate. Re-geocoding to the exact address is the follow-on that unblocks it. **2 `probable` venues were held out entirely** (`ormond-south-center`, `edgewater-hawks` — pickleball evidence was aggregator-only, and Nominatim resolves the Edgewater address to a YMCA, an identity risk); they were never inserted, so they carry no rows at all. **Enum discipline:** the research input contained two values that are not live CHECK vocabulary — Pictona as `fee_type='paid'` and six rows as `fee_type='scheduled'`. Neither exists (`fee_type` is `free/fee/membership/unknown`). Pictona encoded to `access_type='public'` + **`fee_type='fee'`** per the ADR-13 walk-in-commercial map; `scheduled` is an availability note, not a fee, so those rows went to `free` or `null` with the schedule text moved into `public_notes`. The importer asserts every enum against the live vocabulary and aborts rather than coercing. **One correction applied post-import, pre-publish:** the Cherry row was first applied with `slug='cherry-cultural-educational-center'`, malformed because it dropped the `-city-state` suffix every other row carries; corrected on the draft row to `cherry-cultural-educational-center-daytona-beach-fl` before publish froze it into a URL. Fixing a slug is cheap while `status='draft'` and expensive after. **No schema change, no migration** — the schema already existed; this is data. **`/courts/in/daytona-beach` went live with no deploy**, since `metroSlug()` derives metro slugs from the stored `metro_area` and `loadPublishedMetros()` feeds the hub, the routes and the sitemap from that one source. Note the directory's 6h caches (`/courts` + `/courts/[slug]` ISR `revalidate = 21600`, `loadFacilities.ts` loaders in `unstable_cache`) mean new venues can take up to 6h to appear on the list and hub pages — expected propagation lag, not a bug; individual `/courts/[slug]` pages render on demand immediately. Research artifacts live in the gitignored `daytona-count/`.

- **Court directory — Google Maps link fix + `google_place_id` backfill (July 30, 2026) — PR #479, MERGED `210abca`** *(the "53 → 39" and "Daytona/Greensboro HELD" figures below are this slice's end state; the follow-on entry supersedes them)* — the "View on Google Maps" button on `/courts/[slug]` was broken for **53 of 236 published rows**, including **100% of the Daytona and Greensboro batches**. `lib/directory/mapsUrl.ts` fell back to a raw `lat,lng` query whenever `google_place_id` was null, and Maps renders that as an **anonymous dropped pin** — it reverse-geocodes the coordinate, shows no venue card and no name, and re-centers on whatever parcel it picks (the Cherry row's URL rendered with the page title `29°12'40.8"N 81°02'29.9"W`). **The stored coordinates were never wrong** — Cherry's matches the OSM building footprint exactly. This was a URL shape bug plus an importer gap. **New ladder: `place_id` → `"<name>, <address>, <city>, <state> <zip>"` text query → raw coordinate.** Rung 3 keeps the old behavior **deliberately**, because the two better-looking options were tested in a real browser and both fail dangerously: `"<name>, <city>, <state>"` with no street address resolved `Asante, Surprise, AZ` to the **neighborhood centroid ~1.8 km away**, rendered as an authoritative place card; and the `"<lat>,<lng> (<Label>)"` form makes the `api=1` endpoint **ignore the coordinate entirely** and text-search the label — `33.701312,-112.402054 (Asante Pickleball Courts)` landed on a venue in **Henderson, NV, ~600 km away**. The rows that reach rung 3 are disproportionately the HOA / active-adult class ADR-14 records as the venue type Places systematically cannot surface, i.e. exactly where a text query resolves confidently to the wrong place — so an unnamed pin at the right coordinate is the honest floor, and the real fix for those rows is a place_id, not a cleverer URL. `mapsUrl()` now takes an object (`Pick<FacilityDetail, …>`); one caller; **16 unit tests** pin every rung *and* both rejected forms as negative assertions (591 → 599 → **620 tests / 59 files**). **Root cause of the blast radius:** `import-reno-merged.mjs` reads `google_place_id` from its artifact while `import-daytona-merged.mjs` and `import-greensboro-merged.mjs` **hardcoded `null`** on both the candidate and listing rows — fixed in both (a no-op on a re-run, since neither artifact carries the field; it exists so the next batch copied from them inherits the right shape). **`publish-facilities.mjs` gained a NON-BLOCKING advisory** counting eligible rows with no place_id. It is deliberately *not* a gate condition: that script is **reconciling** (it drafts rows that stop being eligible), so a blocking check would have **un-published 31 live rows across two entire metros** on the next pass — the owner ruled against the hard gate for exactly that reason. **Backfill (applied to prod, no migration — DML on an existing nullable column, guarded `google_place_id IS NULL` on both the select and the update so a value can never be overwritten): published rows missing a place_id 53 → 39.** Two were fixed with **zero API calls** — their own `facility_candidates` row already carried a place_id at a 0 m coordinate match that the import never copied across. The other 12 came from `scripts/backfill-place-ids.mjs`, a new **two-phase** tool: `--dry-run` makes one Places call per row and validates each hit against the stored coordinate (≤500 m), writing proposals to the gitignored `place-id-backfill/`; `--apply=<file>` makes **zero** Places calls and writes only what a human reviewed. That halves the paid calls and makes the write provably equal to the review. First run scoped to `--metro=Phoenix,Reno-Sparks` (20 rows, 20 calls, Text Search Pro SKU — inside the 5,000/month free tier, **$0.00**): 13 matched, 7 rejected by distance, **1 of the 13 rejected in hand-review**, 12 written. Reno–Sparks is now complete; **zero place_id collisions site-wide**. **ADR-12** respected — place_id is the only Places datum persisted; `displayName`/`location` validate the match in memory and are discarded. **ADR-14** respected — sequential per-record lookups, rate-limited, never a bulk scrape. **Daytona (17) and Greensboro (14) are deliberately HELD** and remain the 31 of the 39: a `court-verifier` audit found **8 of 18 Daytona venue names are wrong** (dedication-name compression — `Kovalenko Gym` is really *Jessie Stevenson Kovalenko Memorial Gymnasium* — and invented "Courts" suffixes, e.g. `White Place Courts` vs the row's own cited source saying *White Place Park*). Places matches on name, so backfilling before the names are settled would cement wrong matches. **Flagged, not fixed: `sun-city-festival-pickleball-complex-az` (published) and `wagner-sports-complex-buckeye-az` (draft) are a probable duplicate venue** — identical street address `27435 W Beardsley Pkwy`, 100 m apart, Places returns Wagner for both, and the Sun City row's *candidate* place_id points at a third location 2,451 m away. Its match was rejected rather than written. Also flagged: two near-misses rejected only by the 500 m radius despite exactly matching names (Happy Trails Resort 633 m, Viewpoint RV & Golf Resort 593 m); **602 draft rows** also lack a place_id; and **every directory loader carries `tags: ['directory']` but there is no `revalidateTag` caller anywhere in the repo** — the purge hook is dead, so a DB-only change waits out the 6 h ISR + `unstable_cache` windows (worst case ~12 h if the two are out of phase). The code fix does **not** wait: a deploy rebuilds `generateStaticParams` and every pre-rendered page ships the new URL immediately, which is why the DB work was sequenced before the deploy.

- **Court directory — venue name corrections + 308 redirects + backfill completed (July 30, 2026) — PR #481 MERGED `a3175eb`, PR #482** — a `court-verifier` name audit across three batches found **Daytona 8 defects in 18, Greensboro 1 in 15, Little Rock 0 in 16**. Two defect shapes: **dedication-name compression** (`Kovalenko Gym` for the *Jessie Stevenson Kovalenko Memorial Gymnasium*, `Babe James Center` for the *Alonzo "Babe" James Community Center*) and **invented "Courts" suffixes** where the row's own cited controlling-entity source says "Park" (`White Place Courts` → *White Place Park*, `Phil Hall Courts` → *Phil Hall Park*). **The naming problem is specific to the Volusia County batch, not the ingest pipeline** — worth remembering before blaming the importer for the next bad name. **8 rows renamed, 7 redirected.** Renaming changes the slug, and a published slug is a live indexed URL, so `next.config.mjs` gained its **first `redirects()` block** (the apex→www redirect is Vercel domain config, not Next) with 7 permanent **308**s. The 8th, `bethune-beach-park` → `mary-mcleod-bethune-beach-park-…`, is `status='draft'` — held by the publish gate on a low-precision coordinate, never reachable, never indexed — so it got the rename and **deliberately no redirect**: a redirect would assert a URL history that does not exist. **THE ORDERING IS THE LOAD-BEARING PART, and it inverts the usual data-before-code rule: the redirects must DEPLOY BEFORE the slug UPDATEs run.** Between an UPDATE and a deploy the old URL is live, indexed and 404s; deploy first and the redirect merely points at a row that has not moved yet, which resolves the instant it does. Because the deploy takes minutes and the UPDATE takes seconds, redirect-first also makes the broken window seconds instead of minutes. Verified in that order against production: all 7 old URLs returned **308** with correct `Location` before any row moved, then `old → 308 → 200` end-to-end after. Totals unchanged at **870 listings / 252 published** with **870 distinct slugs**, proving pure UPDATEs and no collision. Rejected redirect mechanisms: `middleware.ts` (already runs a `supabase.auth.getUser()` round-trip on every matching request — a cost problem the crawl-budget slice flagged) and a `previous_slugs` column (a migration plus a runtime lookup for 7 rows). **Backfill completed across all four remaining metros: published rows missing a `google_place_id` 55 → 14** (55, not 39, because the Little Rock batch published mid-flight carrying 16 more). Reno–Sparks is at **0**; Phoenix 8, Daytona 3, Little Rock 2, Greensboro 1 remain. **Zero place_id collisions site-wide.** Every match hand-reviewed, and the review is where the value was: **Places does not reliably rank the right venue first.** `hayes-taylor-memorial-ymca` matched *"Simkins Indoor Sports Pavilion"* at 414 m while the exact-name *"Hayes-Taylor Memorial YMCA"* sat **3 m away as runner-up 2** — top-hit-only would have written a place_id for a different building. `scripts/backfill-place-ids.mjs` now records misses with their full candidate list plus any `rescuable_candidates` inside the radius, so that evidence is no longer discarded. **Equally important: "closest" is NOT the selection rule.** Three proposals had a nearer runner-up that was deliberately refused — a generic unnamed "Pickleball Courts" POI 46 m from McElroy Park (a generic Maps card is worse for a reader than the correctly-named park, the same reasoning `isGenericName()` encodes in the publish gate), the broader `Bur-Mil Park` at 5 m (the stored coordinate is the park centroid, so the park is nearer *by construction* — the courts are the venue), and an informal duplicate "Zoo City sports complex" listing. **`QUERY_OVERRIDES` was added for two predicted failure shapes** — courts inside a much larger park, and a venue Places indexes under a name we do not store. The instructive case is Kovalenko: **the corrected official dedication name missed by 5,022 m while the colloquial "City Gym" matched at 30 m — a more accurate name can be less findable.** An override only changes the search string; the ≤500 m check still decides, so a bad override misses rather than smuggling a wrong id through. **Flagged, not fixed:** `the-center-at-bishop-park-bryant-ar` rejected on **two** attempts (both the default and the city's own "The Center @ Bishop Park" styling return the broader park — left null, since its address makes `mapsUrl` rung 2 resolve better than a wrong pin); **near-misses with exact name matches just outside the 500 m bar** — `chuck-lennon-park` 512 m, `lake-daniel-park` 697 m (the Greensboro import recorded that coordinate as a greenway centroid, `precision='medium'`, which explains it), Happy Trails 633 m, Viewpoint 593 m — all suggesting a slightly-off stored coordinate rather than a wrong match, and the radius was **not** relaxed to force them through; and **the Sun City knot is three-way, not two** — `sun-city-festival-pickleball-complex-az` (published, no place_id), `wagner-sports-complex-buckeye-az` (draft) and `sun-city-festival-buckeye-az` (draft) all share `27435 W Beardsley Pkwy`, with the two drafts holding place_ids ~2.4 km apart and the published row — the one users actually reach — holding none. Two Little Rock **draft** rows are barred from any future backfill by an explicit `NEVER_BACKFILL` guard in the script: `pickleball-kingdom-little-rock-ar` (stored address and coordinate are ~7.6 km from the real venue) and `tyndall-park-benton-ar` (its own controlling-entity source lists no pickleball; the claim is aggregator-only, which ADR-14 makes insufficient). **No schema change, no migration** — DML on existing nullable columns throughout.

- **Court directory — `public_notes` surfaced on venue pages (August 1, 2026) — PR #492** — `/courts/[slug]` served 817 published venues with **no body prose at all** except the 13 carrying `enrichment.description`. `public_notes` is populated on **530** published rows and was rendered nowhere — `lib/directory/loadFacilities.ts` did not even select it. **435 venues now render prose, up from 13.** **`public_notes` is NOT a clean operator field and must never be rendered raw:** `scripts/lib/workbook-extract.mjs` concatenates enum values it could not map onto the end of the workbook's prose as `' | field: raw'` pairs, so of the 530 — **87** have a segment 0 that is *itself* a `field: raw` pair (`surface: wood/tile`, `fee type: paid reservation | reservation policy: reservation optional / programmed blocks`), **6** leak schema language (`…conflicting directory counts of one and three leave court_count null`), **2** are bare fragments (`Resort`), and **435** are real visitor prose. New **`lib/directory/publicNotes.ts` `visitorNotes()`** keeps segment 0 and drops the machine tail; **`metaDescription()`** truncates on a word boundary. **The machine-pair rule tests the SHAPE — lowercase snake_case label then a colon — not the nine labels live today**, so a workbook producing a column we have never seen still cannot leak a raw value; verified equivalent to the explicit label list across all 530 rows with **zero disagreements**. **`public_notes` beats `enrichment.description` and they are NEVER both rendered:** all 13 description rows are Phoenix, all 13 also carry `public_notes`, and some descriptions **contradict** them (PebbleCreek's calls a resident-only club a `"public facility"`), so showing both would stack a false claim on a true one — the same failure class `courts-honesty.spec.ts` exists to catch. `enrichment.description` survives only as a fallback and today renders on **0** rows. Meta description takes the same source order and moves off `.slice(0, 200)` onto a **155-char word boundary** — description only ever ran on 13 rows, but `public_notes` runs on 435 with a p90 length of 315, so a mid-word cut went from theoretical to routine. **ADR-14 is not in play, and that is measured, not assumed:** all 530 published values carry **0 URLs and 0 aggregator hostnames** (also 0 HTML tags, 0 entities, 0 markdown). **`tests/e2e/courts-venue.spec.ts` is the first rendered coverage this page has ever had** — `courts-crawler.spec.ts` only asserted HTTP 200, and `courts-{directory,honesty}` never leave `/courts/in/[metro]`; both viewports per ADR-09, and **proven as a negative control** (rendering `public_notes` raw fails it at 375 and 1280 on `john-hunt-park-recreation-center-huntsville-al`). **No schema change, no DB writes.** Unit tests 750 → **794**. **Deferred, both real:** the upstream concatenation in `workbook-extract.mjs` (needs a 29-metro artifact regression sweep) and the 6 leaky strings (a data correction, so a gated write); the 29 rows whose kv tail carries real facts (`12 dedicated outdoor courts`) keep them in the database, unrendered.

- **Court directory — geocode cache durability + placement (August 4, 2026)** — a follow-up to PR #501, closing the two exposures its review raised. **Exposure A: a run threw away everything it had bought.** The three geocode passes in `scripts/lib/workbook-extract.mjs` each call `flushCache()` **outside** their loop, with no `try`/`catch` anywhere in the chain, so a throw at venue 24 of 26 unwound past all three flushes and discarded 24 venues' results — each of which may have spent several live requests walking the query ladder at ≥1.1 s apiece. #501 made this the *dominant* loss path rather than a rare one: the give-up-after-5-attempts error it added is a **new** way for those loops to die (net still a large improvement — before it, any single 429 killed a run instantly). **The fix lives entirely in `scripts/lib/geocode-nominatim.mjs`, not at the three call sites**, so all three passes get it structurally rather than by repeating an edit three times — and `workbook-extract.mjs` takes **zero changes**, which matters because PR #499 also modifies that file (see the warning below). Three properties: **(1) auto-flush after every LIVE request** — measured against the real 551-entry / 459 KB cache at 78–816 ms across a whole metro run versus 114–462 s of live Nominatim time for the same run, i.e. **under 0.2 %**, and bounded anyway by the 1.1 s spacing that gates every request; **(2) atomic writes** (sibling temp file + rename, pid-tagged) because `writeFileSync` truncates before it writes and `loadCache` silently treats an unparseable cache as `{}` — a torn write is total loss of that metro's cache, and auto-flushing takes that window from 3 writes per run to ~180, which is reason to close it rather than widen it 60-fold; **(3) `flushCache()` returns a boolean and never throws**, printing the house ACTION REQUIRED block instead (same posture as `revalidate-directory.mjs`), because a failed write must not abort a run that otherwise succeeded — and since the cache stays dirty, a transient Windows `EPERM` on rename self-heals on the next request. **A seed promotion deliberately does NOT auto-flush** — it costs nothing to redo since its seed is still on disk, so flushing one would make a fully-seeded re-run (which spends *zero* live requests) pay for hundreds of writes it gains nothing from. **Exposure B: where the caches live.** The old fallback wrote `.geocode-cache/` at the **repo root**, which is untracked **and was not gitignored** — worse than it sounds, because unignored means a plain `git clean -fd` reached it, not merely `-fdx`, and `git add -A` would have staged ~1 MB of churning research data. `DEFAULT_CACHE` now resolves inside `metro-research/`, and its basename is **`_default.json`, not `nominatim.json`** — the standalone smoke test defaults to that constant, so the obvious name would have made a smoke test rewrite the 551-entry legacy seed the per-metro split declares read-only. `scripts/metros/little-rock.json` was the only config omitting `geocode_cache` (harmless while one file was shared, permanent once the cache split per metro) and now declares it; a unit test asserts **every** config does, which is deliberately a property rather than a count — the corpus is 33 configs on `main` and 43 on the unmerged `chore/courts-publish-pipeline`, so any hardcoded number is wrong on one of the two trees. A new guard makes `flushCache` create **at most the final directory segment**, so a cache can never materialize a real `metro-research/` inside a working tree — that is the exact shape of the 2026-08-03 loss. `/.geocode-cache/` was added to `.gitignore` and is documented in place as **not** protection from `git clean -fdx` (`-x` removes ignored files too — that is what destroyed the cache on 2026-08-03); the real protection is the *location*. **Re-verified on 2026-08-04, not assumed:** `git clean -ndx` in the main checkout does not list `metro-research` at all (git does not follow the junction) and prints `Would skip repository` for all five nested worktrees; the research repo has its own `.git` with no working tree in any ancestor; and the three legacy caches hold **551 + 316 + 259 entries, pairwise overlap 0, union 1,126 unique keys** — ≈21 minutes of the shared courtesy budget to rebuild, before any backoff. **Backup is recommended but NOT done:** the research repo gitignores `.geocode-cache/` as "regenerable," which is why the cache was lost on 2026-08-03 and why the recovered wave-1 entries still sit in no backup. That rationale is stale post-#501 (no file in that directory is written by more than one metro now, and the three legacy files are written by nothing), and the cache is **not purely regenerable**: the winning anchor survives in the artifact, but the runners-up, township-guard rejections and micro-feature skips exist only in the cache, and re-fetching returns *today's* OSM rather than the OSM those rulings were made against. Un-ignoring it lets the existing `backup-metro-research.mjs` carry it with zero new code. Owner-gated and deliberately **not executed here**: it is a commit+push to a second repo (ADR-10 does not cover it), the research repo is currently dirty with another session's `grand-rapids` work, and two geocoding sessions are live. **Test seam:** `geocodeVenue` now accepts `fetchImpl` (defaulting to real `fetch`), mirroring what `fetchWithRetry` already took — the durability property is only observable on a live request and the existing cache tests are deliberately network-free. The ≥1.1 s spacing is **deliberately not injectable**; the tests pay the real wait. Both regression tests were verified to **fail** with the auto-flush removed. Unit tests 903 → **910**; `tsc --noEmit`, `next build` and the full suite all green. **No schema change, no DB writes, no live Nominatim request.** **Flagged, not fixed:** `.claude/worktrees/metro-wave-1/metro-research` is a **real directory, not a junction**, and `git clean -ndx` inside that worktree says `Would remove metro-research/` — its 5 metro folders are byte-identical to the research repo and its cache is md5-identical to `nominatim-wave1.json`, so nothing is at risk today, but a future extract there would write to an unbacked-up island (`bootstrap-worktree.mjs` leaves an existing directory alone). **WARNING — PR #499 (`chore/courts-publish-pipeline`) would REVERT this and #501.** Its tip contains zero occurrences of `fetchWithRetry` or `parseRetryAfter`, and is 300 lines behind main on `geocode-nominatim.mjs`; it must be rebased or merged by hand, never fast-forwarded. **RESOLVED the same day (August 4, 2026): #499 was CLOSED, not merged — superseded by #503 ("Re-cut the metro pipeline work onto main"), which merged. The `chore/courts-publish-pipeline` branch no longer exists; as of the August 6 consolidation `main` is the only remote branch, so the hazard can no longer materialize.**

- **Court directory — three config-expression gaps closed; TOOLING ONLY, no production write yet (August 6, 2026)** — `chore/guard-expression`. Three guards were refusing correct adjudications because the config language had no way to *state* the fact they needed, and each cost a real venue on August 6. **The unifying rule was that every fix ADDS EXPRESSIVENESS and none relaxes a threshold** — `reconcile_radius_m` stays 200, `internal_proximity_m` stays 150, no bypass flag exists anywhere, and `PRESERVE_ON_RECONCILE` is untouched. **(1) `venue_facts.<key>.coordinate` — coordinate adoption.** Nothing in the pipeline could set a coordinate: `venue_facts.fields` throws outside its nine evidence-bearing fields, a workbook coordinate is never a source, and neither coordinate-changing lever can be aimed at an OSM feature carrying no house number on its street and no name the ladder queries. Orlando's AdventHealth complex published a 14-court flagship **407 m away on a street band at precision `low`** wearing an ADR-16 approximate label, while OSM carried the courts as `leisure/pitch way/1165951096 "Central Winds Pickleball Courts"`. **The config names an OSM FEATURE, never a coordinate** — a new `lookupOsmFeature()` resolves it through Nominatim's `/lookup` endpoint and the untouched `classifyPrecision` decides the precision, so an adoption cannot declare a street band `high` and slip past ADR-16. Four guards, all failing closed: a well-formed id resolving to exactly one feature; a **25 m cross-check** re-derived against the adjudicated point; a **1000 m anchor guard** (the same constant and the same Koons Park trap as the same-site name pass, deliberately **not** config-overridable); and an anchor is **required**, so a venue with no coordinate refuses rather than adopting unguarded. `expect_lat/expect_lng` must be authored from the lookup, **not** from `reconciles[].osm_original` — the two are 34.6 m apart for this very feature, which is why a config-stated coordinate can only ever be a cross-check. **(2) `distinct_from_live` — a distinct venue near a PUBLISHED non-OSM neighbour.** Both existing hatches key on the **neighbour's** `osm_id`: `reconciles` matches `r.osm_id === rec.osm_id`, and `also_at_site` looks one up in `ALSO_AT_SITE_BY_OSM` *and* asserts it is still `status='draft'`. A published row sourced from our own research has `osm_id NULL`, so neither can even name it — which is why Tampa's **Bryan Glazer Family JCC** was deferred out of its batch at 148 m from published `vila-brothers-park-tampa-fl`. The new key names one neighbour by `listing_id`, re-reads it live, and requires its slug to still match. **`distinguishers` is a closed vocabulary whose two exactly-comparable members are VERIFIED against live data** (33606 vs 33609; membership vs public) — a false `different_zip` claim aborts. `different_street` stays **declared**, because comparing address strings can only be permissive in the direction of accepting a claim, and a check that can rubber-stamp a false claim is worse than an honest declaration. **(3) the reconcile coordinate guard.** `listingFields` sends `lat: v.coordinates?.lat ?? null` and lat/lng are deliberately absent from `PRESERVE_ON_RECONCILE`, so the incoming coordinate always wins — nulling a good one when the research row failed to geocode, and replacing a court-accurate pin with a street band when it geocoded badly. **The guard was blind by construction as well as by logic: `RECONCILE_TARGET_COLUMNS` never fetched `lat`/`lng`.** Split by failure mode: **NULL-over-good is fatal with no acknowledgement** (it deletes a coordinate *and* leaves a row the gate blocks — strictly worse on both axes); **low-over-present requires a `coordinate_trade`**, because a legitimate case demonstrably exists and Orlando's owner accepted exactly that one. The ack is **re-derived every run**, so a stale one stops being accepted the moment the data moves, and the abort message offers coordinate adoption *first*. The target's own precision is deliberately not consulted — `location_precision` is generated from provenance and a dormant OSM row has `provenance = NULL` (verified across a 514-row live sample, every one NULL). **Retroactive safety was measured, not assumed:** `scripts/sweep-reconcile-coordinates.mjs` (read-only, committed) reports **22 reconciles — 0 destroys, 1 degrades-unacknowledged, and that one is the AdventHealth row the mechanism was built for.** The script states its own blind spot: for an already-imported metro the reconcile has overwritten the evidence, so the check reads clean by construction, and **all 22 are already imported.** **Both acceptance cases proven READ-ONLY.** Orlando: re-extract adopts `way/1165951096` at **`high`**, cross-check **0 m**, 407 m from the superseded anchor, corroborated as the same feature the row reconciles onto, **1 live Nominatim request**; a structural diff against a re-snapshotted baseline moved **exactly 1 venue of 101** plus `precision_distribution` (low 15→14, high 70→71), `verified_facts_applied` and `extraction_notes`; `--stage=project` still asserts **`EXPECTED-SPLIT ASSERTION: MATCHES` at 85/16**, since `low` publishes under ADR-16 and the split must not move. Tampa: `tampa-addendum-2` with the research row **recovered verbatim from metro-research commit `ed7e92e`** (which carried 11 rows against the shipped `47a9bc4`'s 10 — nothing re-researched or re-typed, only `research_key` re-prefixed); `--stage=candidates --dry-run` prints `distinct-from-live ADJUDICATED (148 m)`, both distinguishers verified live, `ALL ASSERTIONS PASS ✓`, `DRY RUN — nothing written`. **Negative-tested:** disabling the entry restores the original abort, so it is load-bearing rather than decorative. **BOTH PRODUCTION WRITES APPLIED AND VERIFIED (second owner approval, same day).** **AdventHealth** was repaired by `scripts/sql/adventhealth-coordinate-repair-2026-08-06.sql` — a keyed single-row UPDATE asserting `ROW_COUNT = 1`, with the **old coordinate pinned in the `WHERE`** so a re-run matches 0 rows and fails loudly instead of double-applying. It ran **dry first**, terminating in a `RAISE EXCEPTION` after all assertions passed, and the rollback was confirmed by **re-reading the row** rather than inferred from the exception. `lat/lng 28.7073709,-81.2708528 → 28.7075117,-81.2750142`, `location_precision low → high`. **`provenance` was MERGED via `jsonb_set` on the `coordinate` key alone** — the highest-risk part of the write, asserted three ways in-transaction: all **25 top-level keys survived**, `osm_reconcile.osm_original` is intact (destroying it would have destroyed the record that made the repair possible), and precision landed `high`. Verified on the **live page**, not the script's self-report: `/courts/adventhealth-…-winter-springs-fl` returns 200 and contains **zero** "approximate" strings, against a **negative control** of 28 on `/courts/in/orlando` proving the probe works. `--stage=publish --dry-run` still asserts **85/16 recomputed from the database**. **`tampa-addendum-2` imported**: candidates → listings → publish → verify, every stage dry-run first, `--stage=verify` **ALL 37 CHECKS PASS ✓**. Site-wide, measured in SQL on both sides because supabase-js silently caps `.select()` at 1000 rows and reported a false `distinct_slugs=1000`: **listings 2364 → 2365, distinct_slugs 2364 → 2365 (equal to total, so insert-not-duplicate), published 1545 → 1546, candidates 2055 → 2056** — exactly +1 each. Live: the venue page 200 and in `sitemap.xml`, the metro page serving 141 distinct venue links with **both** `bryan-glazer-family-jcc-tampa-fl` and `vila-brothers-park-tampa-fl` present and 200 — two venues 148 m apart, both live, which is the entire point of the slice. **The Sportsplex correction was NOT re-applied**: it had already been shipped by an earlier session, and only its `_open_post_publish_correction` marker was left behind. That stale marker cost an investigation and is exactly the drift class this file exists to prevent, so it is now replaced by `_closed_post_publish_corrections` recording both closures. **Close a marker in the same pass that closes the work.** **Two pre-existing findings surfaced and deliberately NOT absorbed:** `scripts/metros/orlando.json`'s `_reconcile_comment` says "Only TWO are reconciled" while `reconciles` holds **four**, so the artifact's `counts` node was a stale copy that self-healed on rebuild (99/2 → 97/4; the importer reads `config.reconciles`, never `doc.counts`); and the Sportsplex correction lives in `tabs.json` but the on-disk artifact predated it, so the control re-extract picked it up — **that is the input having changed, not nondeterminism**, and the split still asserts at 85/16. **Pre-existing gate failure, reported not fixed:** `scripts/lib/__tests__/geocode-cache.test.ts` fails on `main` because `harrisburg-v3.json` and `jacksonville.json` omit `geocode_cache`; unchanged by this branch (the new config declares it). Gates: `tsc` **EXIT=0**, `next build` **EXIT=0**, `vitest` **1,217 passed / 1 pre-existing failure** (1,135 → 1,217, +82 new tests). No migration; `location_precision` is generated, so an adopted coordinate updates the render layer with no DDL.
- **Court directory — eight metros built or rebuilt under v3 source-led in one day; 1,202 → 1,546 published, 48 → 52 live metros (August 6, 2026)** — one consolidated entry rather than eight, because they were built by the same method on the same day and eight near-identical bullets would bury what is actually worth knowing. **Verified in SQL against production:** Tampa-St. Petersburg-Clearwater **140** published / 18 held, Orlando **85** / 16, Jacksonville **66** / 7, Cape Coral-Fort Myers **46** / 1, Lancaster **34** / 2, Harrisburg **32** / 8, El Paso **24** / 0, Colorado Springs **23** / 13, Jackson **22** / 1. Tampa, Orlando, Jacksonville and Cape Coral were greenfield; Lancaster (5), Harrisburg (8), Jackson (6) and Colorado Springs (16) were re-researched from thin existing states. **THE FINDING THAT DROVE THE METHOD CHANGE: a 100%-complete municipal sweep is a FLOOR, NOT A CEILING.** Tampa's first pass swept **38 of 38** aggregating bodies — 34 incorporated municipalities across 4 counties plus 4 county parks departments — and still missed venues, because source-led discovery is structurally blind to classes that **no local body aggregates**. Stage 2b recovered HCC's Tejas centre (8 lit courts, a USTA award) from a raw OSM row that 38 official sources did not carry. A second gated pass (**Stage 2c**, the unaggregated-venue pass: colleges, private and country clubs, HOAs and 55+ communities, RV resorts, JCCs/YMCAs, and local press) then added **+20 and later +10 more** to Tampa — four private country clubs, a JCC, an Encore/Thousand Trails RV resort, two CDD communities, and a park whose six new courts exist **only in local press**. Gated share moved 22% → 36%. **A two-pass rule for Tier 1 metros follows directly**, and is recorded in `general-ai-team/court-verifier/skills/court-verifier/source-led-methodology.md`. **The trigger was a competitor comparison, not a gate failure:** Lancaster listed 5 venues where a local tourism site listed 24 with full details, and the diagnosis was that the gate cost 4 while **discovery cost 14** — coverage, not strictness, was the binding constraint. **Two pipeline expression gaps were found by these builds and closed the same day** (PR #527, own entry below): Orlando's AdventHealth Central Winds published at a street pin 415 m from its courts because no config key could adopt a known-good OSM coordinate, and Tampa's Bryan Glazer Family JCC was **deferred out of its own batch** because a candidate 148 m from a *published* row sourced from our own research (`osm_id NULL`) could be adjudicated by neither escape hatch. Both venues are now correct and live. **Recall across the directory remains UNMEASURED** — the pipeline validates the publish/hold split on every run but never coverage, and measured miss rates in spot audits run 4% (Lexington) to 63% (Harrisburg), with published count not predicting it. Each metro ran in its own worktree on its own branch under the one-writer rule; every publish was verified against the live site rather than the script's self-report, with held slugs confirmed 404 and absent from the sitemap. **All eight configs were stranded on unmerged branches while production already reflected them** — consolidated to `main` the same day, which is why `scripts/metros/` and the database now agree.
- **Court directory — link-health sweep built and run over all 1,202 published rows; the headline is that it CANNOT detect the closure it was built for (August 6, 2026)** — `scripts/lib/link-health.mjs` (pure classifier) + `scripts/link-health-sweep.mjs` (CLI, `--limit` / `--access-type` / `--urls-only`), read-only. Built because `big-house-pickleball-colorado-springs-co` — published, `source_verified`, citing its own site — is a closed business found **by luck**, and 1,202 published venues had no closure-detection mechanism at all. **Probed 1,017 distinct URLs across 671 hosts. Result: 4 rows gone, 94 broken, 125 blocked, 933 healthy** (46 rows carry no URL and were not swept). **THE CONTROL FAILED ON THE FIRST BUILD, AND THE REASON IS THE FINDING: THE CLOSURE NOTICE WAS AN IMAGE.** Big House's page returns HTTP 200 with stale template text — nav, pricing, "Become a Member", "BOOK NOW!", no closure wording anywhere in the body — but the hero graphic had been swapped for a file named **`BHPB%20CLOSED.png`**, referenced five times in `src`/`srcSet`. The first classifier stripped `<img>` tags before matching and so read two thirds of the response. **`extractAssetText` now scans `src`, `srcset`, `data-src`, `poster`, `alt` and `og:image`, percent-decoded and flattened so a filename reads as words** (`BHPB%20CLOSED.png` → `BHPB CLOSED png`), and the whole URL is scanned rather than the basename because the marker sits **mid-path behind CDN transforms** (`…/BHPB%20CLOSED.png/:/cr=t:3.95%25,…`). It reuses the existing weighting rather than adding a parallel path: own domain → bucket 1, government or multi-row host → bucket 2. **A bare `closed` token is permitted in a filename and nowhere else** — prose gets the narrow phrase list because a body offers a hundred innocent ways to say "closed", whereas a filename is a compressed deliberate act ("somebody made a sign and named the file") — and it carries **`medium` confidence, not `high`**, for the reason below. **MEASURED: image scanning added exactly ONE true positive across 1,017 URLs, and its first version produced TWO FALSE POSITIVES for that one true positive** — `ormondbeach.org` serving a *"City Hall Closed Notice"* graphic and `akron-pa.com` serving a *"Road Closed"* graphic, both the closure of something that is **not the venue**, on town sites that the `.gov` test misses because they are `.org`/`.com`. Both are now negated by subject (`hall|office|lobby|clubhouse|restroom|library|…`, `road|street|bridge|detour|parking|…`) and both are pinned as regression fixtures. **That ratio is the useful measurement: closures are barely detectable from a venue's own site at all.** `BHPB CLOSED.png` is a descriptively-named file and plenty of operators would have called it `hero-2.png`; nothing else in the corpus announced a closure in any readable form. Text-borne closure matching fired **once in 1,017 URLs and it too was a false positive** (`scarboroughmaine.org` listing a *"Going Out of Business Permit"* among town permits, correctly demoted by position weighting), and parked-hosting matching fired **zero** times. **So the tool now reads the whole response and still finds essentially nothing: 3 dead domains plus 1 image. It detects domain death and, rarely, a self-announced closure — never a quiet one.** A closed business's website does not go down; it becomes a zombie. **The three dead domains, all NXDOMAIN on two attempts and independently confirmed against Google Public DNS at both apex and www:** `fig-garden-swim-and-racquet-club-fresno-ca` (Fresno, membership), `the-salvation-army-kroc-center-of-augusta-augusta-ga` (Augusta, membership), `foreside-fitness-and-tennis-falmouth-me` (Portland ME — **stored as `access_type='public'` though it is plainly a commercial fitness/tennis club; flagged, not changed**). **0 of the 4 bucket-1 rows carry a `google_place_id`** — Big House included — so **Places `business_status` cannot confirm any of them without a paid lookup first**; only **283 of 1,202** published rows have a place_id at all (23.5%). That materially weakens "confirm via Places" as a cheap next step, though the image finding **strengthens the case for Places overall**: if reading a venue's entire site end to end surfaces one closure in 1,202 rows, the authoritative field is the only instrument that scales. **3 of the 4 have `website` byte-identical to `name_source_url`**, Big House included — the same shape as the 151-of-207 finding in the wave-3 audit — so **one dead link takes out both of a row's "sources" simultaneously**, and a row that looks doubly-sourced is usually one URL printed twice. **403 IS NOT A CLOSURE SIGNAL AND THE CORPUS PROVES IT: 124 URLs across 70 hosts returned 403/402/timeout, overwhelmingly municipal `.gov`** (`www.scgov.net` 13, `www.bernco.gov` 6, `www.peoriaaz.gov` 5, `www.tempe.gov` 5, plus `www.townofcolonie.gov`, previously recorded 402-ing WebFetch while serving a browser 200). A status-only sweep would have reported all 124 as dead. They classify **`blocked` with confidence `none`** and are listed explicitly so nobody re-checks them by hand. **The dominant bucket-2 signal is also benign: of 45 unrelated redirects, 24 are same-org renames and most of the rest are municipal `.org`/`.us` → `.gov` migrations** (`slco.org`→`saltlakecounty.gov` 10, `codb.us`→`daytonabeach.gov`, `cityofbryant.com`→`bryantar.gov`, `eliotmaine.org`→`eliotme.gov`) — stale URLs, live venues. **`healthy` means NOT PROVEN BROKEN, never "confirmed alive"**: soft-404 detection is deliberately absent (leegov.com returns 200 with an empty `<title>` for pages that do not exist), so a soft-404 lands in healthy — a false negative, which is the safe direction. **Read-only is a machine check, not a claim**: `scripts/lib/__tests__/link-health-fence.test.ts` reads both source files and asserts no mutating supabase-js verb, no Places endpoint, no Nominatim endpoint, and no `facility_listings.status` write; it also pins the `.range()` + short-page + unique-sort pagination that the 1,000-row cap requires (published is 1,202, so an unpaged read would have silently swept 1,000). **Nothing was un-published — retiring a row is a per-venue owner decision and this output is the evidence for one.** ADR-14: aggregator hosts are skipped; **0 published rows currently carry one**, so the filter guards the future. Gates: tsc **EXIT=0**, `npm run test:unit` **86 files / 1,136 tests** (+2 files / +122 tests, all new), `npx next build` **EXIT=0**. **Recommended next step is still NOT a broader link sweep — but that conclusion is now drawn after the instrument reads the WHOLE response, not two thirds of it.** Image scanning was the one real gap and closing it yielded exactly one row. Closure detection needs a different instrument: Places `business_status`, which first needs place_id coverage above 23.5%.
- **User-submitted venues — the submission path was BROKEN IN PRODUCTION for 13 days, and the fix shipped separately (August 6, 2026, PR #528)** — `POST /api/locations` failed on **every** submission from 2026-07-24 until this fix. Migration `20260724000001` ran `alter table public.locations alter column metro_area drop default` on a column that is `NOT NULL`; the route never set it and no trigger fills it, so every insert died with `23502`. **Proven, not inferred**, by a rollback-wrapped probe against production — `BEFORE: 23502 null value in column "metro_area"…` / `AFTER: INSERT SUCCEEDED` — with 0 probe rows left and `locations` still 64 afterwards. Corroborating: **1** row with `created_by` set and **0** at `status='pending'`, i.e. nothing reached the `/admin/locations` queue in that window. **The route's own header comment asserted `metro_area` "uses table defaults"**, which had been false since the day the default was dropped — a stale comment describing a column that had changed underneath it. Files the literal **`'Unknown'`, not the submitted city**: ADR-19 scopes the directory to a defined list of 111 MSAs so mapping a city to a metro is inference, and concretely `scripts/gen-vegas-parity.mjs:50` selects locations with `.eq('metro_area', 'Las Vegas')` — a submission naming a covered city would be swept into a curated parity run it was never reviewed for. **No app code reads `locations.metro_area` at all** (verified across `**/*.{ts,tsx}` **and** `scripts/**/*.mjs`; every metro read in the directory is `facility_listings.metro_area`), so the value satisfies a constraint rather than labelling anything. Shipped as its own PR ahead of the feature work because it is live and independent. Gates: tsc **EXIT=0**, `npx next build` **EXIT=0**, `npm run test:unit` **86 files / 1,136 tests**.
- **User-submitted venues — rich optional detail, written to `facility_listings` (August 6, 2026)** — a submitted venue now creates the **canonical** `facility_listings` draft alongside the operational `locations` row, bridged by `facility_listing_id`. **This pulls ADR-13's Phase 3B write-path cutover forward for this one flow**; nothing else in the app changes and no deprecated `locations` column (`notes`/`phone`/`source_url`/`category`) is written. **No migration — zero new columns.** All 19 optional fields already existed with live CHECK constraints; the audit confirmed the expected conclusion rather than discovering a gap. **Required stays two interactions** (name + address); the optional half sits behind a collapsed disclosure. **THE INVARIANT THAT MATTERS IS `verified_by` STAYING NULL.** `scripts/publish-facilities.mjs` is a *reconciling* gate that publishes eligible drafts on its own, and `passesReleaseFence` is literally `row.verified_by != null` — a user submission otherwise passes the ADR-17 gate (name, coordinate, city, slug), so that NULL is the only thing between a crowd-sourced row and `/courts`. `metro_area` is left NULL as a second layer, since publish runs are `--metro=`-scoped. A unit test asserts both on the written row. `verification_status='unverified'` per ADR-18 — never `source_verified`, `listed` or `human_verified`. **Two semantic rules drove most of the design and both are easy to invert.** (1) A skipped field writes **NULL** ("not yet researched"), never `'unknown'` ("researched and undetermined"), which is rejected server-side even though every CHECK permits it. **One deliberate exception: `access_type`** is nullable but carries a non-NULL default and has **0 NULL rows in 2,364**, so a skipped value is *omitted* and the table default applies rather than inventing a state the column has never held. (2) **Booleans are tri-state selects, not checkboxes** — an unchecked box submits `false`, asserting "this venue has no restrooms", a claim the user never made. `surface` offers a deliberate **6-value subset** of its 16-value CHECK; the rest are raw OSM ingest tags (`hard_court`, `tartan`, `ice`) that are not answers to ask a person for. **ADR-12 was the live tension and the owner ruled on it**: a Places `formatted_address` is not a storable source, so the autocomplete pre-fills **editable** fields the user reviews and submits, stored as `address_source='organizer'` (already in use on 12 rows). **The fields being genuinely editable is what makes that provenance true** — turning them read-only would make the column lie, and both the component and the details route say so in their headers. **The coordinate is deliberately NOT taken from Places** (same ToS class as the address) but from the existing server-side Geocoding call on the confirmed address — provenance identical to what the route always did, and more accurate now that the address is validated. Only `google_place_id` is persisted from Places. **Places runs through a server proxy** (`/api/places/autocomplete`, `/api/places/details`), auth-gated, because `GOOGLE_MAPS_API_KEY` is server-only and unrestricted; publishing it as `NEXT_PUBLIC_*` would hand it to anyone. Spend is bounded by **session tokens** (one per venue, sent with every keystroke *and* the details call, so Google bills one session), a 250 ms debounce, a 3-character minimum, a server-side refusal of any request without a token, and a **per-user daily cap** — in-memory, so the true ceiling is (cap × serverless instances); making it global needs shared state and is a deliberate follow-up, stated in the file rather than implied. **No pricing figure appears anywhere in the code or the PR** — the SKUs are *Autocomplete Requests*, *Autocomplete Session Usage* and the *Place Details* tiers, selected by field mask, and current rates need checking against Google's pricing page. **`slugify`/`directorySlug` are mirrored into `lib/directory/slug.ts`** following the `locationPrecision.ts` cross-boundary pattern, because the originals live in `scripts/lib/workbook-extract.mjs` (plain ESM, outside tsconfig). The parity test **imports the `.mjs` directly** and asserts agreement over a corpus rather than hand-written expectations — the fixture fallback was not needed. Collisions walk `base` → `base-2` … `base-60` matching the importer's ladder, then a random tail; the check-then-insert TOCTOU window is closed by retrying once on `23505`, so a submission never fails because someone else submitted simultaneously. **`/admin/locations` now shows the submitted detail** (omitting unanswered fields entirely, so "they said no" stays distinguishable from "they didn't say") and checks duplicates against **`facility_listings`** as well as the 64-row `locations` table — a user could previously duplicate any of 2,300+ directory rows undetected. That pool read is state-scoped and **count-aware**, reporting truncation rather than silently checking against a partial directory. Approving a venue does **not** publish it; there is no publish button. **Six forms share `NewLocationFields`** (league/tournament/event × create/edit), so this reaches all of them — and a regression would too. Gates: tsc **EXIT=0**, `npx next build` **EXIT=0**, `npm run test:unit` **91 files / 1,288 tests** (+5 files / +152 tests). **Deferred, flagged not fixed:** no email/notification on submission; no standalone `/courts/submit` page; the ADR-12 user-submission carve-out is **not** written (owner chose to record the decision in the PR rather than amend the ADR).
- **Court directory — all three dead-domain rows were LIVE VENUES THAT MOVED HOSTS; they were repointed, not retired (August 6, 2026)** — `scripts/repoint-dead-domains-2026-08-06.mjs`, six keyed single-row UPDATEs (3 `facility_listings` + 3 `facility_candidates`), each asserting exactly 1 affected row, `--dry-run` previewed first. **The slice that produced this was scoped to un-publish all three, and investigation reversed it before any write.** The NXDOMAIN result was re-verified and holds — apex and `www`, on Google Public DNS **and** Cloudflare, six lookups, all NXDOMAIN — but **every one of the three venues is open at a new URL**: `figgardenswim.com` → **`fig-garden.com`**, `www.krocaugusta.org` → **`augustakroc.org`**, `foresidefitnesstennis.com` → **`foresidefit.com`** (an actual rebrand: "Foreside Fitness & Tennis" now trades as "Foreside Fit", which is *why* the domain died). **Each replacement was identity-confirmed by finding the row's OWN stored street address on the live page** — "4722 North Maroa" / "1833 Broad Street" / "196 U.S. Route One" — not by name similarity, which is what separates a trustworthy replacement from a plausible guess. **A DEAD DOMAIN IS NOT PROOF A VENUE IS CLOSED; it is proof we can no longer cite it** — and on this corpus it was wrong about closure **3 times out of 3**. That reframes the sweep as a **stale-URL detector rather than a closure detector**, and it settles the Places question: an authoritative `business_status` is not a refinement, it is the only instrument that tells "moved" from "closed". **The Kroc citation was deliberately split**: `website` → `augustakroc.org/` (user-facing), but `name_source_url` and the candidate's source URLs → `augustakroc.org/krocaugusta/sports-rec`, the page that actually evidences the pickleball claim (leagues, drop-in, lessons) which the home page never mentions — so the repoint upgraded the row's weakest link. **`foreside-fitness-and-tennis-falmouth-me`'s `access_type` was corrected `public` → `membership`** on the live site's own evidence (membership sold, no drop-in path); it had been flagged in the sweep and is now fixed with a citable source. **Its stored `public` came from the ADR-13 enum mapping `commercial -> public`** (`scripts/lib/workbook-extract.mjs`), whose premise is walk-in-and-pay — false for this venue. **The mapping is untouched (changing it is an ADR-13 deviation and an owner call), and 43 other rows share it — 27 published, 16 draft — none checked against its venue's own site.** That audit is an open follow-up, not part of this change. Every row **stays `published`**, every candidate stays `research_status='published'`; no status, slug, coordinate, `verified_by` or `verified_at` was written. Full before/after, the DNS evidence and the reasoning are recorded per row in `provenance.dead_domain_repoint_2026_08_06`. **Production verified: Fresno 17 / Augusta 15 / Portland 41 unchanged, all three slugs 200 and present in `sitemap.xml`, site total 1,201.** **The `directory` tag bust also reached `/courts/[slug]`** — Foreside's page rendered `Membership` (and zero `Public`) within a minute, which answers an open question from the Lancaster follow-up in the affirmative. Note that **neither `website` nor `name_source_url` is rendered on `/courts/[slug]` at all**, so the only user-visible change here was the access label. Gates: tsc **EXIT=0**, `npm run test:unit` **86 files / 1,136 tests**, `npx next build` **EXIT=0**.
- **Court directory — wave-3 dividend released across 14 metros, 1,123 → 1,202 published (August 6, 2026)** — the ADR-16/17 backlog released in **one owner approval over one audit** instead of a per-metro gate, after an audit found the batches were being drip-released four turns at a time. The audit measured **207 draft rows across 33 metros** that pass the ADR-17 gate and are held only by conditions ADR-16/17 retired — **not** the 221/36 in the brief, which predated that day's Jackson and Harrisburg releases. **The finding that changed the method was that "flag any row resting on a single source" is not a usable filter here: it flags 95% of the population.** Of the 207, **24 have no evidence at all**, **151 have a `website` byte-identical to their `name_source_url`**, 22 have a source URL and nothing else, and **only 10 carry two genuinely distinct sources**. A row showing both a source and a website reads as corroborated in a spreadsheet and is usually one link printed twice, so the discriminating question is *what kind* of single source, not how many. Rows were bucketed by that: controlling entity / first-party / aggregator / none. **Released: 14 metros, +79 rows** — Portland ME **22→41**, Spokane 16→24, Buffalo 15→22, Lexington 45→51, Killeen-Temple 9→15, Salt Lake City 27→33, Fresno 11→17, Akron 18→23, Huntsville 19→23, Augusta 11→15, Syracuse 12→15, Memphis 16→18, Springfield MO 35→37, Melbourne 12→13. **Every one of the 14 projections matched the audit's independent DB-side prediction exactly** (artifact publish count − rows already live = predicted gain), which is the check that made a 14-metro batch safe to run as one unit; `--stage=verify` returned **ALL CHECKS PASS on all 14 (602 checks total)**, and `EXPECTED-SPLIT ASSERTION: MATCHES ✓` at both `project` and `publish` for each. **Held entirely (74 rows, 6 metros) on evidence quality, not gate mechanics:** **Provo** (24 — **16 of them share one URL, `utahvalley.com/blog/post/pickleball/`, a tourism-board blog post written into both `name_source_url` and `website`, so each venue page would have cited a listicle as its official site**; one more cites the bare `openstreetmap.org` homepage), **Las Vegas** (23 — the `vegas-parity-2026-07` rows carry **no source URL, no website, no `provenance.method` and no `coordinate` node at all**, so **ADR-12 coordinate provenance cannot be demonstrated for any of them**, and 9 already claimed `source_verified` against zero recorded evidence), **Colorado Springs** (10 — already owner-deferred to v3 re-research; 6 aggregator-only, five sharing one `pppall.net/Outdoor` page, and `wasson-high-school` is the closest Bishop Park analogue on the site: sourced to an aggregator while its own coordinate anchors *"Roy J. Wasson Academic Campus"*, a name that no longer designates a high school), **Madison** and **Modesto** (zero controlling-entity sources among their would-release rows) and **Poughkeepsie**. **10 individual rows held by name**, each with a releasable condition recorded in `facility_candidates.reviewer_notes` and `research_status='held'` set in both the artifact and the database so `project` and `publish` agree — the two stages read research status from different sources, so a hold set in only one place makes the expectation diverge. **Two of the ten are wrong-entity pins rather than imprecise ones, which is the failure mode worth recognizing on sight:** `university-of-toledo-main-campus-pickleball-courts-toledo-oh` is anchored to `amenity/fast_food node/6048531855 "Subway"` (**this was Toledo's only dividend row, so the metro nets to zero and was skipped rather than run as a no-op**), and `fern-bell-recreation-center-huntsville-al` sits on the `"Whitesburg P-8"` school relation. A low-precision pin publishes behind the ADR-16 label; a pin on a *differently named entity* is a defect the label does not describe. **`beach-street-pickleball-courts-saco-me` shipped as a judgment call** — it anchors the Saco School Department office, but at `low` precision it renders behind the approximate-location banner; noted here so it is findable if anyone complains. **The ADR-18 re-tier had to precede every publish** (`--stage=publish` never writes `verification_status`, so publishing first fails `verify` on rows it just promoted): **53 draft rows across 12 batches** moved `source_verified` → `listed`, all drafts, **no live row's tier changed**. **The brief's tier-debt figure was also stale — it is 122 rows across 20 batches, not 111, and 19 are already published**; all 19 are legacy `stockton-lodi-2026-07` rows that predate the stamping convention, left alone as their own gated slice. **12 of the 26 approved metros could not run, and neither reason is a gate question.** **Eight abort at pre-flight on the ADR-14 document-URL assertion** — a `website` column pointing at a PDF, agenda, minutes or CivicAlerts archive (chattanooga 1, durham 1, fayetteville 4, new-haven 3, ogden 1, port-st-lucie 1, wichita 1, winston-salem 1). That check is deliberately **not** would-publish-scoped, so ADR-17 pulled more rows into it; **the remedy the assertion itself prescribes** is to move the document URL to `name_source_url` and null the `website`, which is a content pass over research artifacts and was left for its own slice. **`--stage=publish` does not run pre-flight**, so these eight *could* have been forced through — deliberately not done, since that routes around the assertion and ships document URLs on user-facing rows. **Four more have no metro config anywhere** (Phoenix `az-review-2026-07`, Reno-Sparks, Daytona Beach, Greensboro-High Point — the bespoke-script batches, whose research artifacts no longer exist), so the generic pipeline cannot reach them and hand-stamping `verified_by` is forbidden. **`scripts/metros/_vt_pme.json` is a stale duplicate of `portland-me.json` carrying the same `batch` tag and NO `envelope`** — running `--metro=_vt_pme` would skip the envelope guard entirely; `portland-me.json` is authoritative. Four configs (buffalo, fresno, memphis, salt-lake-city) were imported as exact blobs from `chore/batch3-metros`; their stale `expected_publish` blocks were re-authored with a `_expected_publish_history` key naming ADR-16 and ADR-17 for every divergence. Gates: tsc **EXIT=0**, `npm run test:unit` **84 files / 1,014 tests**, `npx next build` **EXIT=0** (slice adds no tests; the count moved with other merged work). Verified against **production, not script output**: the 14 metro pages render exactly the expected counts (Portland **41**, Buffalo **22**, Spokane **24**, Killeen-Temple **15**), the `/courts` hub shows **48** metros, `sitemap.xml` carries **exactly 1,202** facility URLs — matching the database, so the `.range()` paging is holding well past the 1,000 cap — and **all held slugs 404 and are absent from the sitemap**. Publish re-stamped `verified_at` on the 268 already-live rows in these batches; accepted and reported, not hidden.
- **Court directory — Boise, Albuquerque and North Port–Sarasota released, 965 → 1,021 published, and the 1,000-row truncation that release exposed (August 5, 2026)** — three metros that had **never been imported**, released as full four-stage imports: **Boise 11/13** (metro 43), **Albuquerque 23/24** (metro 44), **North Port–Bradenton–Sarasota 22/24** (metro 45). `--stage=verify` **39 / 38 / 39 CHECKS PASS**, `EXPECTED-SPLIT ASSERTION: MATCHES ✓` at both `project` and `publish` on all three, all 56 rows stamped `verified_by`, all 61 imported rows tier `source_verified`, zero `probable`. 7 rows carry the ADR-16 approximate-location label (4 Albuquerque, 3 North Port). **The arithmetic in the release brief was wrong and the correction is the point:** the approved figure was "+10 → 975", which was the gain over the metros' stale 2026-08-03 `expected_publish` blocks, not over production. Those blocks were locked from an approved projection and the import never ran, so all three contributed **zero** live rows — confirmed by querying both the batch tags and the `metro_area` values before writing. Each metro contributes its **full** count: **+56, and 42 → 45 metros**. **The stale expectations were stale on two independent axes, and each config now records which one moved it:** Albuquerque's +4 is entirely ADR-16 (four rows held on `coordinate precision low`), Boise's +1 is entirely a **re-geocode** (the Flying Pickle Meridian recovered — ADR-16/17 moved nothing there, since every Boise hold was `no coordinate`, which neither ADR touches), and North Port's +5 splits three ADR-16 / two re-geocode. **Two `_geocode_comment` blocks were corrected as part of the same commit** — Boise's claimed 3 coordinate-less rows against an actual 2, and North Port's claimed 4 while blaming "a probable workbook address error" for two of them; **both addresses were correct and both now resolve `high`**, so the note was libelling the data for a limitation of the ladder on the day it ran. **Publishing these three took the site past 1,000 published rows, which silently broke the directory.** PostgREST caps an unbounded select at **1000 rows and returns no error**, and all three site-wide loaders in `lib/directory/loadFacilities.ts` were unbounded: `/sitemap.xml` served **exactly 1000** facility URLs against 1,084 published (**84 venues invisible to search engines**), and `loadPublishedMetros` aggregated over that same truncated window, so **Boise and El Paso vanished from the metro list and their `/courts/in/<slug>` pages hard-404'd while their rows sat published**. El Paso's had been 404ing since the earlier release that day; nobody noticed, because a truncated read looks exactly like a complete one. **The publish scripts had carried a `>= 1000` guard since the ADR-17 work — the app read path never got one**, which is why this stayed invisible until the corpus outgrew the cap. `fetchAllRows()` now pages with `.range()` until a page comes back **short**; a full page means "there may be more", which is precisely the rule the old code got wrong. Applied to the facilities, metros and slugs loaders; **the per-metro loader and the `metro_area IS NULL` loader are deliberately left alone**, both far under the cap. Two loudness changes, because a silent undercount is how this hid: every caller used to destructure `{ data }` and **drop `error`**, so a failed read rendered an empty directory with no signal anywhere — it now throws; and a runaway cursor throws at 50 pages rather than looping. **Pagination needs a total order** or pages overlap and skip, so every paged query now ends its ordering with `slug` (unique, not null) — the metros and slugs reads previously had **no `ORDER BY` at all**. The spec asserts on the **`.range()` windows actually requested**, not on row counts, because the break was never in the shape of the data: it was that one request was issued where two were needed. It covers the **exactly-1000 boundary** that hid the bug and reproduces the Boise/El Paso metro loss directly, and was verified as a **negative control — all 8 fail against the pre-fix loader**. Gates: tsc **EXIT=0**, `npm run test:unit` **83 files / 1,002 tests**, `npx next build` **EXIT=0**. Verified against **production, not script output**: all three metro pages 200 with the right counts, Boise and El Paso restored, the sitemap carrying all 1,084 facility URLs and all 48 metro URLs, and **all five still-held slugs 404 and absent from the sitemap**. **Truncation window: ~17 minutes**, and the exact crossing is worth knowing because it was a near-miss. Publish timestamps (`verified_at`, UTC): Boise **19:41:45** → 976, Albuquerque **19:42:29** → **999 — one row short of the cap**, North Port **19:43:05** → **1,021, which is the write that crossed it**. The concurrent Albany/Worcester/Bakersfield run then landed 19:43:31–19:44:55 → 1,084. The fix deployed ~19:59. So the site served a truncated directory from **19:43:05 to ~19:59 UTC**, and had Albuquerque been the last metro of the day the bug would have stayed latent at 999 with nobody the wiser. **An earlier draft of this entry guessed "roughly 90 minutes, from the Albuquerque publish"; both halves were wrong and were corrected from `verified_at` rather than left as an estimate.** **Two false alarms retired, both the same shape:** `dill-dinkers-sarasota` (4,403 m) and `lakewood-ranch-park` (2,471 m) both tripped the locus/township distance guard against **one segment of a multi-segment road**, with the venue lying between segments — Lakewood Ranch was confirmed by hand against mymanatee.org (same 5350 Lakewood Ranch Blvd, six courts; OSM carries the "Community" name variant, the county does not). **A large guard distance on a long road is a road-geometry artifact, not a mislocation** — check the cache for a nearer segment before treating it as a defect. **Deferred, not fixed:** Cape Coral (27/27 projected, but aborts pre-flight on 6 content blockers — 5 rows sharing one Lee County program-guide PDF as `website`, plus an aggregator `name_source_url` that ADR-17 newly pulled into scope) and New Orleans (its entire +1 is `east-bank-bridge-park-destrehan-la`, whose only match came from dropping the city and landed in **Good Hope/Norco**, not its own Destrehan, on an OSM way that is a **road carrying an interpolated house number** yet scored `high`).
- **Court directory — Lancaster PA published, 5 → 32 → 34 (August 5, 2026)** — the metro's first Current State entry: **PR #511 shipped the v3 import without one**, so this file did not record that Lancaster existed at all while production served 32 venues. Two batches, deliberately separate, same `metro_area` and therefore one public URL `/courts/in/lancaster`. **`lancaster-2026-07-31`** (9 rows) published 5 on July 31. **`lancaster-v3-2026-08-05`** (27 rows) is the **first metro through the coverage-first / source-led methodology** — no workbook and no Google Sheet, the tab grid authored directly in the shape `workbook-extract.mjs --raw` already reads, with `workbook.source_description` saying so instead of the artifact claiming a Sheet it never had. Source-led beat the old venue-led pass **35 leads to 9**, but it is **NOT a superset** — it missed Denver Recreation Center, which venue-led found; the standing fix is to union new research against rows already in the database before calling a metro's discovery complete. Its `expected_publish` was **owner-locked at 27/0 after the projection existed, never before**. Seven researched venues were held out by `exclude` rather than imported, because a live row already represented them — six would have aborted preflight on a slug collision anyway, but `d-f-buchmiller-park` would **not** (the live row is `d-f-buchmiller-county-park…`, one word apart, different slug), and only the 200 m live-proximity guard stood between it and a silent duplicate. **Three follow-ups then closed the batch out on August 5** (`chore/lancaster-followups`): **(1) the Overlook chimera retired.** `overlook-pickleball-courts-activities-center-lancaster-pa` merged Overlook Park and Overlook Activities Center into one listing, which never geocoded and never published; both are now live as separate correct v3 rows. Its **candidate** moved to `research_status='held'` — nothing deleted, the listing and candidate stay as the audit record of what the July 31 pass believed. `held` is in `BLOCKING_RESEARCH_STATUS`, so it is now blocked by *both* publish scripts on a **human decision** rather than only on a missing coordinate, which is the distinction ADR-17 draws when it keeps `held` blocking while releasing `probable`. **(2) the ADR-16 dividend, 5 → 7 on the old batch.** `hempfield-reccenter-landisville-pa` and `lititz-reccenter-lititz-pa` were held solely on a low-precision coordinate; both now publish behind the approximate-location label. **That batch had run its entire July 31 import with no `expected_publish` block, so its split was never machine-checked** — one was authored here **from the 7/9 projection the config's own `--stage=project` printed**, not from a guess, and now asserts at both `project` and `publish`. **(3) Bright Side's address.** `bright-side-opportunities-center-lancaster-pa` was published with `address = NULL`; the v3 pass found `515 Hershey Ave` on the venue's own site — the one genuine improvement that research offered an existing row. Applied as a keyed single-row UPDATE with `address_source='official_page'` + `address_verified_at` per ADR-12, `provenance.fields.address`, and a `lancaster_v3_address_backfill` node recording the prior values. **Coordinate and slug deliberately untouched**: the live pin's own anchor already resolves house number 515 and the v3 geocode of that address lands ~9 m away, so the address was corroborated twice and needed no re-geocode; the URL is indexed. The exact statements live in `scripts/lancaster-followups-2026-08-05.mjs`, **committed because neither change touches a tracked file otherwise** — run-and-discard would leave production writes recorded nowhere in git. **A cross-batch duplicate check was done BY HAND and is not optional**: the 200 m live-proximity guard runs only at `--stage=candidates`, which a publish-only run skips entirely, so nothing mechanical would have caught a near-duplicate. Nearest live neighbours: Lititz recCenter → Moravian Manor **555 m**, Hempfield recCenter → Amos Herr Park **2,055 m**. Verified against **production, not script output**: `/courts/in/lancaster` serves **34 facilities**, the `/courts` hub card reads 34, **12** approximate-location strings (6 low-precision rows × the two-branch mobile/desktop render), both new slugs 200 and in `sitemap.xml`, both held slugs **404 and absent from the sitemap**, Bright Side's venue page rendering the new address. `--stage=verify` **ALL 39 CHECKS PASS**; `next build` **995 → 997 pages**, exactly +2. The 27 v3 rows ended the session provably untouched (`updated_at` unchanged at their own publish timestamp). **Accepted side effect, not a defect:** `--stage=publish` re-stamps `verified_at` on every gate-passing row including the 5 already live, so all 7 now read the August 5 timestamp rather than August 1 — the stage is idempotent by design, and a hand-written UPDATE on just the 2 new rows would have bypassed both the gate and the `expected_publish` assertion. **Flagged, not fixed:** `/api/revalidate-directory` busts the `directory` tag and `revalidatePath('/courts')` but names **no** `/courts/[slug]` path, even though that route is ISR with its own 6h clock — any future venue-page field edit could serve stale HTML. It did **not** bite here (the Bright Side page served the new address within a minute), so this is an unverified asymmetry rather than a confirmed gap, and it deserves a deliberate look rather than a reactive patch. Also still open: **Brubaker Park stays excluded** — OSM has neither the lane nor the park in Denver PA, and the ladder produces a coordinate ~90 km outside the county rather than none; re-admitting it needs a confirmed house number or zip, which is research, not a code change.
- **Court directory — three metros released, 937 → 965 published (August 5, 2026)** — the first release run under the ADR-16/17 gate change, and the release the change was made for. **Little Rock 16 → 19**, **Pensacola 11 → 12**, and **El Paso 0 → 24** (a full four-stage import of a greenfield metro, taking the site to **42 live metros**). Site-wide: listings **1803 → 1827**, published **937 → 965**, candidates **1483 → 1507**, distinct slugs = total on both sides. Every newly-published row was released by ADR-16 (a `low`-precision coordinate publishes behind an approximate-location label) or ADR-17 (`research_status='probable'` is no longer a gate condition) — 4 rows by the first, 24 by the second, nothing by a change in the data. **A prerequisite had to run first, and skipping it would have failed the verify stage on rows the publish run had just promoted.** ADR-18 derives the confidence tier from the candidate's research status, and `--stage=verify` asserts `verification_status === verificationStatusFor(provenance.research_status_at_import)` over **every** row in a batch — but `--stage=publish` writes only `status`/`verified_at`/`verified_by` and never touches the tier, so publishing first cannot fix a mismatch. **12 pre-ADR-18 rows** (little-rock 2, colorado-springs 10) carried `source_verified` on a `probable` candidate and were re-tiered to `listed` first, via `scripts/retier-adr18-debt.mjs` — **committed, because the correction touches no other tracked file** and a run-and-discard would leave the production write recorded nowhere. Scope was the two named batches only: 141 rows site-wide match a loose reading of that predicate and 248 more carry no `research_status_at_import` key at all, and sweeping those is a separate gated pass. The script asserts the exact expected count per batch before writing, guards each UPDATE on every column defining its precondition with `returning` asserted at exactly 1, and records priors in an `adr18_retier_2026_08_05` provenance node merged into the existing object. **Not user-visible:** `lib/directory/loadFacilities.ts` selects `verification_status` in none of its four queries. **All three metros' `expected_publish` blocks were stale and were re-authored from the projection, never toward it** — each divergence `--stage=project` reported was first accounted for by a named ADR (little-rock's 3 holds, pensacola's 1, el-paso's 11), and the reasoning is recorded in an `_expected_publish_history` key in each config so the rewrite is auditable rather than silent. El Paso's was re-authored **before** its import ran, 13/24 → 24/24. **The cross-batch duplicate check was again done by hand and again was not optional**: the 200 m live-proximity guard runs only at `--stage=candidates`, which a publish-only run skips, so nothing mechanical guards little-rock's or pensacola's new rows — SQL haversine put the nearest live neighbour at **1,656 m** (Highpoint Park → Shoreline Park North), and El Paso's closest internal pair at **492 m** against its 150 m guard. **Verified against production, not script output:** `/courts/in/little-rock` 19, `/courts/in/pensacola` 12, `/courts/in/el-paso` 24, all four hub cards present, every new slug in `sitemap.xml`, **all 7 still-held slugs 404 and absent from the sitemap**, and the ADR-16 label rendering on the two low-precision rows checked with a **negative control** (a high-precision row carries none). `--stage=verify`: pensacola **41/41**, el-paso **37/37**. `next build` **997 → 1025 pages**, exactly +28. Gates: tsc **EXIT=0**, `--dir lib` **72 files / 809 tests**, `--dir scripts` **10 / 185**, both unchanged (the only tracked diff is JSON configs). **Colorado Springs and its addendum were DEFERRED and are the significant finding.** `metro-research/colorado-springs/` and `…-addendum/` no longer exist — destroyed by the 2026-08-03 `git clean -fdx`, absent from the research repo, all four nested worktrees, and git history entirely. That blocks **every** stage including the two that need no artifact, because `import-metro-merged.mjs` loads `config.input` at unconditional module scope before the stage dispatch, even though `--stage=publish` and `--stage=verify` read only from the database and the config. **Owner ruling: do not work around it** — a lazy-load patch touches the shared importer on every future batch's path and that module scope also runs the `exclude`, `same_site_pairs` and reconcile validations off `venues` (this config has 2 reconciles, so an empty array trips the stale-config abort); reconstructing the artifact from the database is circular, since `--stage=listings` would read that file on a later rebuild. The metro is **queued for a v3 source-led re-research** alongside McAllen, which rebuilds the artifact and is expected to find more than the 24 it has. **Its 17 live rows were untouched by this run and stay live.** Three findings from the deferred assessment are recorded in `scripts/metros/colorado-springs.json` under `_v3_reresearch_findings` so the re-research pass meets them: the coordinates are **not** the Brubaker failure mode (all three NULL-address rows name-matched onto the correct entity type in the right town, the weakest being a hamlet centroid standing in for Glen Park); `cheyenne-mountain-country-club` cites an **OSM way as its identity source and a different OSM object than its own coordinate came from**, on a members-only club; and **zero published rows site-wide cite `pppall.net` or `openstreetmap.org`** (0 of 965), so releasing that batch as-is would make it the site's first on both. **Pre-existing failure, reported not absorbed:** little-rock's `--stage=verify` ends **40/41**, failing only `reconciled row lr_conway_community_center records preserved_fields (merge ran)`. The `preserved_fields` node was introduced **2026-08-03 (#494)**; that row was reconciled **2026-07-30** by the bespoke `import-little-rock-merged.mjs`, which contains no reference to it. The merge genuinely never ran, four days before the merge existed, and `--stage=publish` cannot write provenance. The assertion is correct and the row is a historical artifact — do not weaken it to make the run green.
- **Court directory — Albany, Worcester and Bakersfield published, +63 (August 5, 2026)** — three wave-1 metros released together: **Albany-Schenectady-Troy 27/30**, **Worcester 22/24**, **Bakersfield-Delano 14/18** (`batch`/`source` = `albany-2026-08-03`, `worcester-2026-08-03`, `bakersfield-2026-08-03`). `--stage=verify` ended **ALL 47 / 46 / 41 CHECKS PASS ✓** and `EXPECTED-SPLIT ASSERTION: MATCHES ✓` at both `project` (artifact) and `publish` (recomputed from the DB). Live-verified rather than self-reported: the three metro pages serve exactly 27/22/14 venue links, the hub cards read the same three numbers, **all 63 venue slugs are in `sitemap.xml`**, and **all 12 negative cases 404 and are absent from the sitemap** — 9 held rows, the 2 excluded rows, and the retired OSM slug. **These three aborted at PRE-FLIGHT, not on a stale expectation**, and that distinction is the whole entry: ADR-17 pulled previously-held rows into the ADR-14 check's *would-publish* scope, and the document-URL check is deliberately **not** would-publish-scoped, so both fired. **19 uncitable URLs were replaced with citable ones** (5 Albany + 3 Worcester YMCA `name_source_url` values pointing at `playtimescheduler.com`; 3 Bakersfield + 8 Worcester `website` values holding a PDF/`CivicAlerts.asp`/`AgendaCenter`/`DocumentCenter` link). **In 11 of those cases the fix was one field, not two** — `website` merely duplicated the document already sitting on `name_source_url`, which the check exempts by design ("citing a PDF is what that column is FOR"), so the evidence was already correctly filed and only `website` needed repointing at the operator's venue page. **Read both columns before hunting for a replacement citation.** **`mil-windle-field` ships with NO `website`:** Millbury Public Schools owns Windle Field and publishes only bid/project pages for it, and `millburyma.gov/1367/Parks-Fields` does not list the venue at all — a row with no URL passes ADR-14 trivially, so this is an owner judgment call rather than something the pipeline swallows. **⚠ THE URL FIXES HAVE NO USER-VISIBLE EFFECT TODAY** and the live-page check for them is not evidence of anything: `lib/directory/loadFacilities.ts:17-23` lists `website`, `name_source_url` and `provenance` as **render-restricted** — the loader never selects them, so no venue page renders a website link at all. Grepping a live page for `playtimescheduler` returns 0 for *every* row in the directory, fixed or not. The pre-flight checks are defence-in-depth against the day someone adds those columns to a select, and against republication out of the database. **THE ALBANY RECONCILE IS THE FIND TO REMEMBER, BECAUSE NOTHING MECHANICAL WOULD HAVE CAUGHT IT.** `schenectady-sportime` ("SPORTIME Capital Region") is the same venue as the dormant OSM draft `sportime-schenectady-schenectady-ny` — identical street address `2699 Curry Road, Schenectady, NY 12303` — but the two generate **different slugs**, so the unique constraint stays silent, **and they sit 271 m apart against a `reconcile_radius_m` of 200**, so pre-flight's live proximity guard skips the pair entirely (`if (d >= RECONCILE_RADIUS_M) continue`). An INSERT would have published a second listing for one venue. It was found by a **manual SQL sweep of live rows inside the metro envelope**, not by any assertion — run that sweep on every new metro, because the guards are radius-bound and the slug is not a duplicate detector. **The radius was deliberately left at 200**: widening it to earn the `reconcile confirmed live` log line would disable the guard for every other neighbour in the metro, which is exactly the failure it exists to prevent, so that line is **absent from the Albany run by design** while Worcester's PickleballU reconcile (0 m) prints it normally. Both reconciles preserved `osm_id`, stashed `osm_original`, and preserved **zero** fields — the targets were empty shells. **Bakersfield excludes 2 and holds 1, and the difference between those instruments is the pattern.** `lake-isabella-mountain-view-church` and `bakersfield-silver-oak-park` are **EXCLUDED** (`expected_count` 20 → 18): both would have published carrying **no source URL of any kind** — aggregator-only evidence that `workbook-extract.mjs` correctly strips from the user-facing columns, leaving `website` and `name_source_url` both null — and both are private/HOA. A venue page with nothing to cite is not what `verification_status='listed'` means under ADR-18 (McAllen precedent). `riverview-community-center-bakersfield-ca` is instead **held via `research_status='held'`**, because the *venue* is fine and only the *coordinate* is wrong: the cached geocode for `401 Willow Drive / 93308` returned OSM `way/10436658` named **"Willow Creek Drive"** as a road centroid with no house number, the name-qualified query returned `[]`, and OSM has no Willow Drive in 93308 at all (the nearest is 5.3 km east in 93301). **That is a wrong pin wearing `low` precision, which reads as "imprecise" rather than "wrong"** — the Brubaker Park / `east-bank-bridge-park` shape. **`held` is the more precise instrument than `exclude`: real-venue-bad-coordinate holds (it stays in the work queue and a re-geocode releases it), not-a-venue excludes.** **`rt-12-pickle-west-boylston-ma` is held on a KNOWN FALSE POSITIVE, accepted deliberately.** `isGenericName()` strips `pickle` as a noise word and then strips digits, leaving `rt` — two characters — so a tier-1-sourced, `research_status='verified'`, high-precision commercial venue with its own website reads as unnamed. Measured across **all 1,354 venues in every metro artifact the rule yields exactly two such false positives** (this and Memphis's `Pickleball 901`) while correctly blocking **76 raw OSM drafts** named `"1"`, `"2"`, `"Pickleball Court"` and the like — so it is load-bearing and must not be gutted. Dropping only the `\d+` strip fixes both while still holding every 1–2-digit OSM name, **but that is a change to the ADR-17 shared module that `publish-facilities.mjs` re-evaluates against live rows, so it belongs in its own slice and must never be folded into a publish run.** Renaming the venue was rejected: it would put a false name in the directory permanently. **All three `expected_publish` blocks were re-authored**, not edited to match output — the stale pre-ADR-16/17 blocks (16/18/11, whose reason strings were mostly `research_status=probable` and `coordinate precision low`, both retired as gate conditions) were **removed**, the projections were run with no expectation present, and the new blocks were written from what they printed. **Concurrency note for future runs:** another session published five metros during this release, so the script's own `REMINDER: the site-wide listings total must have risen by N` is **unusable as a reconcile proof** whenever a second session is writing — site-wide came out **+77 against an expected +29**. Prove it batch-scoped instead: for `albany-2026-08-03`, 29 rows `created_at` within the run and **1 row created 2026-07-20 updated in place** carrying the original `id` and `osm_id`. No schema change, no migration. Gates: `tsc` **EXIT=0**, `next build` **EXIT=0 / 1,060 pages**, `vitest --dir scripts` **10 files / 185 tests**.
- **Court directory — research backup lands on main, scoped rather than blanket (August 4, 2026)** — `scripts/lib/backup-metro-research.mjs` reaches `main` for the **first time**, in fixed form. It was written on `chore/courts-publish-pipeline` and deliberately carved out of the #499 reconcile, so this is a fresh add rather than a repair of shipped code. **The version that was carved out ran `git add -A`, `git commit` and `git push` unconditionally against the shared `joinzer-metro-research` repo at the end of every extract.** That is not a theoretical hazard — it already fired: research-repo commit `cb79409`, *"Research artifacts: grand-rapids extract"*, contains three grand-rapids artifacts **and `hartford/hartford-candidates.json`**, another metro's in-flight work, swept in by `-A` under a message that denies it exists, and pushed. A second structural property of `-A` matters as much and had not been named: **it stages deletions**, so a backup could commit and push a loss — inverting its own purpose. **The fix is that the staged path set is derived from what the run actually did** (the artifact written, the raw dump read, the metro's own cache file) **and then INTERSECTED with `git status --porcelain -z -uall`.** That one intersection does four jobs: an **ignored** path never appears in status, so naming one can never raise `paths are ignored by one of your .gitignore files` (verified — `git add` on an ignored path **errors**, it is not a silent no-op); a **deleted** path is filtered out; an unchanged or absent path is simply not there; and everything status reports that the run did *not* author is **reported and left alone**, turning the `cb79409` sweep into a visible prompt with paste-ready explicit-path commands. `-uall` is required, not optional — the default `-unormal` collapses a brand-new metro directory to a single `toledo/` entry that cannot be intersected with file-level candidates. Two further guards, both proven against a scratch repo rather than assumed: `git add --ignore-removal` cannot put a deletion in the index (a plain `add` on the same paths stages `D`), and **`git commit -m … -- <paths>` does not sweep a co-staged file** — a bare `git commit` writes the whole index, which is exactly how `cb79409` acquired hartford. **THE COMMIT IS AUTOMATIC; THE PUSH IS NOT.** These are different risks: the failure the script exists to prevent is a *local* wipe, and since the research repo moved to a sibling path outside every working tree (2026-08-03) a **local commit already survives that exact event** — `git clean -fdx` does not follow the junction. A push protects against disk or machine loss, and it is an **external send to a second repo that ADR-10 does not cover** (ADR-10 is Joinzer-only), so firing one as a side effect of an unrelated operation would trip a standing escalation on every extract. `--push` is explicit, gated on one constant (`PUSH_BY_DEFAULT = false`), and every run ends by printing the **unpushed-commit count** plus the exact command — the Colorado Springs loss was a *pending step nobody could see*, and this makes the debt unmissable. Owner ruling recorded **in the file** where the next session will read it: a session may run `--push` unasked when it has just committed its own scoped artifacts *and* the report shows nothing unexpected; anything else is an owner decision. There is deliberately **no standalone "commit everything" mode** — this file cannot know which paths a run authored, and guessing is how `-A` got here. **B3 (un-ignoring `.geocode-cache/` in the research repo) remains OPEN and is not assumed either way.** The metro's own cache file is named in the candidate set unconditionally: while the ignore stands it never appears in status and is never staged; the day it is lifted it is carried automatically. Both outcomes are pinned by unit tests so the design is provably outcome-independent. **This corrects the previous entry's claim that un-ignoring the cache would carry it "with zero new code"** — that was true only of `git add -A`; it is the explicit cache path here that keeps it true. Only the metro's own file is ever named, never the legacy shared seeds (`nominatim*.json`), which are read-only by design and written by nothing. **The docstring's old "the geocode cache is deliberately NOT backed up (regenerable, and large)" rationale is gone**: the cache is an **audit artifact** — the winning anchor survives in the candidates file, but the runners-up, township-guard rejections and micro-feature skips exist *only* there, and re-fetching returns **today's** OSM rather than the OSM those rulings were made against. Placed at `scripts/lib/` to match `revalidate-directory.mjs`'s dual library+CLI shape and the same never-fatal ACTION REQUIRED posture. The `--no-backup` escape hatch and the dynamic `await import()` at the call site both survive — the latter is load-bearing, since `import-metro-merged.mjs` imports `workbook-extract.mjs` as a library and only the CLI path needs the backup module. **The two guards were also given different advice for the two "missing" cases**: `.git` is a *file* in a linked worktree and a *directory* in the main checkout, so a worktree is now told it was never bootstrapped rather than that a `git clean` removed a junction — the active `batch3-metros` worktree has no link at all and was getting the wrong diagnosis on every extract. Unit tests **924 → 943** (77 → 78 files); the `cb79409` regression is written in the real commit's exact four-file shape and was **proven to fail** under a mutation that models `-A` (`+ "hartford/hartford-candidates.json"` appearing in the staged set), along with two other tests, while the B3-not-taken test correctly stayed green. `tsc --noEmit`, `next build` and the full suite all green. **No schema change, no DB writes, no live Nominatim request, and nothing was executed against the research repo** — every git behavior was proven in throwaway `mkdtemp` repos. **Flagged, not fixed:** `--no-geocode` without `--out` defaults `out` to `config.input`, so a shape-check run **overwrites the real candidates artifact with an ungeocoded one** — scoped staging shrinks the blast radius but does not fix it, and it deserves its own slice; the research repo's own `README.md` and `.gitignore` comment still describe the old auto-push behavior and a stale "~1.8 MB, regenerable" cache rationale (both are writes to that repo, so out of scope here).

- **Worktree bootstrap + teardown lands on main, with the module junction removed (August 5, 2026)** — `scripts/bootstrap-worktree.mjs` reaches `main` for the **first time**. It was written 2026-08-03 (`ed08610`) and sat unmerged on the never-pushed local branch `chore/port-pipeline-scripts`, so **every session since bootstrapped by hand**. The original commit is cherry-picked verbatim and then fixed, so its reasoning about *why* worktree isolation kept being skipped survives as history. **The defect that had to be fixed first: it junctioned `node_modules` to the main checkout, which breaks `next build`** — Turbopack resolves the module root itself and rejects a link that leaves the project directory (`Symlink [project]/node_modules is invalid, it points out of the filesystem root → TurbopackInternalError`). Shipping that would have made the known-broken path the default for every future worktree; every slice that day unpicked it with `rmdir` + `npm ci` by hand. **The junction existed to save disk, and that premise does not survive measurement: `node_modules` is 0.49 GB against 337 GB free**, so six parallel worktrees cost under 1% of the disk. It bought ~1 minute of install time (measured: `npm ci` completes in **30–50 s**) and cost a working build gate. It was also the **only** reason `rmdir node_modules` appeared in teardown, so deleting the option deletes half the teardown hazard outright rather than documenting it better. **`npm ci`, not `npm install`** — it never rewrites the lockfile, so a bootstrap cannot leave a spurious `package-lock.json` diff in the worktree you are about to work in. **Three further gaps closed, all found in production use.** **(1) "already present, left alone" now verifies instead of trusting.** That branch tested `existsSync`, which cannot tell a junction from a real directory — which is how `.claude/worktrees/metro-wave-1` came to hold five metros' research as a **real** directory: the only copy, invisible to the backup, and reachable by `git clean -fdx`. New `classify()` uses `lstat` (a Windows junction reports `isSymbolicLink() === true`) plus `readlinkSync`, so a verified junction passes, a **wrong target** fails with the repoint commands, a real directory fails naming the metro-wave-1 shape, and a **symlinked env file** fails — enforcing the copied-not-linked invariant the header always asserted but never checked. **(2) Every metro config's `input` is resolved at bootstrap.** 47 of the 48 configs resolve through the `metro-research` junction; `little-rock.json` alone points at `little-rock-count/`, a **gitignored directory inside the Joinzer repo** that a worktree (tracked files only) does not carry — it would have ENOENT'd mid-run for a completely recoverable reason. `geocode_cache` is checked at its **dirname**, since a flush needs the directory, not the file. **Deliberately non-fatal** (only the metro you actually run matters, and a broken junction already exits 1 for the other 47) and **deliberately no auto-copy** — these are ADR-14 private research inputs, so it prints the exact `cpSync` command and lets a human choose. On its first real run it independently surfaced **3** unresolved paths, not the 1 it was built for: Little Rock plus **both Colorado Springs configs**, whose artifacts were destroyed by the 2026-08-03 `git clean -fdx` and are absent from the main checkout too. **(3) Teardown is now a command that can stop itself.** `--teardown <path>` replaces the documented `cmd /c rmdir` sequence, which **lied on 2026-08-04**: it printed its banner, returned success, and `lstat` still reported a junction — intermittently, since the same command had worked on `node_modules` minutes earlier. Trusting that exit code would have sent `git worktree remove --force` walking into the shared research repo. `rmdirSync` replaces it (no shell, no exit code to misread, and on Windows it removes the **link** while leaving the target intact — proven in a scratch directory, not assumed), and **the verification can abort the destructive step rather than reporting beside it**: any surviving junction stops the run and removes nothing. It also refuses to run from inside the target, asks git whether the path is a linked worktree rather than trusting the argument (a linked worktree's `.git` is a *file*, the main checkout's a *directory*, so it cannot tear down the main checkout), and counts each shared target's entries **before and after** — the only thing that would reveal a link that looked removed but was not. **Verified by a real bootstrap and a real teardown, not by inspection:** a throwaway worktree was created, bootstrapped (`node_modules` a real 397-entry directory, `metro-research` a junction, env files real copies), **`npx next build` EXIT=0 inside it** — the single line the whole change exists to produce — then torn down, with the shared research repo unchanged at **61 top-level entries, 12 geocode-cache files, 58 metro directories** before and after. Both refusal guards were exercised as negative controls. `classify`/`countEntries`/`unresolvedConfigPaths` were extracted to **`scripts/lib/worktree-paths.mjs`** (the dual library+CLI shape `revalidate-directory.mjs` established) and pinned by **12 new tests** against real filesystem objects in `mkdtemp` directories rather than a mocked `lstat`, because the whole point is what Windows actually reports for a junction; scripts suite **10 files / 185 tests → 11 / 197**. **No schema change, no migration, no DB writes, no live Nominatim request.** `next build` reads production Supabase via `generateStaticParams` — reads only, named here because a tooling change that touches production at all should say so. **Flagged, not fixed:** `chore/port-pipeline-scripts` is now superseded but still carries two unrelated uncommitted files (`scripts/lib/workbook-extract.mjs`, a staged `scripts/backup-metro-research.mjs` that already landed on `main` at `scripts/lib/`) — left untouched; and `.claude/worktrees/metro-wave-1`'s `metro-research` is still a real directory, which the new check now *reports* but deliberately does not migrate, since moving research data is a gated data operation rather than a bootstrap concern.

- **Google Analytics 4 — base install (July 31, 2026)** — GA4 page-view tracking is live app-wide. `GoogleAnalytics` from **`@next/third-parties@16.2.6`** (pinned to the installed Next version; peer range `^13 || ^14 || ^15 || ^16.0.0-beta.0` covers Next 16.2.6) is mounted in the **App Router root layout `app/layout.tsx`**, inside `<body>` next to the existing `sw-register` script, so it covers every route including `/courts`, `/l/`, `/t/` and the marketing pages. The measurement ID is read from **`NEXT_PUBLIC_GA_MEASUREMENT_ID`** and is never hardcoded; the component renders only when that var is set, so an unset env can't request `gtag/js?id=undefined`. **No Google Tag Manager, no custom events** — the component emits exactly two `next/script` tags (an inline `gtag('js')`+`gtag('config')` init and the gtag.js loader). **Client-side route changes are covered by GA4 Enhanced Measurement, not by app code, and this is deliberate.** The installed component has no `usePathname`/`useSearchParams` subscription and no route-change effect — its only `useEffect` is a one-time `performance.mark` feature signal — so it never emits a manual `page_view` after the first. App Router navigation is `history.pushState`, which GA4's Enhanced Measurement "page changes based on browser history events" already counts; adding a manual pageview here would **double-count every SPA navigation**. If SPA page views ever go missing, the fix is the GA4 data-stream toggle, not app code. **No existing analytics was displaced** — the repo had no gtag, GTM, or third-party analytics package before this, and there are no `@vercel/*` packages, so Vercel Web Analytics / Speed Insights are enabled at the dashboard level but not instrumented in the app; they measure different things (Vercel is server/RUM-side) and cannot double-count GA. **No schema change, no migration, no new env var**, and no CSP to amend (the repo sets none — `next.config.mjs` has no `headers()` and `middleware.ts` sets no security headers). **Known consequence:** `NEXT_PUBLIC_GA_MEASUREMENT_ID` is present in `.env.local`, so `npm run dev` and local `next start` also send hits to the live property — filter localhost in GA4 admin (internal-traffic rule) if that noise matters.

### Not yet built

- **Flex League — Phase 3** — per-round windows (`league_periods` `period_kind='window'` + per-fixture `window_start/end`). Phase 2 (above) shipped the deadline/forfeit cron + self-scheduling; only per-round windows remain. Plan in `docs/phases/flex-league.md`.
- **Team League — Phase 3+** — playoffs (reuse the tournament bracket engine over final team standings) and attendance/sub integration for team lineups. Phase 2 (above) shipped captain self-service (lineup/roster/scoring/captaincy). Plan in `docs/phases/team-league.md`.
- **Unified `competitions` schema** — designed in docs/architecture-target.md, not migrated
- **Organizer onboarding — Phase 1 shipped (July 8, 2026, PRs #302–#305)** — the funnel + guided first-event path now exists. `profiles.signup_intent` (`play|organize|both`, migration `20260708000005`) captured via a required "What brings you to Joinzer?" step in profile setup; `/organizers` gained a co-primary **"Start free — create your first event"** CTA (hybrid with the Calendly demo) that carries `intent=organize` through login + the OAuth callback into setup; after setup an organizer routes to a new **`/get-started`** (League-vs-Tournament cards + how-it-works) instead of the player-first home; and the empty `/home` leads a declared-organizer-who-hasn't-created-yet with a "Create your first event" card. **Phase 2 shipped (July 8, 2026, PRs #307–#309):** the tournament `SetupChecklist` steps are now clickable links (divisions anchor / edit / schedule builder); leagues got a parallel `LeagueSetupChecklist` (open registration → add players → start play, format-aware run link); and both create flows redirect with `?created=1` → an `OrganizerCreatedBanner` "you're live, share to get players" moment above the checklist. **Phase 3 (not built):** organizer-mode signaling, an in-app getting-started resource, leagues "+ Create" button parity with tournaments. Reality note: this is self-serve scaffolding; the #1 open decision (a real organizer conversation) is still unbooked.
- **SMS** — no Twilio / equivalent
- **DUPR integration** — no API connection, no per-division min/max rating
- **Audit log on every state change** — table + helper exist; tournament/league create and division edits still unwired.
- **Platform stats** (`platform_stats_mv` materialized view) — `StatsSection` component exists but removed from homepage (showed 0s with no real data)
- **Public browse** — full-featured (deep filtering, map view) is future; basic `/browse/leagues` and `/browse/tournaments` are shipped, and per-court pages now exist (see Court directory under Shipped)
- **Organization / business layer** — every tournament + league is owned by a single individual organizer; no `organizations` table, no multi-tournament business accounts

### In progress

- **Two-form-factor QA** — code complete and deployed. Public routes QA'd via Playwright. Authenticated desktop routes (tournaments/create, leagues/create, standings, roster) still need eyes with a real logged-in session at 375/768/1280px.

---

## 6. Open Decisions

These are the actual unresolved decisions blocking informed choices:

- **Schema reconciliation.** Live DB has separate `tournaments` and `leagues` domains. A unified `competitions` schema has been designed but not built. Path A (keep separate) vs. Path B (unify) — deferred until an organizer has been spoken to. Design in docs/architecture-target.md.
- **First committed event.** None. No organizer has seen the product yet. Product is shippable for a demo.
- **Organizer conversation.** Not yet booked. This is the #1 blocker — it unblocks Path A vs. B, pricing, and the onboarding flow design.

---

## 7. Coding Style

- TypeScript strict mode
- ES modules, `import`/`export`
- `async`/`await`, not `.then()` chains
- 2-space indent
- Descriptive variable names; single letters only for loop counters
- Comment *why*, not *what*
- Server Components by default; `"use client"` only when needed
- Tailwind only; lucide-react for icons; no shadcn/ui
- One file = one concern; split when a file passes ~200 lines

---

## 8. Working Agreement (Joinzer-Specific)

Global rules from `~/.claude/CLAUDE.md` apply. Joinzer adds:

- **Deploy autonomy (overrides the global "ask before pushing to remote").** For Joinzer, commit, push, merge PRs, and let `main` deploy to production (Vercel) **freely — no per-push confirmation.** Justified by the automated gates below + easy git/Vercel rollback. Still non-negotiable: never commit secrets; `tsc --noEmit` + `next build` (+ tests where they exist) must be green before shipping; confirm before genuinely destructive/irreversible **non-git** actions (dropping DB columns, deleting data). Only pause for a push if Marty explicitly asks to slow down for a specific risky change.
- If a doc claim contradicts the codebase, the codebase wins — flag and stop.
- The last step of any session that changes structure, schema, or status: update CLAUDE.md's "Current State" and verification date. **Drift is the enemy.**
- When finishing a phase or major slice: 10-minute pass through CLAUDE.md to re-verify "Current State" claims, move completed items out of "Open Decisions," update the verification date.
- Do not document fast-changing things (specific routes, specific tables, schema columns) in markdown. Point at the codebase or Supabase Table Editor instead.

### Gotchas learned in session (June 5, 2026)

**Supabase migration rule — apply BEFORE pushing code.**
New DB columns must exist in Supabase before the code that selects them is deployed. Selecting a non-existent column causes the query to return `null` → `notFound()` → 404. Apply migrations in the SQL editor first, verify, then push to Vercel.

**`sendEmail` returns `void` — never destructure `{ error }`.**
`lib/email/send.ts` wraps Resend and returns `Promise<void>`. Do NOT write `const { error } = await sendEmail(...)`. Just `await sendEmail(...)`.

**`league_matches` has no unique constraint.**
`upsert` with `onConflict` on `(session_id, round_number, court_number)` fails — no constraint exists. For edit flows, DELETE the existing row then INSERT the new one.

**`Player` type in `LiveSessionManager` uses snake_case.**
The type has `user_id` (not `userId`). All session player fields are snake_case — don't mix in camelCase.

**Supabase join type inference conflicts.**
When a query uses a foreign-key join (e.g. `profile:profiles!user_id(...)`), TypeScript may reject explicit type assertions on the result. Cast with `as any[]` first, then access fields typed as `any`.

**Fixed-partner mode only activates when pairs are linked in the DB.**
`isFixedMode` in the league scheduler is `true` only when `fixedPairs.size > 0`. If no `partner_user_id` is set in `league_registrations`, the scheduler silently falls back to rotating mode. Organizer must assign pairs via the Roster page before generating rounds.

**Dev server — `npm run dev` is webpack + polling, not Turbopack.**
Turbopack's file watcher goes stale on Windows (serves old compiled code → phantom hydration mismatches, a `(stale)` badge, old UI rendering). The default `dev` script runs `next dev --webpack` with `WATCHPACK_POLLING` for reliable HMR; `npm run dev:turbo` is the faster Turbopack path when it's behaving. If you ever see stale UI, `rm -rf .next && npm run dev`. (Added `cross-env` dev dep so the env var works in both Git Bash and PowerShell.)

**Run commands in a permission-allowlist-friendly way (fewer approval prompts).**
The Bash tool's working directory already persists at the repo root — do **not** prefix commands with `cd .../Joinzer &&`; a `cd` in a compound command defeats the allow rules and forces a prompt. Prefer plain invocations (`git checkout …`, `npx tsc --noEmit`, `gh pr create …`) that match the `Bash(git:*)` / `Bash(gh:*)` / `Bash(npx:*)` allow rules in `.claude/settings.local.json`. For commits use repeated `-m` flags (`git commit -m "title" -m "body" -m "Co-Authored-By: …"`) instead of a `$(cat <<'EOF' …)` heredoc, and merge with a single `gh pr merge N --squash --delete-branch` (no retry `for`-loop) — heredocs and loops can't be auto-approved, so they always prompt.

---

## Quick Links

- Two-form-factor refactor plan: `/docs/phases/two-form-factor.md`
- Target architecture (aspirational): docs/architecture-target.md
- Security rules: @docs/security.md
- Partner invite flow + required Supabase Auth config: `/docs/partner-invite-flow.md`

---

*Last verified against repo: July 16, 2026 (substitution project complete through Phase 6). User-submitted venue path verified against repo + live database August 6, 2026. Substitutions re-verified against repo + live database August 6, 2026 for the post-generation record close + pre-session unfilled warning only — the rest of this section is not re-verified here.*

*Court directory publish pipeline re-verified against the repo AND the production database August 5,
2026 (ADR-16/17/18 — the publish gate, the release fence, and the `listed` tier). **Scope-limited:
that pass covered the directory publish path only** — the gate, both publish scripts,
`facility_listings` gate-relevant columns, and the `/courts` render surfaces. Every other section
above still carries the July 16 date and was not re-checked.*
*Lancaster metro verified against production August 5, 2026 (34 published — both batches, the three
follow-ups, and the render surfaces named in that entry). **Scope-limited to Lancaster:** no other
metro's counts, and nothing outside the court directory, was re-checked by that pass.*
*Little Rock, Pensacola and El Paso verified against production August 5, 2026 (19 / 12 / 24
published, plus the site total of 965 and Colorado Springs' 17 unchanged — see the release entry
above). **Scope-limited to those four metros:** no other metro's counts, and nothing outside the
court directory, was re-checked by that pass.*
*Boise, Albuquerque and North Port–Bradenton–Sarasota verified against production August 5, 2026,
AFTER the pagination fix deployed: metro pages serving **11 / 23 / 22**, El Paso restored to 200
from a 404, `/courts` reporting **1,084 facilities**, `/sitemap.xml` carrying **1137 URLs = 5 static
+ 48 metro + 1,084 facility** (it served exactly 1000 facility URLs and 46 metros before the fix),
and all five still-held slugs 404 and absent from the sitemap. **Scope-limited to those three metros
plus the site-wide sitemap/metro/index counts** — the truncation is what made those site-wide
numbers worth stating, and no other metro's individual count was re-checked. The 1,084 and 48
totals include the +63 from the Albany/Worcester/Bakersfield release above, which this pass did not
otherwise verify.*
*Court-directory section verified July 30, 2026 (crawl-budget incident fix — ISR for `/courts`/`/courts/[slug]`, `unstable_cache` data layer, scoped robots.txt Disallow, prefetch-storm fix; see entry above). Prior verification July 29, 2026 (Playwright suite committed + active-filter fix PR #471). Note: the July 29 entry above for PR #472 (canonical-host fix) still reads "OPEN, NOT DEPLOYED," but `git log origin/main` confirms commit `55530e6` (#472) is merged — that line is stale and wasn't corrected here since reconciling it is outside this fix's scope; flagged for docs-auditor. The rest of this file has NOT been re-verified since July 16 — the July 20–28 directory workstream (facility_listings/facility_candidates schema, Vegas parity, ADR-12/13/14, the Phoenix batch) is only partially reflected here. Full reconciliation handed to docs-auditor.*
